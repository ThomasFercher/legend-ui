name: legend_workspace
description: Dart workspace for the Legend UI Kit rewrite (kit + legend_gen CLI).
publish_to: none

environment:
  sdk: ^3.10.0

workspace:
  - packages/legend_ui
  - packages/legend_gen
  - example
