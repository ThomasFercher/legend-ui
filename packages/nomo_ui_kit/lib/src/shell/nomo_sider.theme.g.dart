// GENERATED by nomo_gen v0.1.0-dev.0 — do not edit.
// Source: nomo_sider.dart
// ignore_for_file: type=lint, unused_import

import 'nomo_sider.dart';
import 'package:flutter/widgets.dart';
import 'package:nomo_ui_kit/nomo_ui_kit.dart';
import 'package:nomo_ui_kit/src/annotations/annotations.dart';
import 'package:nomo_ui_kit/src/primitives/nomo_interactive.dart';
import 'package:nomo_ui_kit/src/primitives/nomo_surface.dart';
import 'package:nomo_ui_kit/src/shell/nomo_nav_item.dart';
import 'package:nomo_ui_kit/src/theme/nomo_theme.dart';

/// Resolved theme for [NomoSider].
class NomoSiderTheme {
  const NomoSiderTheme({
    required this.background,
    required this.width,
    required this.selectedColor,
    required this.unselectedColor,
    required this.itemPadding,
  });

  /// Token-derived defaults (level 5).
  factory NomoSiderTheme.defaults(NomoTokens t) => NomoSiderTheme(
    background: t.colors.surface,
    width: 240.0,
    selectedColor: t.colors.primary,
    unselectedColor: t.colors.foreground2,
    itemPadding: EdgeInsets.symmetric(
      horizontal: t.sizes.md,
      vertical: t.sizes.sm,
    ),
  );

  final Color background;
  final double width;
  final Color selectedColor;
  final Color unselectedColor;
  final EdgeInsetsGeometry itemPadding;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static NomoSiderTheme of(
    BuildContext context, [
    NomoSiderThemeNullable? local,
  ]) {
    final data = NomoTheme.of(context);
    return NomoSiderTheme.defaults(data.tokens)
        .merge(data.component<NomoSiderThemeNullable>())
        .merge(NomoThemeOverride.maybeOf<NomoSiderThemeNullable>(context))
        .merge(local);
  }

  NomoSiderTheme merge(NomoSiderThemeNullable? other) {
    if (other == null) return this;
    return NomoSiderTheme(
      background: other.background ?? background,
      width: other.width ?? width,
      selectedColor: other.selectedColor ?? selectedColor,
      unselectedColor: other.unselectedColor ?? unselectedColor,
      itemPadding: other.itemPadding ?? itemPadding,
    );
  }

  NomoSiderTheme copyWith({
    Color? background,
    double? width,
    Color? selectedColor,
    Color? unselectedColor,
    EdgeInsetsGeometry? itemPadding,
  }) => NomoSiderTheme(
    background: background ?? this.background,
    width: width ?? this.width,
    selectedColor: selectedColor ?? this.selectedColor,
    unselectedColor: unselectedColor ?? this.unselectedColor,
    itemPadding: itemPadding ?? this.itemPadding,
  );

  static NomoSiderTheme lerp(NomoSiderTheme a, NomoSiderTheme b, double t) =>
      NomoSiderTheme(
        background: t < 0.5 ? a.background : b.background,
        width: t < 0.5 ? a.width : b.width,
        selectedColor: Color.lerp(a.selectedColor, b.selectedColor, t)!,
        unselectedColor: Color.lerp(a.unselectedColor, b.unselectedColor, t)!,
        itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
      );
}

/// Sparse overrides for [NomoSiderTheme]; null means "inherit".
class NomoSiderThemeNullable {
  const NomoSiderThemeNullable({
    this.background,
    this.width,
    this.selectedColor,
    this.unselectedColor,
    this.itemPadding,
  });

  final Color? background;
  final double? width;
  final Color? selectedColor;
  final Color? unselectedColor;
  final EdgeInsetsGeometry? itemPadding;

  NomoSiderThemeNullable merge(NomoSiderThemeNullable? other) {
    if (other == null) return this;
    return NomoSiderThemeNullable(
      background: other.background ?? background,
      width: other.width ?? width,
      selectedColor: other.selectedColor ?? selectedColor,
      unselectedColor: other.unselectedColor ?? unselectedColor,
      itemPadding: other.itemPadding ?? itemPadding,
    );
  }
}

/// Overrides [NomoSiderTheme] for a subtree (level 2).
class NomoSiderThemeOverride extends StatelessWidget {
  const NomoSiderThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final NomoSiderThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      NomoThemeOverride<NomoSiderThemeNullable>(data: data, child: child);
}
