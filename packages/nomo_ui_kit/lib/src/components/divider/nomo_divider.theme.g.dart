// GENERATED by nomo_gen v0.1.0-dev.0 — do not edit.
// Source: nomo_divider.dart
// ignore_for_file: type=lint

import 'package:flutter/widgets.dart';
import 'package:nomo_ui_kit/nomo_ui_kit.dart';
import 'package:nomo_ui_kit/src/annotations/annotations.dart';
import 'package:nomo_ui_kit/src/primitives/nomo_surface.dart';

/// Resolved theme for [NomoDivider].
class NomoDividerTheme {
  const NomoDividerTheme({
    required this.color,
    required this.thickness,
    required this.spacing,
  });

  /// Token-derived defaults (level 5).
  factory NomoDividerTheme.defaults(NomoTokens t) => NomoDividerTheme(
    color: t.colors.background3,
    thickness: t.sizes.borderWidth,
    spacing: t.sizes.md,
  );

  final Color color;
  final double thickness;
  final double spacing;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static NomoDividerTheme of(
    BuildContext context, [
    NomoDividerThemeNullable? local,
  ]) {
    final data = NomoTheme.of(context);
    return NomoDividerTheme.defaults(data.tokens)
        .merge(data.component<NomoDividerThemeNullable>())
        .merge(NomoThemeOverride.maybeOf<NomoDividerThemeNullable>(context))
        .merge(local);
  }

  NomoDividerTheme merge(NomoDividerThemeNullable? other) {
    if (other == null) return this;
    return NomoDividerTheme(
      color: other.color ?? color,
      thickness: other.thickness ?? thickness,
      spacing: other.spacing ?? spacing,
    );
  }

  NomoDividerTheme copyWith({
    Color? color,
    double? thickness,
    double? spacing,
  }) => NomoDividerTheme(
    color: color ?? this.color,
    thickness: thickness ?? this.thickness,
    spacing: spacing ?? this.spacing,
  );

  static NomoDividerTheme lerp(
    NomoDividerTheme a,
    NomoDividerTheme b,
    double t,
  ) => NomoDividerTheme(
    color: t < 0.5 ? a.color : b.color,
    thickness: t < 0.5 ? a.thickness : b.thickness,
    spacing: t < 0.5 ? a.spacing : b.spacing,
  );
}

/// Sparse overrides for [NomoDividerTheme]; null means "inherit".
class NomoDividerThemeNullable {
  const NomoDividerThemeNullable({this.color, this.thickness, this.spacing});

  final Color? color;
  final double? thickness;
  final double? spacing;

  NomoDividerThemeNullable merge(NomoDividerThemeNullable? other) {
    if (other == null) return this;
    return NomoDividerThemeNullable(
      color: other.color ?? color,
      thickness: other.thickness ?? thickness,
      spacing: other.spacing ?? spacing,
    );
  }
}

/// Overrides [NomoDividerTheme] for a subtree (level 2).
class NomoDividerThemeOverride extends StatelessWidget {
  const NomoDividerThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final NomoDividerThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      NomoThemeOverride<NomoDividerThemeNullable>(data: data, child: child);
}
