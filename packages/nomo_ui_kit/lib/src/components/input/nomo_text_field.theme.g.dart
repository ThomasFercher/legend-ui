// GENERATED by nomo_gen v0.1.0-dev.0 — do not edit.
// Source: nomo_text_field.dart
// ignore_for_file: type=lint

import 'package:flutter/services.dart';
import 'package:flutter/widgets.dart';
import 'package:nomo_ui_kit/nomo_ui_kit.dart';
import 'package:nomo_ui_kit/src/annotations/annotations.dart';
import 'package:nomo_ui_kit/src/primitives/nomo_surface.dart';
import 'package:nomo_ui_kit/src/theme/nomo_theme.dart';

/// Resolved theme for [NomoTextField].
class NomoTextFieldTheme {
  const NomoTextFieldTheme({
    required this.background,
    required this.borderRadius,
    required this.padding,
    required this.textStyle,
    required this.borderColor,
    required this.focusedBorderColor,
  });

  /// Token-derived defaults (level 5).
  factory NomoTextFieldTheme.defaults(NomoTokens t) => NomoTextFieldTheme(
    background: t.colors.background1,
    borderRadius: t.sizes.borderRadiusMd,
    padding: EdgeInsets.symmetric(horizontal: t.sizes.md, vertical: t.sizes.sm),
    textStyle: t.typography.b1,
    borderColor: t.colors.background3,
    focusedBorderColor: t.colors.primary,
  );

  final Color background;
  final BorderRadius borderRadius;
  final EdgeInsetsGeometry padding;
  final TextStyle textStyle;
  final Color borderColor;
  final Color focusedBorderColor;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static NomoTextFieldTheme of(
    BuildContext context, [
    NomoTextFieldThemeNullable? local,
  ]) {
    final data = NomoTheme.of(context);
    return NomoTextFieldTheme.defaults(data.tokens)
        .merge(data.component<NomoTextFieldThemeNullable>())
        .merge(NomoThemeOverride.maybeOf<NomoTextFieldThemeNullable>(context))
        .merge(local);
  }

  NomoTextFieldTheme merge(NomoTextFieldThemeNullable? other) {
    if (other == null) return this;
    return NomoTextFieldTheme(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      textStyle: other.textStyle ?? textStyle,
      borderColor: other.borderColor ?? borderColor,
      focusedBorderColor: other.focusedBorderColor ?? focusedBorderColor,
    );
  }

  NomoTextFieldTheme copyWith({
    Color? background,
    BorderRadius? borderRadius,
    EdgeInsetsGeometry? padding,
    TextStyle? textStyle,
    Color? borderColor,
    Color? focusedBorderColor,
  }) => NomoTextFieldTheme(
    background: background ?? this.background,
    borderRadius: borderRadius ?? this.borderRadius,
    padding: padding ?? this.padding,
    textStyle: textStyle ?? this.textStyle,
    borderColor: borderColor ?? this.borderColor,
    focusedBorderColor: focusedBorderColor ?? this.focusedBorderColor,
  );

  static NomoTextFieldTheme lerp(
    NomoTextFieldTheme a,
    NomoTextFieldTheme b,
    double t,
  ) => NomoTextFieldTheme(
    background: t < 0.5 ? a.background : b.background,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    padding: t < 0.5 ? a.padding : b.padding,
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
    borderColor: t < 0.5 ? a.borderColor : b.borderColor,
    focusedBorderColor: t < 0.5 ? a.focusedBorderColor : b.focusedBorderColor,
  );
}

/// Sparse overrides for [NomoTextFieldTheme]; null means "inherit".
class NomoTextFieldThemeNullable {
  const NomoTextFieldThemeNullable({
    this.background,
    this.borderRadius,
    this.padding,
    this.textStyle,
    this.borderColor,
    this.focusedBorderColor,
  });

  final Color? background;
  final BorderRadius? borderRadius;
  final EdgeInsetsGeometry? padding;
  final TextStyle? textStyle;
  final Color? borderColor;
  final Color? focusedBorderColor;

  NomoTextFieldThemeNullable merge(NomoTextFieldThemeNullable? other) {
    if (other == null) return this;
    return NomoTextFieldThemeNullable(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      textStyle: other.textStyle ?? textStyle,
      borderColor: other.borderColor ?? borderColor,
      focusedBorderColor: other.focusedBorderColor ?? focusedBorderColor,
    );
  }
}

/// Overrides [NomoTextFieldTheme] for a subtree (level 2).
class NomoTextFieldThemeOverride extends StatelessWidget {
  const NomoTextFieldThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final NomoTextFieldThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      NomoThemeOverride<NomoTextFieldThemeNullable>(data: data, child: child);
}
