// GENERATED by nomo_gen v0.1.0-dev.0 — do not edit.
// Source: nomo_card.dart
// ignore_for_file: type=lint

import 'package:flutter/widgets.dart';
import 'package:nomo_ui_kit/nomo_ui_kit.dart';
import 'package:nomo_ui_kit/src/annotations/annotations.dart';
import 'package:nomo_ui_kit/src/primitives/nomo_interactive.dart';
import 'package:nomo_ui_kit/src/primitives/nomo_surface.dart';

/// Resolved theme for [NomoCard].
class NomoCardTheme {
  const NomoCardTheme({
    required this.background,
    required this.borderRadius,
    required this.padding,
    required this.shadows,
  });

  /// Token-derived defaults (level 5).
  factory NomoCardTheme.defaults(NomoTokens t) => NomoCardTheme(
    background: t.colors.surface,
    borderRadius: t.sizes.borderRadiusLg,
    padding: EdgeInsets.all(t.sizes.md),
    shadows: t.shadows.low,
  );

  final Color background;
  final BorderRadius borderRadius;
  final EdgeInsetsGeometry padding;
  final List<BoxShadow> shadows;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static NomoCardTheme of(
    BuildContext context, [
    NomoCardThemeNullable? local,
  ]) {
    final data = NomoTheme.of(context);
    return NomoCardTheme.defaults(data.tokens)
        .merge(data.component<NomoCardThemeNullable>())
        .merge(NomoThemeOverride.maybeOf<NomoCardThemeNullable>(context))
        .merge(local);
  }

  NomoCardTheme merge(NomoCardThemeNullable? other) {
    if (other == null) return this;
    return NomoCardTheme(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      shadows: other.shadows ?? shadows,
    );
  }

  NomoCardTheme copyWith({
    Color? background,
    BorderRadius? borderRadius,
    EdgeInsetsGeometry? padding,
    List<BoxShadow>? shadows,
  }) => NomoCardTheme(
    background: background ?? this.background,
    borderRadius: borderRadius ?? this.borderRadius,
    padding: padding ?? this.padding,
    shadows: shadows ?? this.shadows,
  );

  static NomoCardTheme lerp(NomoCardTheme a, NomoCardTheme b, double t) =>
      NomoCardTheme(
        background: Color.lerp(a.background, b.background, t)!,
        borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
        padding: t < 0.5 ? a.padding : b.padding,
        shadows: t < 0.5 ? a.shadows : b.shadows,
      );
}

/// Sparse overrides for [NomoCardTheme]; null means "inherit".
class NomoCardThemeNullable {
  const NomoCardThemeNullable({
    this.background,
    this.borderRadius,
    this.padding,
    this.shadows,
  });

  final Color? background;
  final BorderRadius? borderRadius;
  final EdgeInsetsGeometry? padding;
  final List<BoxShadow>? shadows;

  NomoCardThemeNullable merge(NomoCardThemeNullable? other) {
    if (other == null) return this;
    return NomoCardThemeNullable(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      shadows: other.shadows ?? shadows,
    );
  }
}

/// Overrides [NomoCardTheme] for a subtree (level 2).
class NomoCardThemeOverride extends StatelessWidget {
  const NomoCardThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final NomoCardThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      NomoThemeOverride<NomoCardThemeNullable>(data: data, child: child);
}
