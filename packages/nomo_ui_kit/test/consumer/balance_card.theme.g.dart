// GENERATED by nomo_gen v0.1.0-dev.0 — do not edit.
// Source: balance_card.dart
// ignore_for_file: type=lint, unused_import

import 'balance_card.dart';
import 'package:flutter/widgets.dart';
import 'package:nomo_ui_kit/nomo_ui_kit.dart';

/// Resolved theme for [BalanceCard].
class BalanceCardTheme {
  const BalanceCardTheme({
    required this.background,
    required this.accent,
    required this.padding,
  });

  /// Token-derived defaults (level 5).
  factory BalanceCardTheme.defaults(NomoTokens t) => BalanceCardTheme(
    background: t.colors.surface,
    accent: t.colors.secondary,
    padding: EdgeInsets.all(t.sizes.lg),
  );

  final Color background;
  final Color accent;
  final EdgeInsetsGeometry padding;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static BalanceCardTheme of(
    BuildContext context, [
    BalanceCardThemeNullable? local,
  ]) {
    final data = NomoTheme.of(context);
    return BalanceCardTheme.defaults(data.tokens)
        .merge(data.component<BalanceCardThemeNullable>())
        .merge(NomoThemeOverride.maybeOf<BalanceCardThemeNullable>(context))
        .merge(local);
  }

  BalanceCardTheme merge(BalanceCardThemeNullable? other) {
    if (other == null) return this;
    return BalanceCardTheme(
      background: other.background ?? background,
      accent: other.accent ?? accent,
      padding: other.padding ?? padding,
    );
  }

  BalanceCardTheme copyWith({
    Color? background,
    Color? accent,
    EdgeInsetsGeometry? padding,
  }) => BalanceCardTheme(
    background: background ?? this.background,
    accent: accent ?? this.accent,
    padding: padding ?? this.padding,
  );

  static BalanceCardTheme lerp(
    BalanceCardTheme a,
    BalanceCardTheme b,
    double t,
  ) => BalanceCardTheme(
    background: Color.lerp(a.background, b.background, t)!,
    accent: t < 0.5 ? a.accent : b.accent,
    padding: t < 0.5 ? a.padding : b.padding,
  );
}

/// Sparse overrides for [BalanceCardTheme]; null means "inherit".
class BalanceCardThemeNullable {
  const BalanceCardThemeNullable({this.background, this.accent, this.padding});

  final Color? background;
  final Color? accent;
  final EdgeInsetsGeometry? padding;

  BalanceCardThemeNullable merge(BalanceCardThemeNullable? other) {
    if (other == null) return this;
    return BalanceCardThemeNullable(
      background: other.background ?? background,
      accent: other.accent ?? accent,
      padding: other.padding ?? padding,
    );
  }
}

/// Overrides [BalanceCardTheme] for a subtree (level 2).
class BalanceCardThemeOverride extends StatelessWidget {
  const BalanceCardThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final BalanceCardThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      NomoThemeOverride<BalanceCardThemeNullable>(data: data, child: child);
}
