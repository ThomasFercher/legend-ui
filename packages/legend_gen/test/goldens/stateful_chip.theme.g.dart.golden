// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: stateful_chip.dart
// ignore_for_file: type=lint, unused_element
part of 'stateful_chip.dart';

/// Resolved theme for [StatefulChip].
class StatefulChipTheme {
  const StatefulChipTheme({required this.accent, required this.label});

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory StatefulChipTheme.defaults(LegendTokens t) => StatefulChipTheme(
    accent: StatefulChip._accent(t),
    label: StatefulChip._label(t),
  );

  final ChipAccent accent;
  final Color label;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [StatefulChip] first, [StatefulChipThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static StatefulChipTheme of(
    BuildContext context, [
    StatefulChipThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<StatefulChipThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$StatefulChipAspectAccent);
    LegendThemeOverride.depend<StatefulChipThemeNullable>(
      context,
      _$StatefulChipOverrideAspectAccent,
    );
    return StatefulChipTheme.defaults(data.tokens)
        .merge(data.componentOf<StatefulChipThemeNullable>(StatefulChip))
        .merge(override)
        .merge(local);
  }

  StatefulChipTheme merge(StatefulChipThemeNullable? other) {
    if (other == null) return this;
    return StatefulChipTheme(
      accent: accent.merge(other.accent),
      label: other.label ?? label,
    );
  }

  StatefulChipTheme copyWith({ChipAccent? accent, Color? label}) =>
      StatefulChipTheme(
        accent: accent ?? this.accent,
        label: label ?? this.label,
      );

  static StatefulChipTheme lerp(
    StatefulChipTheme a,
    StatefulChipTheme b,
    double t,
  ) => StatefulChipTheme(
    accent: ChipAccent.lerp(a.accent, b.accent, t)!,
    label: t < 0.5 ? a.label : b.label,
  );
}

/// Sparse overrides for [StatefulChipTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class StatefulChipThemeNullable {
  const StatefulChipThemeNullable({this.accent, this.label});

  final ChipAccent? accent;
  final Color? label;

  StatefulChipThemeNullable merge(StatefulChipThemeNullable? other) {
    if (other == null) return this;
    return StatefulChipThemeNullable(
      accent: accent?.merge(other.accent) ?? other.accent,
      label: other.label ?? label,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is StatefulChipThemeNullable &&
          other.accent == accent &&
          other.label == label;

  @override
  int get hashCode => Object.hashAll([accent, label]);
}

/// Overrides [StatefulChipTheme] for a subtree (level 2).
class StatefulChipThemeOverride extends StatelessWidget {
  const StatefulChipThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final StatefulChipThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<StatefulChipThemeNullable>(data: data, child: child);
}

/// Resolves [StatefulChip.accent] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
ChipAccent _$StatefulChipSelectAccent(LegendThemeData data) =>
    StatefulChip._accent(
      data.tokens,
    ).merge(data.componentOf<StatefulChipThemeNullable>(StatefulChip)?.accent);
const _$StatefulChipAspectAccent = LegendThemeAspect(
  _$StatefulChipSelectAccent,
);
Object? _$StatefulChipOverrideSelectAccent(StatefulChipThemeNullable data) =>
    data.accent;
const _$StatefulChipOverrideAspectAccent =
    LegendOverrideAspect<StatefulChipThemeNullable>(
      _$StatefulChipOverrideSelectAccent,
    );

/// Resolves [StatefulChip.label] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$StatefulChipSelectLabel(LegendThemeData data) =>
    data.componentOf<StatefulChipThemeNullable>(StatefulChip)?.label ??
    StatefulChip._label(data.tokens);
const _$StatefulChipAspectLabel = LegendThemeAspect(_$StatefulChipSelectLabel);
Object? _$StatefulChipOverrideSelectLabel(StatefulChipThemeNullable data) =>
    data.label;
const _$StatefulChipOverrideAspectLabel =
    LegendOverrideAspect<StatefulChipThemeNullable>(
      _$StatefulChipOverrideSelectLabel,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class StatefulChipThemeListenables {
  /// Change stream of the resolved [StatefulChipTheme.accent].
  static ValueListenable<ChipAccent> accent(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$StatefulChipSelectAccent);

  /// Change stream of the resolved [StatefulChipTheme.label].
  static ValueListenable<Color> label(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$StatefulChipSelectLabel);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$StatefulChipThemeResolve on StatefulChip {
  /// [StatefulChipTheme.of] with this widget's constructor params as
  /// level 1.
  StatefulChipTheme _theme(BuildContext context) => StatefulChipTheme.of(
    context,
    StatefulChipThemeNullable(accent: accent, label: label),
  );

  /// Resolves ONLY [StatefulChip.accent] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._accent(context)`).
  ChipAccent _accent(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<StatefulChipThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$StatefulChipAspectAccent);
    LegendThemeOverride.depend<StatefulChipThemeNullable>(
      context,
      _$StatefulChipOverrideAspectAccent,
    );
    return _$StatefulChipSelectAccent(
      data,
    ).merge(override?.accent).merge(accent);
  }

  /// Resolves ONLY [StatefulChip.label] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._label(context)`).
  Color _label(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<StatefulChipThemeNullable>(
      context,
    );
    return label ?? override?.label ?? _$StatefulChipSelectLabel(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_StatefulChipState]
/// is this file's `State<StatefulChip>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$StatefulChipThemeState])
/// wins over this extension.
extension _$StatefulChipThemeOn_StatefulChipState on _StatefulChipState {
  StatefulChipTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<StatefulChip>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$StatefulChipThemeState on State<StatefulChip> {
  StatefulChipTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class StatefulChip extends
/// _$StatefulChipBase` receives the resolved
/// [StatefulChipTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$StatefulChipBase
    extends LegendStatelessWidget<StatefulChipTheme> {
  const _$StatefulChipBase({super.key});

  ChipAccent? get accent;
  Color? get label;

  @override
  StatefulChipTheme resolveThemeOf(BuildContext context) =>
      StatefulChipTheme.of(
        context,
        StatefulChipThemeNullable(accent: accent, label: label),
      );
}
