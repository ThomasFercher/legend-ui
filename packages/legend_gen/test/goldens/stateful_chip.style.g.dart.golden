// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: stateful_chip.dart
// ignore_for_file: type=lint, unused_element
part of 'stateful_chip.dart';

/// Generated mechanical members for [ChipAccent] (RFC-002 R6
/// amendment 7): member-wise sparse [merge] and value
/// `==`/`hashCode`. Applied via `with _$ChipAccent`; fields are
/// read through a private cast (no abstract getters, so the
/// class declares no overrides).
mixin _$ChipAccent {
  ChipAccent get _self => this as ChipAccent;

  /// Member-wise sparse merge: [other]'s set members win,
  /// unset members inherit — a theme level that names only
  /// one member never clobbers another level's members. Null
  /// passes this instance through unchanged.
  ChipAccent merge(ChipAccent? other) {
    if (other == null) return _self;
    return ChipAccent(
      fill: other.fill ?? _self.fill,
      outline: other.outline ?? _self.outline,
      weight: other.weight ?? _self.weight,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ChipAccent &&
          other.fill == _self.fill &&
          other.outline == _self.outline &&
          other.weight == _self.weight;

  @override
  int get hashCode => Object.hashAll([_self.fill, _self.outline, _self.weight]);
}

/// Member-wise lerp for [ChipAccent] with the type-appropriate
/// lerper per member (nested style classes via their own
/// `lerp` static, members without a lerper step at t=0.5);
/// the public [ChipAccent.lerp] redirects here.
ChipAccent? _$ChipAccentLerp(ChipAccent? a, ChipAccent? b, double t) {
  if (identical(a, b)) return a;
  return ChipAccent(
    fill: Color.lerp(a?.fill, b?.fill, t),
    outline: Color.lerp(a?.outline, b?.outline, t),
    weight: _$ChipAccentLerpDouble(a?.weight, b?.weight, t),
  );
}

/// Null-aware double lerp for [_$ChipAccentLerp] — steps
/// when either side is unset (fading a length against
/// null has no meaning).
double? _$ChipAccentLerpDouble(double? a, double? b, double t) =>
    a == null || b == null ? (t < 0.5 ? a : b) : a + (b - a) * t;
