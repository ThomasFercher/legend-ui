// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: fancy_box.dart
// ignore_for_file: type=lint, unused_element
part of 'fancy_box.dart';

/// Resolved theme for [FancyBox].
class FancyBoxTheme {
  const FancyBoxTheme({
    required this.background,
    required this.gap,
    required this.padding,
    required this.stripeWidth,
    required this.outline,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory FancyBoxTheme.defaults(LegendTokens t) => FancyBoxTheme(
    background: LegendColorsRef.surface(t),
    gap: 8.0,
    padding: FancyBox._padding(t),
    stripeWidth: _stripeWidth(t),
    outline: null,
  );

  final Color background;
  final double gap;
  final EdgeInsetsGeometry padding;
  final double stripeWidth;
  final Color? outline;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [FancyBox] first, [FancyBoxThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static FancyBoxTheme of(
    BuildContext context, [
    FancyBoxThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<FancyBoxThemeNullable>(context);
    LegendTheme.depend(context, _$FancyBoxAspectBackground);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$FancyBoxAspectGap);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectGap,
    );
    LegendTheme.depend(context, _$FancyBoxAspectPadding);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$FancyBoxAspectStripeWidth);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectStripeWidth,
    );
    LegendTheme.depend(context, _$FancyBoxAspectOutline);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectOutline,
    );
    return FancyBoxTheme.defaults(data.tokens)
        .merge(data.componentOf<FancyBoxThemeNullable>(FancyBox))
        .merge(override)
        .merge(local);
  }

  FancyBoxTheme merge(FancyBoxThemeNullable? other) {
    if (other == null) return this;
    return FancyBoxTheme(
      background: other.background ?? background,
      gap: other.gap ?? gap,
      padding: other.padding ?? padding,
      stripeWidth: other.stripeWidth ?? stripeWidth,
      outline: other.outline ?? outline,
    );
  }

  FancyBoxTheme copyWith({
    Color? background,
    double? gap,
    EdgeInsetsGeometry? padding,
    double? stripeWidth,
    Color? outline,
  }) => FancyBoxTheme(
    background: background ?? this.background,
    gap: gap ?? this.gap,
    padding: padding ?? this.padding,
    stripeWidth: stripeWidth ?? this.stripeWidth,
    outline: outline ?? this.outline,
  );

  static FancyBoxTheme lerp(FancyBoxTheme a, FancyBoxTheme b, double t) =>
      FancyBoxTheme(
        background: Color.lerp(a.background, b.background, t)!,
        gap: a.gap + (b.gap - a.gap) * t,
        padding: t < 0.5 ? a.padding : b.padding,
        stripeWidth: t < 0.5 ? a.stripeWidth : b.stripeWidth,
        outline: Color.lerp(a.outline, b.outline, t),
      );
}

/// Sparse overrides for [FancyBoxTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class FancyBoxThemeNullable {
  const FancyBoxThemeNullable({
    this.background,
    this.gap,
    this.padding,
    this.stripeWidth,
    this.outline,
  });

  final Color? background;
  final double? gap;
  final EdgeInsetsGeometry? padding;
  final double? stripeWidth;
  final Color? outline;

  FancyBoxThemeNullable merge(FancyBoxThemeNullable? other) {
    if (other == null) return this;
    return FancyBoxThemeNullable(
      background: other.background ?? background,
      gap: other.gap ?? gap,
      padding: other.padding ?? padding,
      stripeWidth: other.stripeWidth ?? stripeWidth,
      outline: other.outline ?? outline,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FancyBoxThemeNullable &&
          other.background == background &&
          other.gap == gap &&
          other.padding == padding &&
          other.stripeWidth == stripeWidth &&
          other.outline == outline;

  @override
  int get hashCode =>
      Object.hashAll([background, gap, padding, stripeWidth, outline]);
}

/// Overrides [FancyBoxTheme] for a subtree (level 2).
class FancyBoxThemeOverride extends StatelessWidget {
  const FancyBoxThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final FancyBoxThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<FancyBoxThemeNullable>(data: data, child: child);
}

/// Resolves [FancyBox.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$FancyBoxSelectBackground(LegendThemeData data) =>
    data.componentOf<FancyBoxThemeNullable>(FancyBox)?.background ??
    LegendColorsRef.surface(data.tokens);
const _$FancyBoxAspectBackground = LegendThemeAspect(
  _$FancyBoxSelectBackground,
);
Object? _$FancyBoxOverrideSelectBackground(FancyBoxThemeNullable data) =>
    data.background;
const _$FancyBoxOverrideAspectBackground =
    LegendOverrideAspect<FancyBoxThemeNullable>(
      _$FancyBoxOverrideSelectBackground,
    );

/// Resolves [FancyBox.gap] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$FancyBoxSelectGap(LegendThemeData data) =>
    data.componentOf<FancyBoxThemeNullable>(FancyBox)?.gap ?? 8.0;
const _$FancyBoxAspectGap = LegendThemeAspect(_$FancyBoxSelectGap);
Object? _$FancyBoxOverrideSelectGap(FancyBoxThemeNullable data) => data.gap;
const _$FancyBoxOverrideAspectGap = LegendOverrideAspect<FancyBoxThemeNullable>(
  _$FancyBoxOverrideSelectGap,
);

/// Resolves [FancyBox.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$FancyBoxSelectPadding(LegendThemeData data) =>
    data.componentOf<FancyBoxThemeNullable>(FancyBox)?.padding ??
    FancyBox._padding(data.tokens);
const _$FancyBoxAspectPadding = LegendThemeAspect(_$FancyBoxSelectPadding);
Object? _$FancyBoxOverrideSelectPadding(FancyBoxThemeNullable data) =>
    data.padding;
const _$FancyBoxOverrideAspectPadding =
    LegendOverrideAspect<FancyBoxThemeNullable>(
      _$FancyBoxOverrideSelectPadding,
    );

/// Resolves [FancyBox.stripeWidth] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$FancyBoxSelectStripeWidth(LegendThemeData data) =>
    data.componentOf<FancyBoxThemeNullable>(FancyBox)?.stripeWidth ??
    _stripeWidth(data.tokens);
const _$FancyBoxAspectStripeWidth = LegendThemeAspect(
  _$FancyBoxSelectStripeWidth,
);
Object? _$FancyBoxOverrideSelectStripeWidth(FancyBoxThemeNullable data) =>
    data.stripeWidth;
const _$FancyBoxOverrideAspectStripeWidth =
    LegendOverrideAspect<FancyBoxThemeNullable>(
      _$FancyBoxOverrideSelectStripeWidth,
    );

/// Resolves [FancyBox.outline] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color? _$FancyBoxSelectOutline(LegendThemeData data) =>
    data.componentOf<FancyBoxThemeNullable>(FancyBox)?.outline;
const _$FancyBoxAspectOutline = LegendThemeAspect(_$FancyBoxSelectOutline);
Object? _$FancyBoxOverrideSelectOutline(FancyBoxThemeNullable data) =>
    data.outline;
const _$FancyBoxOverrideAspectOutline =
    LegendOverrideAspect<FancyBoxThemeNullable>(
      _$FancyBoxOverrideSelectOutline,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class FancyBoxThemeListenables {
  /// Change stream of the resolved [FancyBoxTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$FancyBoxSelectBackground);

  /// Change stream of the resolved [FancyBoxTheme.gap].
  static ValueListenable<double> gap(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$FancyBoxSelectGap);

  /// Change stream of the resolved [FancyBoxTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$FancyBoxSelectPadding);

  /// Change stream of the resolved [FancyBoxTheme.stripeWidth].
  static ValueListenable<double> stripeWidth(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$FancyBoxSelectStripeWidth);

  /// Change stream of the resolved [FancyBoxTheme.outline].
  static ValueListenable<Color?> outline(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$FancyBoxSelectOutline);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$FancyBoxThemeResolve on FancyBox {
  /// [FancyBoxTheme.of] with this widget's constructor params as
  /// level 1.
  FancyBoxTheme _theme(BuildContext context) => FancyBoxTheme.of(
    context,
    FancyBoxThemeNullable(
      background: background,
      gap: gap,
      padding: padding,
      stripeWidth: stripeWidth,
      outline: outline,
    ),
  );

  /// Resolves ONLY [FancyBox.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<FancyBoxThemeNullable>(context);
    LegendTheme.depend(context, _$FancyBoxAspectBackground);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$FancyBoxSelectBackground(data);
  }

  /// Resolves ONLY [FancyBox.gap] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._gap(context)`).
  double _gap(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<FancyBoxThemeNullable>(context);
    LegendTheme.depend(context, _$FancyBoxAspectGap);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectGap,
    );
    return gap ?? override?.gap ?? _$FancyBoxSelectGap(data);
  }

  /// Resolves ONLY [FancyBox.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<FancyBoxThemeNullable>(context);
    LegendTheme.depend(context, _$FancyBoxAspectPadding);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$FancyBoxSelectPadding(data);
  }

  /// Resolves ONLY [FancyBox.stripeWidth] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._stripeWidth(context)`).
  double _stripeWidth(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<FancyBoxThemeNullable>(context);
    LegendTheme.depend(context, _$FancyBoxAspectStripeWidth);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectStripeWidth,
    );
    return stripeWidth ??
        override?.stripeWidth ??
        _$FancyBoxSelectStripeWidth(data);
  }

  /// Resolves ONLY [FancyBox.outline] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._outline(context)`).
  Color? _outline(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<FancyBoxThemeNullable>(context);
    LegendTheme.depend(context, _$FancyBoxAspectOutline);
    LegendThemeOverride.depend<FancyBoxThemeNullable>(
      context,
      _$FancyBoxOverrideAspectOutline,
    );
    return outline ?? override?.outline ?? _$FancyBoxSelectOutline(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class FancyBox extends
/// _$FancyBoxBase` receives the resolved
/// [FancyBoxTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$FancyBoxBase extends LegendStatelessWidget<FancyBoxTheme> {
  const _$FancyBoxBase({super.key});

  Color? get background;
  double? get gap;
  EdgeInsetsGeometry? get padding;
  double? get stripeWidth;
  Color? get outline;

  @override
  FancyBoxTheme resolveThemeOf(BuildContext context) => FancyBoxTheme.of(
    context,
    FancyBoxThemeNullable(
      background: background,
      gap: gap,
      padding: padding,
      stripeWidth: stripeWidth,
      outline: outline,
    ),
  );
}
