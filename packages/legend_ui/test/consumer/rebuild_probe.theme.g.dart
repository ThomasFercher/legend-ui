// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: rebuild_probe.dart
// ignore_for_file: type=lint, unused_element
part of 'rebuild_probe.dart';

/// Resolved theme for [ProbeBox].
class ProbeBoxTheme {
  const ProbeBoxTheme({required this.tint, required this.gap});

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory ProbeBoxTheme.defaults(LegendTokens t) =>
      ProbeBoxTheme(tint: ColorRef.primary(t), gap: 8);

  final Color tint;
  final double gap;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [ProbeBox] first, [ProbeBoxThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static ProbeBoxTheme of(
    BuildContext context, [
    ProbeBoxThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<ProbeBoxThemeNullable>(context);
    LegendTheme.depend(context, _$ProbeBoxAspectTint);
    LegendThemeOverride.depend<ProbeBoxThemeNullable>(
      context,
      _$ProbeBoxOverrideAspectTint,
    );
    return ProbeBoxTheme.defaults(data.tokens)
        .merge(data.componentOf<ProbeBoxThemeNullable>(ProbeBox))
        .merge(override)
        .merge(local);
  }

  ProbeBoxTheme merge(ProbeBoxThemeNullable? other) {
    if (other == null) return this;
    return ProbeBoxTheme(tint: other.tint ?? tint, gap: other.gap ?? gap);
  }

  ProbeBoxTheme copyWith({Color? tint, double? gap}) =>
      ProbeBoxTheme(tint: tint ?? this.tint, gap: gap ?? this.gap);

  static ProbeBoxTheme lerp(ProbeBoxTheme a, ProbeBoxTheme b, double t) =>
      ProbeBoxTheme(
        tint: t < 0.5 ? a.tint : b.tint,
        gap: t < 0.5 ? a.gap : b.gap,
      );
}

/// Sparse overrides for [ProbeBoxTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class ProbeBoxThemeNullable {
  const ProbeBoxThemeNullable({this.tint, this.gap});

  final Color? tint;
  final double? gap;

  ProbeBoxThemeNullable merge(ProbeBoxThemeNullable? other) {
    if (other == null) return this;
    return ProbeBoxThemeNullable(
      tint: other.tint ?? tint,
      gap: other.gap ?? gap,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ProbeBoxThemeNullable && other.tint == tint && other.gap == gap;

  @override
  int get hashCode => Object.hashAll([tint, gap]);
}

/// Overrides [ProbeBoxTheme] for a subtree (level 2).
class ProbeBoxThemeOverride extends StatelessWidget {
  const ProbeBoxThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final ProbeBoxThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<ProbeBoxThemeNullable>(data: data, child: child);
}

/// Resolves [ProbeBox.tint] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$ProbeBoxSelectTint(LegendThemeData data) =>
    data.componentOf<ProbeBoxThemeNullable>(ProbeBox)?.tint ??
    ColorRef.primary(data.tokens);
const _$ProbeBoxAspectTint = LegendThemeAspect(_$ProbeBoxSelectTint);
Object? _$ProbeBoxOverrideSelectTint(ProbeBoxThemeNullable data) => data.tint;
const _$ProbeBoxOverrideAspectTint =
    LegendOverrideAspect<ProbeBoxThemeNullable>(_$ProbeBoxOverrideSelectTint);

/// Resolves [ProbeBox.gap] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$ProbeBoxSelectGap(LegendThemeData data) =>
    data.componentOf<ProbeBoxThemeNullable>(ProbeBox)?.gap ?? 8;
const _$ProbeBoxAspectGap = LegendThemeAspect(_$ProbeBoxSelectGap);
Object? _$ProbeBoxOverrideSelectGap(ProbeBoxThemeNullable data) => data.gap;
const _$ProbeBoxOverrideAspectGap = LegendOverrideAspect<ProbeBoxThemeNullable>(
  _$ProbeBoxOverrideSelectGap,
);

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class ProbeBoxThemeListenables {
  /// Change stream of the resolved [ProbeBoxTheme.tint].
  static ValueListenable<Color> tint(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$ProbeBoxSelectTint);

  /// Change stream of the resolved [ProbeBoxTheme.gap].
  static ValueListenable<double> gap(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$ProbeBoxSelectGap);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$ProbeBoxThemeResolve on ProbeBox {
  /// [ProbeBoxTheme.of] with this widget's constructor params as
  /// level 1.
  ProbeBoxTheme _theme(BuildContext context) =>
      ProbeBoxTheme.of(context, ProbeBoxThemeNullable(tint: tint, gap: gap));

  /// Resolves ONLY [ProbeBox.tint] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._tint(context)`).
  Color _tint(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<ProbeBoxThemeNullable>(context);
    LegendTheme.depend(context, _$ProbeBoxAspectTint);
    LegendThemeOverride.depend<ProbeBoxThemeNullable>(
      context,
      _$ProbeBoxOverrideAspectTint,
    );
    return tint ?? override?.tint ?? _$ProbeBoxSelectTint(data);
  }

  /// Resolves ONLY [ProbeBox.gap] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._gap(context)`).
  double _gap(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<ProbeBoxThemeNullable>(context);
    return gap ?? override?.gap ?? _$ProbeBoxSelectGap(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class ProbeBox extends
/// _$ProbeBoxBase` receives the resolved
/// [ProbeBoxTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$ProbeBoxBase extends LegendStatelessWidget<ProbeBoxTheme> {
  const _$ProbeBoxBase({super.key});

  Color? get tint;
  double? get gap;

  @override
  ProbeBoxTheme resolveThemeOf(BuildContext context) =>
      ProbeBoxTheme.of(context, ProbeBoxThemeNullable(tint: tint, gap: gap));
}

/// Resolved theme for [OtherBox].
class OtherBoxTheme {
  const OtherBoxTheme({required this.fill});

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory OtherBoxTheme.defaults(LegendTokens t) =>
      OtherBoxTheme(fill: ColorRef.secondary(t));

  final Color fill;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [OtherBox] first, [OtherBoxThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static OtherBoxTheme of(
    BuildContext context, [
    OtherBoxThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<OtherBoxThemeNullable>(context);
    LegendTheme.depend(context, _$OtherBoxAspectFill);
    LegendThemeOverride.depend<OtherBoxThemeNullable>(
      context,
      _$OtherBoxOverrideAspectFill,
    );
    return OtherBoxTheme.defaults(data.tokens)
        .merge(data.componentOf<OtherBoxThemeNullable>(OtherBox))
        .merge(override)
        .merge(local);
  }

  OtherBoxTheme merge(OtherBoxThemeNullable? other) {
    if (other == null) return this;
    return OtherBoxTheme(fill: other.fill ?? fill);
  }

  OtherBoxTheme copyWith({Color? fill}) =>
      OtherBoxTheme(fill: fill ?? this.fill);

  static OtherBoxTheme lerp(OtherBoxTheme a, OtherBoxTheme b, double t) =>
      OtherBoxTheme(fill: t < 0.5 ? a.fill : b.fill);
}

/// Sparse overrides for [OtherBoxTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class OtherBoxThemeNullable {
  const OtherBoxThemeNullable({this.fill});

  final Color? fill;

  OtherBoxThemeNullable merge(OtherBoxThemeNullable? other) {
    if (other == null) return this;
    return OtherBoxThemeNullable(fill: other.fill ?? fill);
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is OtherBoxThemeNullable && other.fill == fill;

  @override
  int get hashCode => Object.hashAll([fill]);
}

/// Overrides [OtherBoxTheme] for a subtree (level 2).
class OtherBoxThemeOverride extends StatelessWidget {
  const OtherBoxThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final OtherBoxThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<OtherBoxThemeNullable>(data: data, child: child);
}

/// Resolves [OtherBox.fill] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$OtherBoxSelectFill(LegendThemeData data) =>
    data.componentOf<OtherBoxThemeNullable>(OtherBox)?.fill ??
    ColorRef.secondary(data.tokens);
const _$OtherBoxAspectFill = LegendThemeAspect(_$OtherBoxSelectFill);
Object? _$OtherBoxOverrideSelectFill(OtherBoxThemeNullable data) => data.fill;
const _$OtherBoxOverrideAspectFill =
    LegendOverrideAspect<OtherBoxThemeNullable>(_$OtherBoxOverrideSelectFill);

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class OtherBoxThemeListenables {
  /// Change stream of the resolved [OtherBoxTheme.fill].
  static ValueListenable<Color> fill(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$OtherBoxSelectFill);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$OtherBoxThemeResolve on OtherBox {
  /// [OtherBoxTheme.of] with this widget's constructor params as
  /// level 1.
  OtherBoxTheme _theme(BuildContext context) =>
      OtherBoxTheme.of(context, OtherBoxThemeNullable(fill: fill));

  /// Resolves ONLY [OtherBox.fill] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._fill(context)`).
  Color _fill(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<OtherBoxThemeNullable>(context);
    LegendTheme.depend(context, _$OtherBoxAspectFill);
    LegendThemeOverride.depend<OtherBoxThemeNullable>(
      context,
      _$OtherBoxOverrideAspectFill,
    );
    return fill ?? override?.fill ?? _$OtherBoxSelectFill(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class OtherBox extends
/// _$OtherBoxBase` receives the resolved
/// [OtherBoxTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$OtherBoxBase extends LegendStatelessWidget<OtherBoxTheme> {
  const _$OtherBoxBase({super.key});

  Color? get fill;

  @override
  OtherBoxTheme resolveThemeOf(BuildContext context) =>
      OtherBoxTheme.of(context, OtherBoxThemeNullable(fill: fill));
}

/// Resolved theme for [TintOnlyBox].
class TintOnlyBoxTheme {
  const TintOnlyBoxTheme({required this.tint, required this.gap});

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory TintOnlyBoxTheme.defaults(LegendTokens t) =>
      TintOnlyBoxTheme(tint: ColorRef.primary(t), gap: 8);

  final Color tint;
  final double gap;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [TintOnlyBox] first, [TintOnlyBoxThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static TintOnlyBoxTheme of(
    BuildContext context, [
    TintOnlyBoxThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<TintOnlyBoxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$TintOnlyBoxAspectTint);
    LegendThemeOverride.depend<TintOnlyBoxThemeNullable>(
      context,
      _$TintOnlyBoxOverrideAspectTint,
    );
    LegendTheme.depend(context, _$TintOnlyBoxAspectGap);
    LegendThemeOverride.depend<TintOnlyBoxThemeNullable>(
      context,
      _$TintOnlyBoxOverrideAspectGap,
    );
    return TintOnlyBoxTheme.defaults(data.tokens)
        .merge(data.componentOf<TintOnlyBoxThemeNullable>(TintOnlyBox))
        .merge(override)
        .merge(local);
  }

  TintOnlyBoxTheme merge(TintOnlyBoxThemeNullable? other) {
    if (other == null) return this;
    return TintOnlyBoxTheme(tint: other.tint ?? tint, gap: other.gap ?? gap);
  }

  TintOnlyBoxTheme copyWith({Color? tint, double? gap}) =>
      TintOnlyBoxTheme(tint: tint ?? this.tint, gap: gap ?? this.gap);

  static TintOnlyBoxTheme lerp(
    TintOnlyBoxTheme a,
    TintOnlyBoxTheme b,
    double t,
  ) => TintOnlyBoxTheme(
    tint: t < 0.5 ? a.tint : b.tint,
    gap: t < 0.5 ? a.gap : b.gap,
  );
}

/// Sparse overrides for [TintOnlyBoxTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class TintOnlyBoxThemeNullable {
  const TintOnlyBoxThemeNullable({this.tint, this.gap});

  final Color? tint;
  final double? gap;

  TintOnlyBoxThemeNullable merge(TintOnlyBoxThemeNullable? other) {
    if (other == null) return this;
    return TintOnlyBoxThemeNullable(
      tint: other.tint ?? tint,
      gap: other.gap ?? gap,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TintOnlyBoxThemeNullable &&
          other.tint == tint &&
          other.gap == gap;

  @override
  int get hashCode => Object.hashAll([tint, gap]);
}

/// Overrides [TintOnlyBoxTheme] for a subtree (level 2).
class TintOnlyBoxThemeOverride extends StatelessWidget {
  const TintOnlyBoxThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final TintOnlyBoxThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<TintOnlyBoxThemeNullable>(data: data, child: child);
}

/// Resolves [TintOnlyBox.tint] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$TintOnlyBoxSelectTint(LegendThemeData data) =>
    data.componentOf<TintOnlyBoxThemeNullable>(TintOnlyBox)?.tint ??
    ColorRef.primary(data.tokens);
const _$TintOnlyBoxAspectTint = LegendThemeAspect(_$TintOnlyBoxSelectTint);
Object? _$TintOnlyBoxOverrideSelectTint(TintOnlyBoxThemeNullable data) =>
    data.tint;
const _$TintOnlyBoxOverrideAspectTint =
    LegendOverrideAspect<TintOnlyBoxThemeNullable>(
      _$TintOnlyBoxOverrideSelectTint,
    );

/// Resolves [TintOnlyBox.gap] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$TintOnlyBoxSelectGap(LegendThemeData data) =>
    data.componentOf<TintOnlyBoxThemeNullable>(TintOnlyBox)?.gap ?? 8;
const _$TintOnlyBoxAspectGap = LegendThemeAspect(_$TintOnlyBoxSelectGap);
Object? _$TintOnlyBoxOverrideSelectGap(TintOnlyBoxThemeNullable data) =>
    data.gap;
const _$TintOnlyBoxOverrideAspectGap =
    LegendOverrideAspect<TintOnlyBoxThemeNullable>(
      _$TintOnlyBoxOverrideSelectGap,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class TintOnlyBoxThemeListenables {
  /// Change stream of the resolved [TintOnlyBoxTheme.tint].
  static ValueListenable<Color> tint(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$TintOnlyBoxSelectTint);

  /// Change stream of the resolved [TintOnlyBoxTheme.gap].
  static ValueListenable<double> gap(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$TintOnlyBoxSelectGap);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$TintOnlyBoxThemeResolve on TintOnlyBox {
  /// [TintOnlyBoxTheme.of] with this widget's constructor params as
  /// level 1.
  TintOnlyBoxTheme _theme(BuildContext context) => TintOnlyBoxTheme.of(
    context,
    TintOnlyBoxThemeNullable(tint: tint, gap: gap),
  );

  /// Resolves ONLY [TintOnlyBox.tint] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._tint(context)`).
  Color _tint(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<TintOnlyBoxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$TintOnlyBoxAspectTint);
    LegendThemeOverride.depend<TintOnlyBoxThemeNullable>(
      context,
      _$TintOnlyBoxOverrideAspectTint,
    );
    return tint ?? override?.tint ?? _$TintOnlyBoxSelectTint(data);
  }

  /// Resolves ONLY [TintOnlyBox.gap] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._gap(context)`).
  double _gap(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<TintOnlyBoxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$TintOnlyBoxAspectGap);
    LegendThemeOverride.depend<TintOnlyBoxThemeNullable>(
      context,
      _$TintOnlyBoxOverrideAspectGap,
    );
    return gap ?? override?.gap ?? _$TintOnlyBoxSelectGap(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class TintOnlyBox extends
/// _$TintOnlyBoxBase` receives the resolved
/// [TintOnlyBoxTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$TintOnlyBoxBase
    extends LegendStatelessWidget<TintOnlyBoxTheme> {
  const _$TintOnlyBoxBase({super.key});

  Color? get tint;
  double? get gap;

  @override
  TintOnlyBoxTheme resolveThemeOf(BuildContext context) => TintOnlyBoxTheme.of(
    context,
    TintOnlyBoxThemeNullable(tint: tint, gap: gap),
  );
}
