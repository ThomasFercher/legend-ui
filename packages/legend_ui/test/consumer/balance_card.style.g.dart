// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: balance_card.dart
// ignore_for_file: type=lint, unused_element
part of 'balance_card.dart';

/// Generated mechanical members for [BalanceAccent] (RFC-002 R6
/// amendment 7): member-wise sparse [merge] and value
/// `==`/`hashCode`. Applied via `with _$BalanceAccent`; fields are
/// read through a private cast (no abstract getters, so the
/// class declares no overrides).
mixin _$BalanceAccent {
  BalanceAccent get _self => this as BalanceAccent;

  /// Member-wise sparse merge: [other]'s set members win,
  /// unset members inherit — a theme level that names only
  /// one member never clobbers another level's members. Null
  /// passes this instance through unchanged.
  BalanceAccent merge(BalanceAccent? other) {
    if (other == null) return _self;
    return BalanceAccent(
      amount: other.amount ?? _self.amount,
      caption: other.caption ?? _self.caption,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BalanceAccent &&
          other.amount == _self.amount &&
          other.caption == _self.caption;

  @override
  int get hashCode => Object.hashAll([_self.amount, _self.caption]);
}

/// Member-wise lerp for [BalanceAccent] with the type-appropriate
/// lerper per member (nested style classes via their own
/// `lerp` static, members without a lerper step at t=0.5);
/// the public [BalanceAccent.lerp] redirects here.
BalanceAccent? _$BalanceAccentLerp(
  BalanceAccent? a,
  BalanceAccent? b,
  double t,
) {
  if (identical(a, b)) return a;
  return BalanceAccent(
    amount: Color.lerp(a?.amount, b?.amount, t),
    caption: Color.lerp(a?.caption, b?.caption, t),
  );
}
