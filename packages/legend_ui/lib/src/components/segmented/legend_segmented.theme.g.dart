// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_segmented.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_segmented.dart';

/// Resolved theme for [LegendSegmented].
class LegendSegmentedTheme {
  const LegendSegmentedTheme({
    required this.background,
    required this.thumb,
    required this.labelStyle,
    required this.selectedLabelStyle,
    required this.borderRadius,
    required this.padding,
    required this.segmentPadding,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendSegmentedTheme.defaults(LegendTokens t) => LegendSegmentedTheme(
    background: ColorRef.background2(t),
    thumb: _thumb(t),
    labelStyle: _labelStyle(t),
    selectedLabelStyle: _selectedLabelStyle(t),
    borderRadius: _borderRadius(t),
    padding: _padding(t),
    segmentPadding: _segmentPadding(t),
  );

  final Color background;
  final InteractiveColors thumb;
  final TextStyle labelStyle;
  final TextStyle selectedLabelStyle;
  final BorderRadius borderRadius;
  final EdgeInsetsGeometry padding;
  final EdgeInsetsGeometry segmentPadding;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendSegmented] first, [LegendSegmentedThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendSegmentedTheme of(
    BuildContext context, [
    LegendSegmentedThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectBackground);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectThumb);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectThumb,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectLabelStyle);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectLabelStyle,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectSelectedLabelStyle);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectSelectedLabelStyle,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectBorderRadius);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectPadding);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectSegmentPadding);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectSegmentPadding,
    );
    return LegendSegmentedTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendSegmentedThemeNullable>(LegendSegmented))
        .merge(override)
        .merge(local);
  }

  LegendSegmentedTheme merge(LegendSegmentedThemeNullable? other) {
    if (other == null) return this;
    return LegendSegmentedTheme(
      background: other.background ?? background,
      thumb: thumb.merge(other.thumb),
      labelStyle: other.labelStyle ?? labelStyle,
      selectedLabelStyle: other.selectedLabelStyle ?? selectedLabelStyle,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      segmentPadding: other.segmentPadding ?? segmentPadding,
    );
  }

  LegendSegmentedTheme copyWith({
    Color? background,
    InteractiveColors? thumb,
    TextStyle? labelStyle,
    TextStyle? selectedLabelStyle,
    BorderRadius? borderRadius,
    EdgeInsetsGeometry? padding,
    EdgeInsetsGeometry? segmentPadding,
  }) => LegendSegmentedTheme(
    background: background ?? this.background,
    thumb: thumb ?? this.thumb,
    labelStyle: labelStyle ?? this.labelStyle,
    selectedLabelStyle: selectedLabelStyle ?? this.selectedLabelStyle,
    borderRadius: borderRadius ?? this.borderRadius,
    padding: padding ?? this.padding,
    segmentPadding: segmentPadding ?? this.segmentPadding,
  );

  static LegendSegmentedTheme lerp(
    LegendSegmentedTheme a,
    LegendSegmentedTheme b,
    double t,
  ) => LegendSegmentedTheme(
    background: t < 0.5 ? a.background : b.background,
    thumb: t < 0.5 ? a.thumb : b.thumb,
    labelStyle: t < 0.5 ? a.labelStyle : b.labelStyle,
    selectedLabelStyle: t < 0.5 ? a.selectedLabelStyle : b.selectedLabelStyle,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    padding: t < 0.5 ? a.padding : b.padding,
    segmentPadding: t < 0.5 ? a.segmentPadding : b.segmentPadding,
  );
}

/// Sparse overrides for [LegendSegmentedTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendSegmentedThemeNullable {
  const LegendSegmentedThemeNullable({
    this.background,
    this.thumb,
    this.labelStyle,
    this.selectedLabelStyle,
    this.borderRadius,
    this.padding,
    this.segmentPadding,
  });

  final Color? background;
  final InteractiveColors? thumb;
  final TextStyle? labelStyle;
  final TextStyle? selectedLabelStyle;
  final BorderRadius? borderRadius;
  final EdgeInsetsGeometry? padding;
  final EdgeInsetsGeometry? segmentPadding;

  LegendSegmentedThemeNullable merge(LegendSegmentedThemeNullable? other) {
    if (other == null) return this;
    return LegendSegmentedThemeNullable(
      background: other.background ?? background,
      thumb: thumb?.merge(other.thumb) ?? other.thumb,
      labelStyle: other.labelStyle ?? labelStyle,
      selectedLabelStyle: other.selectedLabelStyle ?? selectedLabelStyle,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      segmentPadding: other.segmentPadding ?? segmentPadding,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendSegmentedThemeNullable &&
          other.background == background &&
          other.thumb == thumb &&
          other.labelStyle == labelStyle &&
          other.selectedLabelStyle == selectedLabelStyle &&
          other.borderRadius == borderRadius &&
          other.padding == padding &&
          other.segmentPadding == segmentPadding;

  @override
  int get hashCode => Object.hashAll([
    background,
    thumb,
    labelStyle,
    selectedLabelStyle,
    borderRadius,
    padding,
    segmentPadding,
  ]);
}

/// Overrides [LegendSegmentedTheme] for a subtree (level 2).
class LegendSegmentedThemeOverride extends StatelessWidget {
  const LegendSegmentedThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendSegmentedThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendSegmentedThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendSegmented.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendSegmentedSelectBackground(LegendThemeData data) =>
    data
        .componentOf<LegendSegmentedThemeNullable>(LegendSegmented)
        ?.background ??
    ColorRef.background2(data.tokens);
const _$LegendSegmentedAspectBackground = LegendThemeAspect(
  _$LegendSegmentedSelectBackground,
);
Object? _$LegendSegmentedOverrideSelectBackground(
  LegendSegmentedThemeNullable data,
) => data.background;
const _$LegendSegmentedOverrideAspectBackground =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectBackground,
    );

/// Resolves [LegendSegmented.thumb] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendSegmentedSelectThumb(LegendThemeData data) => _thumb(
  data.tokens,
).merge(data.componentOf<LegendSegmentedThemeNullable>(LegendSegmented)?.thumb);
const _$LegendSegmentedAspectThumb = LegendThemeAspect(
  _$LegendSegmentedSelectThumb,
);
Object? _$LegendSegmentedOverrideSelectThumb(
  LegendSegmentedThemeNullable data,
) => data.thumb;
const _$LegendSegmentedOverrideAspectThumb =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectThumb,
    );

/// Resolves [LegendSegmented.labelStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendSegmentedSelectLabelStyle(LegendThemeData data) =>
    data
        .componentOf<LegendSegmentedThemeNullable>(LegendSegmented)
        ?.labelStyle ??
    _labelStyle(data.tokens);
const _$LegendSegmentedAspectLabelStyle = LegendThemeAspect(
  _$LegendSegmentedSelectLabelStyle,
);
Object? _$LegendSegmentedOverrideSelectLabelStyle(
  LegendSegmentedThemeNullable data,
) => data.labelStyle;
const _$LegendSegmentedOverrideAspectLabelStyle =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectLabelStyle,
    );

/// Resolves [LegendSegmented.selectedLabelStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendSegmentedSelectSelectedLabelStyle(LegendThemeData data) =>
    data
        .componentOf<LegendSegmentedThemeNullable>(LegendSegmented)
        ?.selectedLabelStyle ??
    _selectedLabelStyle(data.tokens);
const _$LegendSegmentedAspectSelectedLabelStyle = LegendThemeAspect(
  _$LegendSegmentedSelectSelectedLabelStyle,
);
Object? _$LegendSegmentedOverrideSelectSelectedLabelStyle(
  LegendSegmentedThemeNullable data,
) => data.selectedLabelStyle;
const _$LegendSegmentedOverrideAspectSelectedLabelStyle =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectSelectedLabelStyle,
    );

/// Resolves [LegendSegmented.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendSegmentedSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendSegmentedThemeNullable>(LegendSegmented)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendSegmentedAspectBorderRadius = LegendThemeAspect(
  _$LegendSegmentedSelectBorderRadius,
);
Object? _$LegendSegmentedOverrideSelectBorderRadius(
  LegendSegmentedThemeNullable data,
) => data.borderRadius;
const _$LegendSegmentedOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectBorderRadius,
    );

/// Resolves [LegendSegmented.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendSegmentedSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendSegmentedThemeNullable>(LegendSegmented)?.padding ??
    _padding(data.tokens);
const _$LegendSegmentedAspectPadding = LegendThemeAspect(
  _$LegendSegmentedSelectPadding,
);
Object? _$LegendSegmentedOverrideSelectPadding(
  LegendSegmentedThemeNullable data,
) => data.padding;
const _$LegendSegmentedOverrideAspectPadding =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectPadding,
    );

/// Resolves [LegendSegmented.segmentPadding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendSegmentedSelectSegmentPadding(
  LegendThemeData data,
) =>
    data
        .componentOf<LegendSegmentedThemeNullable>(LegendSegmented)
        ?.segmentPadding ??
    _segmentPadding(data.tokens);
const _$LegendSegmentedAspectSegmentPadding = LegendThemeAspect(
  _$LegendSegmentedSelectSegmentPadding,
);
Object? _$LegendSegmentedOverrideSelectSegmentPadding(
  LegendSegmentedThemeNullable data,
) => data.segmentPadding;
const _$LegendSegmentedOverrideAspectSegmentPadding =
    LegendOverrideAspect<LegendSegmentedThemeNullable>(
      _$LegendSegmentedOverrideSelectSegmentPadding,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendSegmentedThemeListenables {
  /// Change stream of the resolved [LegendSegmentedTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSegmentedSelectBackground);

  /// Change stream of the resolved [LegendSegmentedTheme.thumb].
  static ValueListenable<InteractiveColors> thumb(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSegmentedSelectThumb);

  /// Change stream of the resolved [LegendSegmentedTheme.labelStyle].
  static ValueListenable<TextStyle> labelStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSegmentedSelectLabelStyle);

  /// Change stream of the resolved [LegendSegmentedTheme.selectedLabelStyle].
  static ValueListenable<TextStyle> selectedLabelStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendSegmentedSelectSelectedLabelStyle,
  );

  /// Change stream of the resolved [LegendSegmentedTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSegmentedSelectBorderRadius);

  /// Change stream of the resolved [LegendSegmentedTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSegmentedSelectPadding);

  /// Change stream of the resolved [LegendSegmentedTheme.segmentPadding].
  static ValueListenable<EdgeInsetsGeometry> segmentPadding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSegmentedSelectSegmentPadding);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendSegmentedThemeResolve on LegendSegmented {
  /// [LegendSegmentedTheme.of] with this widget's constructor params as
  /// level 1.
  LegendSegmentedTheme _theme(BuildContext context) => LegendSegmentedTheme.of(
    context,
    LegendSegmentedThemeNullable(
      background: background,
      thumb: thumb,
      labelStyle: labelStyle,
      selectedLabelStyle: selectedLabelStyle,
      borderRadius: borderRadius,
      padding: padding,
      segmentPadding: segmentPadding,
    ),
  );

  /// Resolves ONLY [LegendSegmented.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectBackground);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendSegmentedSelectBackground(data);
  }

  /// Resolves ONLY [LegendSegmented.thumb] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._thumb(context)`).
  InteractiveColors _thumb(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectThumb);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectThumb,
    );
    return _$LegendSegmentedSelectThumb(
      data,
    ).merge(override?.thumb).merge(thumb);
  }

  /// Resolves ONLY [LegendSegmented.labelStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._labelStyle(context)`).
  TextStyle _labelStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectLabelStyle);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectLabelStyle,
    );
    return labelStyle ??
        override?.labelStyle ??
        _$LegendSegmentedSelectLabelStyle(data);
  }

  /// Resolves ONLY [LegendSegmented.selectedLabelStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedLabelStyle(context)`).
  TextStyle _selectedLabelStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectSelectedLabelStyle);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectSelectedLabelStyle,
    );
    return selectedLabelStyle ??
        override?.selectedLabelStyle ??
        _$LegendSegmentedSelectSelectedLabelStyle(data);
  }

  /// Resolves ONLY [LegendSegmented.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectBorderRadius);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendSegmentedSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendSegmented.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectPadding);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendSegmentedSelectPadding(data);
  }

  /// Resolves ONLY [LegendSegmented.segmentPadding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._segmentPadding(context)`).
  EdgeInsetsGeometry _segmentPadding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSegmentedThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSegmentedAspectSegmentPadding);
    LegendThemeOverride.depend<LegendSegmentedThemeNullable>(
      context,
      _$LegendSegmentedOverrideAspectSegmentPadding,
    );
    return segmentPadding ??
        override?.segmentPadding ??
        _$LegendSegmentedSelectSegmentPadding(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendSegmentedState]
/// is this file's `State<LegendSegmented>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendSegmentedThemeState])
/// wins over this extension.
extension _$LegendSegmentedThemeOn_LegendSegmentedState<T>
    on _LegendSegmentedState<T> {
  LegendSegmentedTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendSegmented>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendSegmentedThemeState on State<LegendSegmented> {
  LegendSegmentedTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendSegmented extends
/// _$LegendSegmentedBase` receives the resolved
/// [LegendSegmentedTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendSegmentedBase
    extends LegendStatelessWidget<LegendSegmentedTheme> {
  const _$LegendSegmentedBase({super.key});

  Color? get background;
  InteractiveColors? get thumb;
  TextStyle? get labelStyle;
  TextStyle? get selectedLabelStyle;
  BorderRadius? get borderRadius;
  EdgeInsetsGeometry? get padding;
  EdgeInsetsGeometry? get segmentPadding;

  @override
  LegendSegmentedTheme resolveThemeOf(BuildContext context) =>
      LegendSegmentedTheme.of(
        context,
        LegendSegmentedThemeNullable(
          background: background,
          thumb: thumb,
          labelStyle: labelStyle,
          selectedLabelStyle: selectedLabelStyle,
          borderRadius: borderRadius,
          padding: padding,
          segmentPadding: segmentPadding,
        ),
      );
}
