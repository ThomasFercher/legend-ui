// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_combobox.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_combobox.dart';

/// Resolved theme for [LegendCombobox].
class LegendComboboxTheme {
  const LegendComboboxTheme({
    required this.menuBackground,
    required this.menuBorderRadius,
    required this.menuShadows,
    required this.menuMaxHeight,
    required this.itemPadding,
    required this.textStyle,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendComboboxTheme.defaults(LegendTokens t) => LegendComboboxTheme(
    menuBackground: _menuBackground(t),
    menuBorderRadius: _menuBorderRadius(t),
    menuShadows: ShadowRef.medium(t),
    menuMaxHeight: 320,
    itemPadding: _itemPadding(t),
    textStyle: TextRef.b2(t),
  );

  final InteractiveColors menuBackground;
  final BorderRadius menuBorderRadius;
  final List<BoxShadow> menuShadows;
  final double menuMaxHeight;
  final EdgeInsetsGeometry itemPadding;
  final TextStyle textStyle;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendCombobox] first, [LegendComboboxThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendComboboxTheme of(
    BuildContext context, [
    LegendComboboxThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuBackground);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuBackground,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuBorderRadius);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuBorderRadius,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuShadows);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuShadows,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuMaxHeight);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuMaxHeight,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectItemPadding);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectItemPadding,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectTextStyle);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectTextStyle,
    );
    return LegendComboboxTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendComboboxThemeNullable>(LegendCombobox))
        .merge(override)
        .merge(local);
  }

  LegendComboboxTheme merge(LegendComboboxThemeNullable? other) {
    if (other == null) return this;
    return LegendComboboxTheme(
      menuBackground: menuBackground.merge(other.menuBackground),
      menuBorderRadius: other.menuBorderRadius ?? menuBorderRadius,
      menuShadows: other.menuShadows ?? menuShadows,
      menuMaxHeight: other.menuMaxHeight ?? menuMaxHeight,
      itemPadding: other.itemPadding ?? itemPadding,
      textStyle: other.textStyle ?? textStyle,
    );
  }

  LegendComboboxTheme copyWith({
    InteractiveColors? menuBackground,
    BorderRadius? menuBorderRadius,
    List<BoxShadow>? menuShadows,
    double? menuMaxHeight,
    EdgeInsetsGeometry? itemPadding,
    TextStyle? textStyle,
  }) => LegendComboboxTheme(
    menuBackground: menuBackground ?? this.menuBackground,
    menuBorderRadius: menuBorderRadius ?? this.menuBorderRadius,
    menuShadows: menuShadows ?? this.menuShadows,
    menuMaxHeight: menuMaxHeight ?? this.menuMaxHeight,
    itemPadding: itemPadding ?? this.itemPadding,
    textStyle: textStyle ?? this.textStyle,
  );

  static LegendComboboxTheme lerp(
    LegendComboboxTheme a,
    LegendComboboxTheme b,
    double t,
  ) => LegendComboboxTheme(
    menuBackground: t < 0.5 ? a.menuBackground : b.menuBackground,
    menuBorderRadius: t < 0.5 ? a.menuBorderRadius : b.menuBorderRadius,
    menuShadows: t < 0.5 ? a.menuShadows : b.menuShadows,
    menuMaxHeight: t < 0.5 ? a.menuMaxHeight : b.menuMaxHeight,
    itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
  );
}

/// Sparse overrides for [LegendComboboxTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendComboboxThemeNullable {
  const LegendComboboxThemeNullable({
    this.menuBackground,
    this.menuBorderRadius,
    this.menuShadows,
    this.menuMaxHeight,
    this.itemPadding,
    this.textStyle,
  });

  final InteractiveColors? menuBackground;
  final BorderRadius? menuBorderRadius;
  final List<BoxShadow>? menuShadows;
  final double? menuMaxHeight;
  final EdgeInsetsGeometry? itemPadding;
  final TextStyle? textStyle;

  LegendComboboxThemeNullable merge(LegendComboboxThemeNullable? other) {
    if (other == null) return this;
    return LegendComboboxThemeNullable(
      menuBackground:
          menuBackground?.merge(other.menuBackground) ?? other.menuBackground,
      menuBorderRadius: other.menuBorderRadius ?? menuBorderRadius,
      menuShadows: other.menuShadows ?? menuShadows,
      menuMaxHeight: other.menuMaxHeight ?? menuMaxHeight,
      itemPadding: other.itemPadding ?? itemPadding,
      textStyle: other.textStyle ?? textStyle,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendComboboxThemeNullable &&
          other.menuBackground == menuBackground &&
          other.menuBorderRadius == menuBorderRadius &&
          other.menuShadows == menuShadows &&
          other.menuMaxHeight == menuMaxHeight &&
          other.itemPadding == itemPadding &&
          other.textStyle == textStyle;

  @override
  int get hashCode => Object.hashAll([
    menuBackground,
    menuBorderRadius,
    menuShadows,
    menuMaxHeight,
    itemPadding,
    textStyle,
  ]);
}

/// Overrides [LegendComboboxTheme] for a subtree (level 2).
class LegendComboboxThemeOverride extends StatelessWidget {
  const LegendComboboxThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendComboboxThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendComboboxThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendCombobox.menuBackground] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendComboboxSelectMenuBackground(LegendThemeData data) =>
    _menuBackground(data.tokens).merge(
      data
          .componentOf<LegendComboboxThemeNullable>(LegendCombobox)
          ?.menuBackground,
    );
const _$LegendComboboxAspectMenuBackground = LegendThemeAspect(
  _$LegendComboboxSelectMenuBackground,
);
Object? _$LegendComboboxOverrideSelectMenuBackground(
  LegendComboboxThemeNullable data,
) => data.menuBackground;
const _$LegendComboboxOverrideAspectMenuBackground =
    LegendOverrideAspect<LegendComboboxThemeNullable>(
      _$LegendComboboxOverrideSelectMenuBackground,
    );

/// Resolves [LegendCombobox.menuBorderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendComboboxSelectMenuBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendComboboxThemeNullable>(LegendCombobox)
        ?.menuBorderRadius ??
    _menuBorderRadius(data.tokens);
const _$LegendComboboxAspectMenuBorderRadius = LegendThemeAspect(
  _$LegendComboboxSelectMenuBorderRadius,
);
Object? _$LegendComboboxOverrideSelectMenuBorderRadius(
  LegendComboboxThemeNullable data,
) => data.menuBorderRadius;
const _$LegendComboboxOverrideAspectMenuBorderRadius =
    LegendOverrideAspect<LegendComboboxThemeNullable>(
      _$LegendComboboxOverrideSelectMenuBorderRadius,
    );

/// Resolves [LegendCombobox.menuShadows] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
List<BoxShadow> _$LegendComboboxSelectMenuShadows(LegendThemeData data) =>
    data
        .componentOf<LegendComboboxThemeNullable>(LegendCombobox)
        ?.menuShadows ??
    ShadowRef.medium(data.tokens);
const _$LegendComboboxAspectMenuShadows = LegendThemeAspect(
  _$LegendComboboxSelectMenuShadows,
);
Object? _$LegendComboboxOverrideSelectMenuShadows(
  LegendComboboxThemeNullable data,
) => data.menuShadows;
const _$LegendComboboxOverrideAspectMenuShadows =
    LegendOverrideAspect<LegendComboboxThemeNullable>(
      _$LegendComboboxOverrideSelectMenuShadows,
    );

/// Resolves [LegendCombobox.menuMaxHeight] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendComboboxSelectMenuMaxHeight(LegendThemeData data) =>
    data
        .componentOf<LegendComboboxThemeNullable>(LegendCombobox)
        ?.menuMaxHeight ??
    320;
const _$LegendComboboxAspectMenuMaxHeight = LegendThemeAspect(
  _$LegendComboboxSelectMenuMaxHeight,
);
Object? _$LegendComboboxOverrideSelectMenuMaxHeight(
  LegendComboboxThemeNullable data,
) => data.menuMaxHeight;
const _$LegendComboboxOverrideAspectMenuMaxHeight =
    LegendOverrideAspect<LegendComboboxThemeNullable>(
      _$LegendComboboxOverrideSelectMenuMaxHeight,
    );

/// Resolves [LegendCombobox.itemPadding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendComboboxSelectItemPadding(LegendThemeData data) =>
    data
        .componentOf<LegendComboboxThemeNullable>(LegendCombobox)
        ?.itemPadding ??
    _itemPadding(data.tokens);
const _$LegendComboboxAspectItemPadding = LegendThemeAspect(
  _$LegendComboboxSelectItemPadding,
);
Object? _$LegendComboboxOverrideSelectItemPadding(
  LegendComboboxThemeNullable data,
) => data.itemPadding;
const _$LegendComboboxOverrideAspectItemPadding =
    LegendOverrideAspect<LegendComboboxThemeNullable>(
      _$LegendComboboxOverrideSelectItemPadding,
    );

/// Resolves [LegendCombobox.textStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendComboboxSelectTextStyle(LegendThemeData data) =>
    data.componentOf<LegendComboboxThemeNullable>(LegendCombobox)?.textStyle ??
    TextRef.b2(data.tokens);
const _$LegendComboboxAspectTextStyle = LegendThemeAspect(
  _$LegendComboboxSelectTextStyle,
);
Object? _$LegendComboboxOverrideSelectTextStyle(
  LegendComboboxThemeNullable data,
) => data.textStyle;
const _$LegendComboboxOverrideAspectTextStyle =
    LegendOverrideAspect<LegendComboboxThemeNullable>(
      _$LegendComboboxOverrideSelectTextStyle,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendComboboxThemeListenables {
  /// Change stream of the resolved [LegendComboboxTheme.menuBackground].
  static ValueListenable<InteractiveColors> menuBackground(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendComboboxSelectMenuBackground);

  /// Change stream of the resolved [LegendComboboxTheme.menuBorderRadius].
  static ValueListenable<BorderRadius> menuBorderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) =>
      LegendThemeSelector(source, data, _$LegendComboboxSelectMenuBorderRadius);

  /// Change stream of the resolved [LegendComboboxTheme.menuShadows].
  static ValueListenable<List<BoxShadow>> menuShadows(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendComboboxSelectMenuShadows);

  /// Change stream of the resolved [LegendComboboxTheme.menuMaxHeight].
  static ValueListenable<double> menuMaxHeight(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendComboboxSelectMenuMaxHeight);

  /// Change stream of the resolved [LegendComboboxTheme.itemPadding].
  static ValueListenable<EdgeInsetsGeometry> itemPadding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendComboboxSelectItemPadding);

  /// Change stream of the resolved [LegendComboboxTheme.textStyle].
  static ValueListenable<TextStyle> textStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendComboboxSelectTextStyle);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendComboboxThemeResolve on LegendCombobox {
  /// [LegendComboboxTheme.of] with this widget's constructor params as
  /// level 1.
  LegendComboboxTheme _theme(BuildContext context) => LegendComboboxTheme.of(
    context,
    LegendComboboxThemeNullable(
      menuBackground: menuBackground,
      menuBorderRadius: menuBorderRadius,
      menuShadows: menuShadows,
      menuMaxHeight: menuMaxHeight,
      itemPadding: itemPadding,
      textStyle: textStyle,
    ),
  );

  /// Resolves ONLY [LegendCombobox.menuBackground] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._menuBackground(context)`).
  InteractiveColors _menuBackground(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuBackground);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuBackground,
    );
    return _$LegendComboboxSelectMenuBackground(
      data,
    ).merge(override?.menuBackground).merge(menuBackground);
  }

  /// Resolves ONLY [LegendCombobox.menuBorderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._menuBorderRadius(context)`).
  BorderRadius _menuBorderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuBorderRadius);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuBorderRadius,
    );
    return menuBorderRadius ??
        override?.menuBorderRadius ??
        _$LegendComboboxSelectMenuBorderRadius(data);
  }

  /// Resolves ONLY [LegendCombobox.menuShadows] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._menuShadows(context)`).
  List<BoxShadow> _menuShadows(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuShadows);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuShadows,
    );
    return menuShadows ??
        override?.menuShadows ??
        _$LegendComboboxSelectMenuShadows(data);
  }

  /// Resolves ONLY [LegendCombobox.menuMaxHeight] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._menuMaxHeight(context)`).
  double _menuMaxHeight(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectMenuMaxHeight);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectMenuMaxHeight,
    );
    return menuMaxHeight ??
        override?.menuMaxHeight ??
        _$LegendComboboxSelectMenuMaxHeight(data);
  }

  /// Resolves ONLY [LegendCombobox.itemPadding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemPadding(context)`).
  EdgeInsetsGeometry _itemPadding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectItemPadding);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectItemPadding,
    );
    return itemPadding ??
        override?.itemPadding ??
        _$LegendComboboxSelectItemPadding(data);
  }

  /// Resolves ONLY [LegendCombobox.textStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._textStyle(context)`).
  TextStyle _textStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendComboboxThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendComboboxAspectTextStyle);
    LegendThemeOverride.depend<LegendComboboxThemeNullable>(
      context,
      _$LegendComboboxOverrideAspectTextStyle,
    );
    return textStyle ??
        override?.textStyle ??
        _$LegendComboboxSelectTextStyle(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendComboboxState]
/// is this file's `State<LegendCombobox>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendComboboxThemeState])
/// wins over this extension.
extension _$LegendComboboxThemeOn_LegendComboboxState<T>
    on _LegendComboboxState<T> {
  LegendComboboxTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendCombobox>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendComboboxThemeState on State<LegendCombobox> {
  LegendComboboxTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendCombobox extends
/// _$LegendComboboxBase` receives the resolved
/// [LegendComboboxTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendComboboxBase
    extends LegendStatelessWidget<LegendComboboxTheme> {
  const _$LegendComboboxBase({super.key});

  InteractiveColors? get menuBackground;
  BorderRadius? get menuBorderRadius;
  List<BoxShadow>? get menuShadows;
  double? get menuMaxHeight;
  EdgeInsetsGeometry? get itemPadding;
  TextStyle? get textStyle;

  @override
  LegendComboboxTheme resolveThemeOf(BuildContext context) =>
      LegendComboboxTheme.of(
        context,
        LegendComboboxThemeNullable(
          menuBackground: menuBackground,
          menuBorderRadius: menuBorderRadius,
          menuShadows: menuShadows,
          menuMaxHeight: menuMaxHeight,
          itemPadding: itemPadding,
          textStyle: textStyle,
        ),
      );
}
