// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_number_field.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_number_field.dart';

/// Resolved theme for [LegendNumberField].
class LegendNumberFieldTheme {
  const LegendNumberFieldTheme({
    required this.background,
    required this.borderRadius,
    required this.padding,
    required this.textStyle,
    required this.borderColor,
    required this.focusedBorderColor,
    required this.stepperColors,
    required this.stepperForeground,
    required this.stepperWidth,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendNumberFieldTheme.defaults(LegendTokens t) =>
      LegendNumberFieldTheme(
        background: ColorRef.background1(t),
        borderRadius: _borderRadius(t),
        padding: _padding(t),
        textStyle: TextRef.b1(t),
        borderColor: ColorRef.background3(t),
        focusedBorderColor: ColorRef.primary(t),
        stepperColors: _stepperColors(t),
        stepperForeground: ColorRef.foreground2(t),
        stepperWidth: SizeRef.lg(t),
      );

  final Color background;
  final BorderRadius borderRadius;
  final EdgeInsetsGeometry padding;
  final TextStyle textStyle;
  final Color borderColor;
  final Color focusedBorderColor;
  final InteractiveColors stepperColors;
  final Color stepperForeground;
  final double stepperWidth;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendNumberField] first, [LegendNumberFieldThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendNumberFieldTheme of(
    BuildContext context, [
    LegendNumberFieldThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectBackground);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectBorderRadius);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectPadding);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectTextStyle);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectTextStyle,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectBorderColor);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectBorderColor,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectFocusedBorderColor);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectFocusedBorderColor,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectStepperColors);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectStepperColors,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectStepperForeground);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectStepperForeground,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectStepperWidth);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectStepperWidth,
    );
    return LegendNumberFieldTheme.defaults(data.tokens)
        .merge(
          data.componentOf<LegendNumberFieldThemeNullable>(LegendNumberField),
        )
        .merge(override)
        .merge(local);
  }

  LegendNumberFieldTheme merge(LegendNumberFieldThemeNullable? other) {
    if (other == null) return this;
    return LegendNumberFieldTheme(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      textStyle: other.textStyle ?? textStyle,
      borderColor: other.borderColor ?? borderColor,
      focusedBorderColor: other.focusedBorderColor ?? focusedBorderColor,
      stepperColors: stepperColors.merge(other.stepperColors),
      stepperForeground: other.stepperForeground ?? stepperForeground,
      stepperWidth: other.stepperWidth ?? stepperWidth,
    );
  }

  LegendNumberFieldTheme copyWith({
    Color? background,
    BorderRadius? borderRadius,
    EdgeInsetsGeometry? padding,
    TextStyle? textStyle,
    Color? borderColor,
    Color? focusedBorderColor,
    InteractiveColors? stepperColors,
    Color? stepperForeground,
    double? stepperWidth,
  }) => LegendNumberFieldTheme(
    background: background ?? this.background,
    borderRadius: borderRadius ?? this.borderRadius,
    padding: padding ?? this.padding,
    textStyle: textStyle ?? this.textStyle,
    borderColor: borderColor ?? this.borderColor,
    focusedBorderColor: focusedBorderColor ?? this.focusedBorderColor,
    stepperColors: stepperColors ?? this.stepperColors,
    stepperForeground: stepperForeground ?? this.stepperForeground,
    stepperWidth: stepperWidth ?? this.stepperWidth,
  );

  static LegendNumberFieldTheme lerp(
    LegendNumberFieldTheme a,
    LegendNumberFieldTheme b,
    double t,
  ) => LegendNumberFieldTheme(
    background: t < 0.5 ? a.background : b.background,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    padding: t < 0.5 ? a.padding : b.padding,
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
    borderColor: t < 0.5 ? a.borderColor : b.borderColor,
    focusedBorderColor: t < 0.5 ? a.focusedBorderColor : b.focusedBorderColor,
    stepperColors: t < 0.5 ? a.stepperColors : b.stepperColors,
    stepperForeground: t < 0.5 ? a.stepperForeground : b.stepperForeground,
    stepperWidth: t < 0.5 ? a.stepperWidth : b.stepperWidth,
  );
}

/// Sparse overrides for [LegendNumberFieldTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendNumberFieldThemeNullable {
  const LegendNumberFieldThemeNullable({
    this.background,
    this.borderRadius,
    this.padding,
    this.textStyle,
    this.borderColor,
    this.focusedBorderColor,
    this.stepperColors,
    this.stepperForeground,
    this.stepperWidth,
  });

  final Color? background;
  final BorderRadius? borderRadius;
  final EdgeInsetsGeometry? padding;
  final TextStyle? textStyle;
  final Color? borderColor;
  final Color? focusedBorderColor;
  final InteractiveColors? stepperColors;
  final Color? stepperForeground;
  final double? stepperWidth;

  LegendNumberFieldThemeNullable merge(LegendNumberFieldThemeNullable? other) {
    if (other == null) return this;
    return LegendNumberFieldThemeNullable(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      textStyle: other.textStyle ?? textStyle,
      borderColor: other.borderColor ?? borderColor,
      focusedBorderColor: other.focusedBorderColor ?? focusedBorderColor,
      stepperColors:
          stepperColors?.merge(other.stepperColors) ?? other.stepperColors,
      stepperForeground: other.stepperForeground ?? stepperForeground,
      stepperWidth: other.stepperWidth ?? stepperWidth,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendNumberFieldThemeNullable &&
          other.background == background &&
          other.borderRadius == borderRadius &&
          other.padding == padding &&
          other.textStyle == textStyle &&
          other.borderColor == borderColor &&
          other.focusedBorderColor == focusedBorderColor &&
          other.stepperColors == stepperColors &&
          other.stepperForeground == stepperForeground &&
          other.stepperWidth == stepperWidth;

  @override
  int get hashCode => Object.hashAll([
    background,
    borderRadius,
    padding,
    textStyle,
    borderColor,
    focusedBorderColor,
    stepperColors,
    stepperForeground,
    stepperWidth,
  ]);
}

/// Overrides [LegendNumberFieldTheme] for a subtree (level 2).
class LegendNumberFieldThemeOverride extends StatelessWidget {
  const LegendNumberFieldThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendNumberFieldThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendNumberFieldThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendNumberField.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendNumberFieldSelectBackground(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.background ??
    ColorRef.background1(data.tokens);
const _$LegendNumberFieldAspectBackground = LegendThemeAspect(
  _$LegendNumberFieldSelectBackground,
);
Object? _$LegendNumberFieldOverrideSelectBackground(
  LegendNumberFieldThemeNullable data,
) => data.background;
const _$LegendNumberFieldOverrideAspectBackground =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectBackground,
    );

/// Resolves [LegendNumberField.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendNumberFieldSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendNumberFieldAspectBorderRadius = LegendThemeAspect(
  _$LegendNumberFieldSelectBorderRadius,
);
Object? _$LegendNumberFieldOverrideSelectBorderRadius(
  LegendNumberFieldThemeNullable data,
) => data.borderRadius;
const _$LegendNumberFieldOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectBorderRadius,
    );

/// Resolves [LegendNumberField.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendNumberFieldSelectPadding(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.padding ??
    _padding(data.tokens);
const _$LegendNumberFieldAspectPadding = LegendThemeAspect(
  _$LegendNumberFieldSelectPadding,
);
Object? _$LegendNumberFieldOverrideSelectPadding(
  LegendNumberFieldThemeNullable data,
) => data.padding;
const _$LegendNumberFieldOverrideAspectPadding =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectPadding,
    );

/// Resolves [LegendNumberField.textStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendNumberFieldSelectTextStyle(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.textStyle ??
    TextRef.b1(data.tokens);
const _$LegendNumberFieldAspectTextStyle = LegendThemeAspect(
  _$LegendNumberFieldSelectTextStyle,
);
Object? _$LegendNumberFieldOverrideSelectTextStyle(
  LegendNumberFieldThemeNullable data,
) => data.textStyle;
const _$LegendNumberFieldOverrideAspectTextStyle =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectTextStyle,
    );

/// Resolves [LegendNumberField.borderColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendNumberFieldSelectBorderColor(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.borderColor ??
    ColorRef.background3(data.tokens);
const _$LegendNumberFieldAspectBorderColor = LegendThemeAspect(
  _$LegendNumberFieldSelectBorderColor,
);
Object? _$LegendNumberFieldOverrideSelectBorderColor(
  LegendNumberFieldThemeNullable data,
) => data.borderColor;
const _$LegendNumberFieldOverrideAspectBorderColor =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectBorderColor,
    );

/// Resolves [LegendNumberField.focusedBorderColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendNumberFieldSelectFocusedBorderColor(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.focusedBorderColor ??
    ColorRef.primary(data.tokens);
const _$LegendNumberFieldAspectFocusedBorderColor = LegendThemeAspect(
  _$LegendNumberFieldSelectFocusedBorderColor,
);
Object? _$LegendNumberFieldOverrideSelectFocusedBorderColor(
  LegendNumberFieldThemeNullable data,
) => data.focusedBorderColor;
const _$LegendNumberFieldOverrideAspectFocusedBorderColor =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectFocusedBorderColor,
    );

/// Resolves [LegendNumberField.stepperColors] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendNumberFieldSelectStepperColors(
  LegendThemeData data,
) => _stepperColors(data.tokens).merge(
  data
      .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
      ?.stepperColors,
);
const _$LegendNumberFieldAspectStepperColors = LegendThemeAspect(
  _$LegendNumberFieldSelectStepperColors,
);
Object? _$LegendNumberFieldOverrideSelectStepperColors(
  LegendNumberFieldThemeNullable data,
) => data.stepperColors;
const _$LegendNumberFieldOverrideAspectStepperColors =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectStepperColors,
    );

/// Resolves [LegendNumberField.stepperForeground] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendNumberFieldSelectStepperForeground(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.stepperForeground ??
    ColorRef.foreground2(data.tokens);
const _$LegendNumberFieldAspectStepperForeground = LegendThemeAspect(
  _$LegendNumberFieldSelectStepperForeground,
);
Object? _$LegendNumberFieldOverrideSelectStepperForeground(
  LegendNumberFieldThemeNullable data,
) => data.stepperForeground;
const _$LegendNumberFieldOverrideAspectStepperForeground =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectStepperForeground,
    );

/// Resolves [LegendNumberField.stepperWidth] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendNumberFieldSelectStepperWidth(LegendThemeData data) =>
    data
        .componentOf<LegendNumberFieldThemeNullable>(LegendNumberField)
        ?.stepperWidth ??
    SizeRef.lg(data.tokens);
const _$LegendNumberFieldAspectStepperWidth = LegendThemeAspect(
  _$LegendNumberFieldSelectStepperWidth,
);
Object? _$LegendNumberFieldOverrideSelectStepperWidth(
  LegendNumberFieldThemeNullable data,
) => data.stepperWidth;
const _$LegendNumberFieldOverrideAspectStepperWidth =
    LegendOverrideAspect<LegendNumberFieldThemeNullable>(
      _$LegendNumberFieldOverrideSelectStepperWidth,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendNumberFieldThemeListenables {
  /// Change stream of the resolved [LegendNumberFieldTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendNumberFieldSelectBackground);

  /// Change stream of the resolved [LegendNumberFieldTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendNumberFieldSelectBorderRadius);

  /// Change stream of the resolved [LegendNumberFieldTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendNumberFieldSelectPadding);

  /// Change stream of the resolved [LegendNumberFieldTheme.textStyle].
  static ValueListenable<TextStyle> textStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendNumberFieldSelectTextStyle);

  /// Change stream of the resolved [LegendNumberFieldTheme.borderColor].
  static ValueListenable<Color> borderColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendNumberFieldSelectBorderColor);

  /// Change stream of the resolved [LegendNumberFieldTheme.focusedBorderColor].
  static ValueListenable<Color> focusedBorderColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendNumberFieldSelectFocusedBorderColor,
  );

  /// Change stream of the resolved [LegendNumberFieldTheme.stepperColors].
  static ValueListenable<InteractiveColors> stepperColors(
    Listenable source,
    LegendThemeData Function() data,
  ) =>
      LegendThemeSelector(source, data, _$LegendNumberFieldSelectStepperColors);

  /// Change stream of the resolved [LegendNumberFieldTheme.stepperForeground].
  static ValueListenable<Color> stepperForeground(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendNumberFieldSelectStepperForeground,
  );

  /// Change stream of the resolved [LegendNumberFieldTheme.stepperWidth].
  static ValueListenable<double> stepperWidth(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendNumberFieldSelectStepperWidth);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendNumberFieldThemeResolve on LegendNumberField {
  /// [LegendNumberFieldTheme.of] with this widget's constructor params as
  /// level 1.
  LegendNumberFieldTheme _theme(BuildContext context) =>
      LegendNumberFieldTheme.of(
        context,
        LegendNumberFieldThemeNullable(
          background: background,
          borderRadius: borderRadius,
          padding: padding,
          textStyle: textStyle,
          borderColor: borderColor,
          focusedBorderColor: focusedBorderColor,
          stepperColors: stepperColors,
          stepperForeground: stepperForeground,
          stepperWidth: stepperWidth,
        ),
      );

  /// Resolves ONLY [LegendNumberField.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectBackground);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendNumberFieldSelectBackground(data);
  }

  /// Resolves ONLY [LegendNumberField.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectBorderRadius);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendNumberFieldSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendNumberField.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectPadding);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectPadding,
    );
    return padding ??
        override?.padding ??
        _$LegendNumberFieldSelectPadding(data);
  }

  /// Resolves ONLY [LegendNumberField.textStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._textStyle(context)`).
  TextStyle _textStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectTextStyle);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectTextStyle,
    );
    return textStyle ??
        override?.textStyle ??
        _$LegendNumberFieldSelectTextStyle(data);
  }

  /// Resolves ONLY [LegendNumberField.borderColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderColor(context)`).
  Color _borderColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectBorderColor);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectBorderColor,
    );
    return borderColor ??
        override?.borderColor ??
        _$LegendNumberFieldSelectBorderColor(data);
  }

  /// Resolves ONLY [LegendNumberField.focusedBorderColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._focusedBorderColor(context)`).
  Color _focusedBorderColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectFocusedBorderColor);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectFocusedBorderColor,
    );
    return focusedBorderColor ??
        override?.focusedBorderColor ??
        _$LegendNumberFieldSelectFocusedBorderColor(data);
  }

  /// Resolves ONLY [LegendNumberField.stepperColors] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._stepperColors(context)`).
  InteractiveColors _stepperColors(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectStepperColors);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectStepperColors,
    );
    return _$LegendNumberFieldSelectStepperColors(
      data,
    ).merge(override?.stepperColors).merge(stepperColors);
  }

  /// Resolves ONLY [LegendNumberField.stepperForeground] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._stepperForeground(context)`).
  Color _stepperForeground(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectStepperForeground);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectStepperForeground,
    );
    return stepperForeground ??
        override?.stepperForeground ??
        _$LegendNumberFieldSelectStepperForeground(data);
  }

  /// Resolves ONLY [LegendNumberField.stepperWidth] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._stepperWidth(context)`).
  double _stepperWidth(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendNumberFieldThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendNumberFieldAspectStepperWidth);
    LegendThemeOverride.depend<LegendNumberFieldThemeNullable>(
      context,
      _$LegendNumberFieldOverrideAspectStepperWidth,
    );
    return stepperWidth ??
        override?.stepperWidth ??
        _$LegendNumberFieldSelectStepperWidth(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendNumberFieldState]
/// is this file's `State<LegendNumberField>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendNumberFieldThemeState])
/// wins over this extension.
extension _$LegendNumberFieldThemeOn_LegendNumberFieldState
    on _LegendNumberFieldState {
  LegendNumberFieldTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendNumberField>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendNumberFieldThemeState on State<LegendNumberField> {
  LegendNumberFieldTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendNumberField extends
/// _$LegendNumberFieldBase` receives the resolved
/// [LegendNumberFieldTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendNumberFieldBase
    extends LegendStatelessWidget<LegendNumberFieldTheme> {
  const _$LegendNumberFieldBase({super.key});

  Color? get background;
  BorderRadius? get borderRadius;
  EdgeInsetsGeometry? get padding;
  TextStyle? get textStyle;
  Color? get borderColor;
  Color? get focusedBorderColor;
  InteractiveColors? get stepperColors;
  Color? get stepperForeground;
  double? get stepperWidth;

  @override
  LegendNumberFieldTheme resolveThemeOf(BuildContext context) =>
      LegendNumberFieldTheme.of(
        context,
        LegendNumberFieldThemeNullable(
          background: background,
          borderRadius: borderRadius,
          padding: padding,
          textStyle: textStyle,
          borderColor: borderColor,
          focusedBorderColor: focusedBorderColor,
          stepperColors: stepperColors,
          stepperForeground: stepperForeground,
          stepperWidth: stepperWidth,
        ),
      );
}
