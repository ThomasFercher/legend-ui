// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_list_item.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_list_item.dart';

/// Resolved theme for [LegendListItem].
class LegendListItemTheme {
  const LegendListItemTheme({
    required this.colors,
    required this.selectedBackground,
    required this.selectedColor,
    required this.titleStyle,
    required this.subtitleStyle,
    required this.padding,
    required this.borderRadius,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendListItemTheme.defaults(LegendTokens t) => LegendListItemTheme(
    colors: _colors(t),
    selectedBackground: ColorRef.primaryContainer(t),
    selectedColor: ColorRef.primary(t),
    titleStyle: _titleStyle(t),
    subtitleStyle: _subtitleStyle(t),
    padding: _padding(t),
    borderRadius: _borderRadius(t),
  );

  final InteractiveColors colors;
  final Color selectedBackground;
  final Color selectedColor;
  final TextStyle titleStyle;
  final TextStyle subtitleStyle;
  final EdgeInsetsGeometry padding;
  final BorderRadius borderRadius;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendListItem] first, [LegendListItemThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendListItemTheme of(
    BuildContext context, [
    LegendListItemThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectColors);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectColors,
    );
    LegendTheme.depend(context, _$LegendListItemAspectSelectedBackground);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectSelectedBackground,
    );
    LegendTheme.depend(context, _$LegendListItemAspectSelectedColor);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectSelectedColor,
    );
    LegendTheme.depend(context, _$LegendListItemAspectTitleStyle);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectTitleStyle,
    );
    LegendTheme.depend(context, _$LegendListItemAspectSubtitleStyle);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectSubtitleStyle,
    );
    LegendTheme.depend(context, _$LegendListItemAspectPadding);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$LegendListItemAspectBorderRadius);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectBorderRadius,
    );
    return LegendListItemTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendListItemThemeNullable>(LegendListItem))
        .merge(override)
        .merge(local);
  }

  LegendListItemTheme merge(LegendListItemThemeNullable? other) {
    if (other == null) return this;
    return LegendListItemTheme(
      colors: colors.merge(other.colors),
      selectedBackground: other.selectedBackground ?? selectedBackground,
      selectedColor: other.selectedColor ?? selectedColor,
      titleStyle: other.titleStyle ?? titleStyle,
      subtitleStyle: other.subtitleStyle ?? subtitleStyle,
      padding: other.padding ?? padding,
      borderRadius: other.borderRadius ?? borderRadius,
    );
  }

  LegendListItemTheme copyWith({
    InteractiveColors? colors,
    Color? selectedBackground,
    Color? selectedColor,
    TextStyle? titleStyle,
    TextStyle? subtitleStyle,
    EdgeInsetsGeometry? padding,
    BorderRadius? borderRadius,
  }) => LegendListItemTheme(
    colors: colors ?? this.colors,
    selectedBackground: selectedBackground ?? this.selectedBackground,
    selectedColor: selectedColor ?? this.selectedColor,
    titleStyle: titleStyle ?? this.titleStyle,
    subtitleStyle: subtitleStyle ?? this.subtitleStyle,
    padding: padding ?? this.padding,
    borderRadius: borderRadius ?? this.borderRadius,
  );

  static LegendListItemTheme lerp(
    LegendListItemTheme a,
    LegendListItemTheme b,
    double t,
  ) => LegendListItemTheme(
    colors: t < 0.5 ? a.colors : b.colors,
    selectedBackground: Color.lerp(
      a.selectedBackground,
      b.selectedBackground,
      t,
    )!,
    selectedColor: Color.lerp(a.selectedColor, b.selectedColor, t)!,
    titleStyle: t < 0.5 ? a.titleStyle : b.titleStyle,
    subtitleStyle: t < 0.5 ? a.subtitleStyle : b.subtitleStyle,
    padding: t < 0.5 ? a.padding : b.padding,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
  );
}

/// Sparse overrides for [LegendListItemTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendListItemThemeNullable {
  const LegendListItemThemeNullable({
    this.colors,
    this.selectedBackground,
    this.selectedColor,
    this.titleStyle,
    this.subtitleStyle,
    this.padding,
    this.borderRadius,
  });

  final InteractiveColors? colors;
  final Color? selectedBackground;
  final Color? selectedColor;
  final TextStyle? titleStyle;
  final TextStyle? subtitleStyle;
  final EdgeInsetsGeometry? padding;
  final BorderRadius? borderRadius;

  LegendListItemThemeNullable merge(LegendListItemThemeNullable? other) {
    if (other == null) return this;
    return LegendListItemThemeNullable(
      colors: colors?.merge(other.colors) ?? other.colors,
      selectedBackground: other.selectedBackground ?? selectedBackground,
      selectedColor: other.selectedColor ?? selectedColor,
      titleStyle: other.titleStyle ?? titleStyle,
      subtitleStyle: other.subtitleStyle ?? subtitleStyle,
      padding: other.padding ?? padding,
      borderRadius: other.borderRadius ?? borderRadius,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendListItemThemeNullable &&
          other.colors == colors &&
          other.selectedBackground == selectedBackground &&
          other.selectedColor == selectedColor &&
          other.titleStyle == titleStyle &&
          other.subtitleStyle == subtitleStyle &&
          other.padding == padding &&
          other.borderRadius == borderRadius;

  @override
  int get hashCode => Object.hashAll([
    colors,
    selectedBackground,
    selectedColor,
    titleStyle,
    subtitleStyle,
    padding,
    borderRadius,
  ]);
}

/// Overrides [LegendListItemTheme] for a subtree (level 2).
class LegendListItemThemeOverride extends StatelessWidget {
  const LegendListItemThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendListItemThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendListItemThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendListItem.colors] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendListItemSelectColors(LegendThemeData data) => _colors(
  data.tokens,
).merge(data.componentOf<LegendListItemThemeNullable>(LegendListItem)?.colors);
const _$LegendListItemAspectColors = LegendThemeAspect(
  _$LegendListItemSelectColors,
);
Object? _$LegendListItemOverrideSelectColors(
  LegendListItemThemeNullable data,
) => data.colors;
const _$LegendListItemOverrideAspectColors =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectColors,
    );

/// Resolves [LegendListItem.selectedBackground] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendListItemSelectSelectedBackground(LegendThemeData data) =>
    data
        .componentOf<LegendListItemThemeNullable>(LegendListItem)
        ?.selectedBackground ??
    ColorRef.primaryContainer(data.tokens);
const _$LegendListItemAspectSelectedBackground = LegendThemeAspect(
  _$LegendListItemSelectSelectedBackground,
);
Object? _$LegendListItemOverrideSelectSelectedBackground(
  LegendListItemThemeNullable data,
) => data.selectedBackground;
const _$LegendListItemOverrideAspectSelectedBackground =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectSelectedBackground,
    );

/// Resolves [LegendListItem.selectedColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendListItemSelectSelectedColor(LegendThemeData data) =>
    data
        .componentOf<LegendListItemThemeNullable>(LegendListItem)
        ?.selectedColor ??
    ColorRef.primary(data.tokens);
const _$LegendListItemAspectSelectedColor = LegendThemeAspect(
  _$LegendListItemSelectSelectedColor,
);
Object? _$LegendListItemOverrideSelectSelectedColor(
  LegendListItemThemeNullable data,
) => data.selectedColor;
const _$LegendListItemOverrideAspectSelectedColor =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectSelectedColor,
    );

/// Resolves [LegendListItem.titleStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendListItemSelectTitleStyle(LegendThemeData data) =>
    data.componentOf<LegendListItemThemeNullable>(LegendListItem)?.titleStyle ??
    _titleStyle(data.tokens);
const _$LegendListItemAspectTitleStyle = LegendThemeAspect(
  _$LegendListItemSelectTitleStyle,
);
Object? _$LegendListItemOverrideSelectTitleStyle(
  LegendListItemThemeNullable data,
) => data.titleStyle;
const _$LegendListItemOverrideAspectTitleStyle =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectTitleStyle,
    );

/// Resolves [LegendListItem.subtitleStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendListItemSelectSubtitleStyle(LegendThemeData data) =>
    data
        .componentOf<LegendListItemThemeNullable>(LegendListItem)
        ?.subtitleStyle ??
    _subtitleStyle(data.tokens);
const _$LegendListItemAspectSubtitleStyle = LegendThemeAspect(
  _$LegendListItemSelectSubtitleStyle,
);
Object? _$LegendListItemOverrideSelectSubtitleStyle(
  LegendListItemThemeNullable data,
) => data.subtitleStyle;
const _$LegendListItemOverrideAspectSubtitleStyle =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectSubtitleStyle,
    );

/// Resolves [LegendListItem.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendListItemSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendListItemThemeNullable>(LegendListItem)?.padding ??
    _padding(data.tokens);
const _$LegendListItemAspectPadding = LegendThemeAspect(
  _$LegendListItemSelectPadding,
);
Object? _$LegendListItemOverrideSelectPadding(
  LegendListItemThemeNullable data,
) => data.padding;
const _$LegendListItemOverrideAspectPadding =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectPadding,
    );

/// Resolves [LegendListItem.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendListItemSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendListItemThemeNullable>(LegendListItem)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendListItemAspectBorderRadius = LegendThemeAspect(
  _$LegendListItemSelectBorderRadius,
);
Object? _$LegendListItemOverrideSelectBorderRadius(
  LegendListItemThemeNullable data,
) => data.borderRadius;
const _$LegendListItemOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendListItemThemeNullable>(
      _$LegendListItemOverrideSelectBorderRadius,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendListItemThemeListenables {
  /// Change stream of the resolved [LegendListItemTheme.colors].
  static ValueListenable<InteractiveColors> colors(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendListItemSelectColors);

  /// Change stream of the resolved [LegendListItemTheme.selectedBackground].
  static ValueListenable<Color> selectedBackground(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendListItemSelectSelectedBackground,
  );

  /// Change stream of the resolved [LegendListItemTheme.selectedColor].
  static ValueListenable<Color> selectedColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendListItemSelectSelectedColor);

  /// Change stream of the resolved [LegendListItemTheme.titleStyle].
  static ValueListenable<TextStyle> titleStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendListItemSelectTitleStyle);

  /// Change stream of the resolved [LegendListItemTheme.subtitleStyle].
  static ValueListenable<TextStyle> subtitleStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendListItemSelectSubtitleStyle);

  /// Change stream of the resolved [LegendListItemTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendListItemSelectPadding);

  /// Change stream of the resolved [LegendListItemTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendListItemSelectBorderRadius);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendListItemThemeResolve on LegendListItem {
  /// [LegendListItemTheme.of] with this widget's constructor params as
  /// level 1.
  LegendListItemTheme _theme(BuildContext context) => LegendListItemTheme.of(
    context,
    LegendListItemThemeNullable(
      colors: colors,
      selectedBackground: selectedBackground,
      selectedColor: selectedColor,
      titleStyle: titleStyle,
      subtitleStyle: subtitleStyle,
      padding: padding,
      borderRadius: borderRadius,
    ),
  );

  /// Resolves ONLY [LegendListItem.colors] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._colors(context)`).
  InteractiveColors _colors(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectColors);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectColors,
    );
    return _$LegendListItemSelectColors(
      data,
    ).merge(override?.colors).merge(colors);
  }

  /// Resolves ONLY [LegendListItem.selectedBackground] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedBackground(context)`).
  Color _selectedBackground(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectSelectedBackground);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectSelectedBackground,
    );
    return selectedBackground ??
        override?.selectedBackground ??
        _$LegendListItemSelectSelectedBackground(data);
  }

  /// Resolves ONLY [LegendListItem.selectedColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedColor(context)`).
  Color _selectedColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectSelectedColor);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectSelectedColor,
    );
    return selectedColor ??
        override?.selectedColor ??
        _$LegendListItemSelectSelectedColor(data);
  }

  /// Resolves ONLY [LegendListItem.titleStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._titleStyle(context)`).
  TextStyle _titleStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectTitleStyle);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectTitleStyle,
    );
    return titleStyle ??
        override?.titleStyle ??
        _$LegendListItemSelectTitleStyle(data);
  }

  /// Resolves ONLY [LegendListItem.subtitleStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._subtitleStyle(context)`).
  TextStyle _subtitleStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectSubtitleStyle);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectSubtitleStyle,
    );
    return subtitleStyle ??
        override?.subtitleStyle ??
        _$LegendListItemSelectSubtitleStyle(data);
  }

  /// Resolves ONLY [LegendListItem.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectPadding);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendListItemSelectPadding(data);
  }

  /// Resolves ONLY [LegendListItem.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendListItemThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendListItemAspectBorderRadius);
    LegendThemeOverride.depend<LegendListItemThemeNullable>(
      context,
      _$LegendListItemOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendListItemSelectBorderRadius(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendListItem extends
/// _$LegendListItemBase` receives the resolved
/// [LegendListItemTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendListItemBase
    extends LegendStatelessWidget<LegendListItemTheme> {
  const _$LegendListItemBase({super.key});

  InteractiveColors? get colors;
  Color? get selectedBackground;
  Color? get selectedColor;
  TextStyle? get titleStyle;
  TextStyle? get subtitleStyle;
  EdgeInsetsGeometry? get padding;
  BorderRadius? get borderRadius;

  @override
  LegendListItemTheme resolveThemeOf(BuildContext context) =>
      LegendListItemTheme.of(
        context,
        LegendListItemThemeNullable(
          colors: colors,
          selectedBackground: selectedBackground,
          selectedColor: selectedColor,
          titleStyle: titleStyle,
          subtitleStyle: subtitleStyle,
          padding: padding,
          borderRadius: borderRadius,
        ),
      );
}
