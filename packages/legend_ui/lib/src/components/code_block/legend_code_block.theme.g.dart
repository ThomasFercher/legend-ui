// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_code_block.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_code_block.dart';

/// Resolved theme for [LegendCodeBlock].
class LegendCodeBlockTheme {
  const LegendCodeBlockTheme({
    required this.background,
    required this.textStyle,
    required this.borderRadius,
    required this.padding,
    required this.languageLabelStyle,
    required this.copyGlyphColor,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendCodeBlockTheme.defaults(LegendTokens t) => LegendCodeBlockTheme(
    background: ColorRef.background2(t),
    textStyle: _textStyle(t),
    borderRadius: _borderRadius(t),
    padding: _padding(t),
    languageLabelStyle: _languageLabelStyle(t),
    copyGlyphColor: ColorRef.foreground2(t),
  );

  final Color background;
  final TextStyle textStyle;
  final BorderRadius borderRadius;
  final EdgeInsetsGeometry padding;
  final TextStyle languageLabelStyle;
  final Color copyGlyphColor;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendCodeBlock] first, [LegendCodeBlockThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendCodeBlockTheme of(
    BuildContext context, [
    LegendCodeBlockThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectBackground);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectTextStyle);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectTextStyle,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectBorderRadius);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectPadding);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectLanguageLabelStyle);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectLanguageLabelStyle,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectCopyGlyphColor);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectCopyGlyphColor,
    );
    return LegendCodeBlockTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock))
        .merge(override)
        .merge(local);
  }

  LegendCodeBlockTheme merge(LegendCodeBlockThemeNullable? other) {
    if (other == null) return this;
    return LegendCodeBlockTheme(
      background: other.background ?? background,
      textStyle: other.textStyle ?? textStyle,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      languageLabelStyle: other.languageLabelStyle ?? languageLabelStyle,
      copyGlyphColor: other.copyGlyphColor ?? copyGlyphColor,
    );
  }

  LegendCodeBlockTheme copyWith({
    Color? background,
    TextStyle? textStyle,
    BorderRadius? borderRadius,
    EdgeInsetsGeometry? padding,
    TextStyle? languageLabelStyle,
    Color? copyGlyphColor,
  }) => LegendCodeBlockTheme(
    background: background ?? this.background,
    textStyle: textStyle ?? this.textStyle,
    borderRadius: borderRadius ?? this.borderRadius,
    padding: padding ?? this.padding,
    languageLabelStyle: languageLabelStyle ?? this.languageLabelStyle,
    copyGlyphColor: copyGlyphColor ?? this.copyGlyphColor,
  );

  static LegendCodeBlockTheme lerp(
    LegendCodeBlockTheme a,
    LegendCodeBlockTheme b,
    double t,
  ) => LegendCodeBlockTheme(
    background: t < 0.5 ? a.background : b.background,
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    padding: t < 0.5 ? a.padding : b.padding,
    languageLabelStyle: t < 0.5 ? a.languageLabelStyle : b.languageLabelStyle,
    copyGlyphColor: t < 0.5 ? a.copyGlyphColor : b.copyGlyphColor,
  );
}

/// Sparse overrides for [LegendCodeBlockTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendCodeBlockThemeNullable {
  const LegendCodeBlockThemeNullable({
    this.background,
    this.textStyle,
    this.borderRadius,
    this.padding,
    this.languageLabelStyle,
    this.copyGlyphColor,
  });

  final Color? background;
  final TextStyle? textStyle;
  final BorderRadius? borderRadius;
  final EdgeInsetsGeometry? padding;
  final TextStyle? languageLabelStyle;
  final Color? copyGlyphColor;

  LegendCodeBlockThemeNullable merge(LegendCodeBlockThemeNullable? other) {
    if (other == null) return this;
    return LegendCodeBlockThemeNullable(
      background: other.background ?? background,
      textStyle: other.textStyle ?? textStyle,
      borderRadius: other.borderRadius ?? borderRadius,
      padding: other.padding ?? padding,
      languageLabelStyle: other.languageLabelStyle ?? languageLabelStyle,
      copyGlyphColor: other.copyGlyphColor ?? copyGlyphColor,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendCodeBlockThemeNullable &&
          other.background == background &&
          other.textStyle == textStyle &&
          other.borderRadius == borderRadius &&
          other.padding == padding &&
          other.languageLabelStyle == languageLabelStyle &&
          other.copyGlyphColor == copyGlyphColor;

  @override
  int get hashCode => Object.hashAll([
    background,
    textStyle,
    borderRadius,
    padding,
    languageLabelStyle,
    copyGlyphColor,
  ]);
}

/// Overrides [LegendCodeBlockTheme] for a subtree (level 2).
class LegendCodeBlockThemeOverride extends StatelessWidget {
  const LegendCodeBlockThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendCodeBlockThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendCodeBlockThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendCodeBlock.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendCodeBlockSelectBackground(LegendThemeData data) =>
    data
        .componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock)
        ?.background ??
    ColorRef.background2(data.tokens);
const _$LegendCodeBlockAspectBackground = LegendThemeAspect(
  _$LegendCodeBlockSelectBackground,
);
Object? _$LegendCodeBlockOverrideSelectBackground(
  LegendCodeBlockThemeNullable data,
) => data.background;
const _$LegendCodeBlockOverrideAspectBackground =
    LegendOverrideAspect<LegendCodeBlockThemeNullable>(
      _$LegendCodeBlockOverrideSelectBackground,
    );

/// Resolves [LegendCodeBlock.textStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendCodeBlockSelectTextStyle(LegendThemeData data) =>
    data
        .componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock)
        ?.textStyle ??
    _textStyle(data.tokens);
const _$LegendCodeBlockAspectTextStyle = LegendThemeAspect(
  _$LegendCodeBlockSelectTextStyle,
);
Object? _$LegendCodeBlockOverrideSelectTextStyle(
  LegendCodeBlockThemeNullable data,
) => data.textStyle;
const _$LegendCodeBlockOverrideAspectTextStyle =
    LegendOverrideAspect<LegendCodeBlockThemeNullable>(
      _$LegendCodeBlockOverrideSelectTextStyle,
    );

/// Resolves [LegendCodeBlock.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendCodeBlockSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendCodeBlockAspectBorderRadius = LegendThemeAspect(
  _$LegendCodeBlockSelectBorderRadius,
);
Object? _$LegendCodeBlockOverrideSelectBorderRadius(
  LegendCodeBlockThemeNullable data,
) => data.borderRadius;
const _$LegendCodeBlockOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendCodeBlockThemeNullable>(
      _$LegendCodeBlockOverrideSelectBorderRadius,
    );

/// Resolves [LegendCodeBlock.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendCodeBlockSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock)?.padding ??
    _padding(data.tokens);
const _$LegendCodeBlockAspectPadding = LegendThemeAspect(
  _$LegendCodeBlockSelectPadding,
);
Object? _$LegendCodeBlockOverrideSelectPadding(
  LegendCodeBlockThemeNullable data,
) => data.padding;
const _$LegendCodeBlockOverrideAspectPadding =
    LegendOverrideAspect<LegendCodeBlockThemeNullable>(
      _$LegendCodeBlockOverrideSelectPadding,
    );

/// Resolves [LegendCodeBlock.languageLabelStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendCodeBlockSelectLanguageLabelStyle(LegendThemeData data) =>
    data
        .componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock)
        ?.languageLabelStyle ??
    _languageLabelStyle(data.tokens);
const _$LegendCodeBlockAspectLanguageLabelStyle = LegendThemeAspect(
  _$LegendCodeBlockSelectLanguageLabelStyle,
);
Object? _$LegendCodeBlockOverrideSelectLanguageLabelStyle(
  LegendCodeBlockThemeNullable data,
) => data.languageLabelStyle;
const _$LegendCodeBlockOverrideAspectLanguageLabelStyle =
    LegendOverrideAspect<LegendCodeBlockThemeNullable>(
      _$LegendCodeBlockOverrideSelectLanguageLabelStyle,
    );

/// Resolves [LegendCodeBlock.copyGlyphColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendCodeBlockSelectCopyGlyphColor(LegendThemeData data) =>
    data
        .componentOf<LegendCodeBlockThemeNullable>(LegendCodeBlock)
        ?.copyGlyphColor ??
    ColorRef.foreground2(data.tokens);
const _$LegendCodeBlockAspectCopyGlyphColor = LegendThemeAspect(
  _$LegendCodeBlockSelectCopyGlyphColor,
);
Object? _$LegendCodeBlockOverrideSelectCopyGlyphColor(
  LegendCodeBlockThemeNullable data,
) => data.copyGlyphColor;
const _$LegendCodeBlockOverrideAspectCopyGlyphColor =
    LegendOverrideAspect<LegendCodeBlockThemeNullable>(
      _$LegendCodeBlockOverrideSelectCopyGlyphColor,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendCodeBlockThemeListenables {
  /// Change stream of the resolved [LegendCodeBlockTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendCodeBlockSelectBackground);

  /// Change stream of the resolved [LegendCodeBlockTheme.textStyle].
  static ValueListenable<TextStyle> textStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendCodeBlockSelectTextStyle);

  /// Change stream of the resolved [LegendCodeBlockTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendCodeBlockSelectBorderRadius);

  /// Change stream of the resolved [LegendCodeBlockTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendCodeBlockSelectPadding);

  /// Change stream of the resolved [LegendCodeBlockTheme.languageLabelStyle].
  static ValueListenable<TextStyle> languageLabelStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendCodeBlockSelectLanguageLabelStyle,
  );

  /// Change stream of the resolved [LegendCodeBlockTheme.copyGlyphColor].
  static ValueListenable<Color> copyGlyphColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendCodeBlockSelectCopyGlyphColor);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendCodeBlockThemeResolve on LegendCodeBlock {
  /// [LegendCodeBlockTheme.of] with this widget's constructor params as
  /// level 1.
  LegendCodeBlockTheme _theme(BuildContext context) => LegendCodeBlockTheme.of(
    context,
    LegendCodeBlockThemeNullable(
      background: background,
      textStyle: textStyle,
      borderRadius: borderRadius,
      padding: padding,
      languageLabelStyle: languageLabelStyle,
      copyGlyphColor: copyGlyphColor,
    ),
  );

  /// Resolves ONLY [LegendCodeBlock.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectBackground);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendCodeBlockSelectBackground(data);
  }

  /// Resolves ONLY [LegendCodeBlock.textStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._textStyle(context)`).
  TextStyle _textStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectTextStyle);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectTextStyle,
    );
    return textStyle ??
        override?.textStyle ??
        _$LegendCodeBlockSelectTextStyle(data);
  }

  /// Resolves ONLY [LegendCodeBlock.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectBorderRadius);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendCodeBlockSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendCodeBlock.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectPadding);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendCodeBlockSelectPadding(data);
  }

  /// Resolves ONLY [LegendCodeBlock.languageLabelStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._languageLabelStyle(context)`).
  TextStyle _languageLabelStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectLanguageLabelStyle);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectLanguageLabelStyle,
    );
    return languageLabelStyle ??
        override?.languageLabelStyle ??
        _$LegendCodeBlockSelectLanguageLabelStyle(data);
  }

  /// Resolves ONLY [LegendCodeBlock.copyGlyphColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._copyGlyphColor(context)`).
  Color _copyGlyphColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendCodeBlockThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendCodeBlockAspectCopyGlyphColor);
    LegendThemeOverride.depend<LegendCodeBlockThemeNullable>(
      context,
      _$LegendCodeBlockOverrideAspectCopyGlyphColor,
    );
    return copyGlyphColor ??
        override?.copyGlyphColor ??
        _$LegendCodeBlockSelectCopyGlyphColor(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendCodeBlockState]
/// is this file's `State<LegendCodeBlock>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendCodeBlockThemeState])
/// wins over this extension.
extension _$LegendCodeBlockThemeOn_LegendCodeBlockState
    on _LegendCodeBlockState {
  LegendCodeBlockTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendCodeBlock>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendCodeBlockThemeState on State<LegendCodeBlock> {
  LegendCodeBlockTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendCodeBlock extends
/// _$LegendCodeBlockBase` receives the resolved
/// [LegendCodeBlockTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendCodeBlockBase
    extends LegendStatelessWidget<LegendCodeBlockTheme> {
  const _$LegendCodeBlockBase({super.key});

  Color? get background;
  TextStyle? get textStyle;
  BorderRadius? get borderRadius;
  EdgeInsetsGeometry? get padding;
  TextStyle? get languageLabelStyle;
  Color? get copyGlyphColor;

  @override
  LegendCodeBlockTheme resolveThemeOf(BuildContext context) =>
      LegendCodeBlockTheme.of(
        context,
        LegendCodeBlockThemeNullable(
          background: background,
          textStyle: textStyle,
          borderRadius: borderRadius,
          padding: padding,
          languageLabelStyle: languageLabelStyle,
          copyGlyphColor: copyGlyphColor,
        ),
      );
}
