// GENERATED by legend_gen v0.1.0-dev.0 — do not edit.
// Source: primary_legend_button.dart
// ignore_for_file: type=lint, unused_import

import 'package:flutter/widgets.dart';
import 'package:legend_ui/legend_ui.dart';
import 'package:legend_ui/src/annotations/annotations.dart';
import 'package:legend_ui/src/primitives/legend_button_core.dart';
import 'primary_legend_button.dart';

/// Resolved theme for [PrimaryLegendButton].
class PrimaryLegendButtonTheme {
  const PrimaryLegendButtonTheme({
    required this.background,
    required this.foreground,
    required this.padding,
    required this.borderRadius,
    required this.textStyle,
    required this.shadows,
  });

  /// Token-derived defaults (level 5).
  factory PrimaryLegendButtonTheme.defaults(LegendTokens t) =>
      PrimaryLegendButtonTheme(
        background: t.colors.primary,
        foreground: t.colors.onPrimary,
        padding: EdgeInsets.symmetric(
          horizontal: t.sizes.md,
          vertical: t.sizes.sm,
        ),
        borderRadius: t.sizes.borderRadiusMd,
        textStyle: t.typography.b2,
        shadows: t.shadows.none,
      );

  final Color background;
  final Color foreground;
  final EdgeInsetsGeometry padding;
  final BorderRadius borderRadius;
  final TextStyle textStyle;
  final List<BoxShadow> shadows;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static PrimaryLegendButtonTheme of(
    BuildContext context, [
    PrimaryLegendButtonThemeNullable? local,
  ]) {
    final data = LegendTheme.of(context);
    return PrimaryLegendButtonTheme.defaults(data.tokens)
        .merge(data.component<PrimaryLegendButtonThemeNullable>())
        .merge(
          LegendThemeOverride.maybeOf<PrimaryLegendButtonThemeNullable>(
            context,
          ),
        )
        .merge(local);
  }

  PrimaryLegendButtonTheme merge(PrimaryLegendButtonThemeNullable? other) {
    if (other == null) return this;
    return PrimaryLegendButtonTheme(
      background: other.background ?? background,
      foreground: other.foreground ?? foreground,
      padding: other.padding ?? padding,
      borderRadius: other.borderRadius ?? borderRadius,
      textStyle: other.textStyle ?? textStyle,
      shadows: other.shadows ?? shadows,
    );
  }

  PrimaryLegendButtonTheme copyWith({
    Color? background,
    Color? foreground,
    EdgeInsetsGeometry? padding,
    BorderRadius? borderRadius,
    TextStyle? textStyle,
    List<BoxShadow>? shadows,
  }) => PrimaryLegendButtonTheme(
    background: background ?? this.background,
    foreground: foreground ?? this.foreground,
    padding: padding ?? this.padding,
    borderRadius: borderRadius ?? this.borderRadius,
    textStyle: textStyle ?? this.textStyle,
    shadows: shadows ?? this.shadows,
  );

  static PrimaryLegendButtonTheme lerp(
    PrimaryLegendButtonTheme a,
    PrimaryLegendButtonTheme b,
    double t,
  ) => PrimaryLegendButtonTheme(
    background: Color.lerp(a.background, b.background, t)!,
    foreground: Color.lerp(a.foreground, b.foreground, t)!,
    padding: t < 0.5 ? a.padding : b.padding,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
    shadows: t < 0.5 ? a.shadows : b.shadows,
  );
}

/// Sparse overrides for [PrimaryLegendButtonTheme]; null means "inherit".
class PrimaryLegendButtonThemeNullable {
  const PrimaryLegendButtonThemeNullable({
    this.background,
    this.foreground,
    this.padding,
    this.borderRadius,
    this.textStyle,
    this.shadows,
  });

  final Color? background;
  final Color? foreground;
  final EdgeInsetsGeometry? padding;
  final BorderRadius? borderRadius;
  final TextStyle? textStyle;
  final List<BoxShadow>? shadows;

  PrimaryLegendButtonThemeNullable merge(
    PrimaryLegendButtonThemeNullable? other,
  ) {
    if (other == null) return this;
    return PrimaryLegendButtonThemeNullable(
      background: other.background ?? background,
      foreground: other.foreground ?? foreground,
      padding: other.padding ?? padding,
      borderRadius: other.borderRadius ?? borderRadius,
      textStyle: other.textStyle ?? textStyle,
      shadows: other.shadows ?? shadows,
    );
  }
}

/// Overrides [PrimaryLegendButtonTheme] for a subtree (level 2).
class PrimaryLegendButtonThemeOverride extends StatelessWidget {
  const PrimaryLegendButtonThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final PrimaryLegendButtonThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<PrimaryLegendButtonThemeNullable>(
        data: data,
        child: child,
      );
}
