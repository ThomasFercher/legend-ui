// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_expandable.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_expandable.dart';

/// Resolved theme for [LegendExpandable].
class LegendExpandableTheme {
  const LegendExpandableTheme({
    required this.headerPadding,
    required this.backgroundColor,
    required this.borderRadius,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendExpandableTheme.defaults(LegendTokens t) =>
      LegendExpandableTheme(
        headerPadding: LegendExpandable._headerPadding(t),
        backgroundColor: LegendExpandable._backgroundColor(t),
        borderRadius: LegendExpandable._borderRadius(t),
      );

  final EdgeInsetsGeometry headerPadding;
  final Color backgroundColor;
  final BorderRadius borderRadius;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendExpandable] first, [LegendExpandableThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  static LegendExpandableTheme of(
    BuildContext context, [
    LegendExpandableThemeNullable? local,
  ]) {
    final data = LegendTheme.of(context);
    final resolved = LegendExpandableTheme.defaults(data.tokens)
        .merge(
          data.componentOf<LegendExpandableThemeNullable>(LegendExpandable),
        )
        .merge(
          LegendThemeOverride.maybeOf<LegendExpandableThemeNullable>(context),
        )
        .merge(local);
    return resolved;
  }

  LegendExpandableTheme merge(LegendExpandableThemeNullable? other) {
    if (other == null) return this;
    return LegendExpandableTheme(
      headerPadding: other.headerPadding ?? headerPadding,
      backgroundColor: other.backgroundColor ?? backgroundColor,
      borderRadius: other.borderRadius ?? borderRadius,
    );
  }

  LegendExpandableTheme copyWith({
    EdgeInsetsGeometry? headerPadding,
    Color? backgroundColor,
    BorderRadius? borderRadius,
  }) => LegendExpandableTheme(
    headerPadding: headerPadding ?? this.headerPadding,
    backgroundColor: backgroundColor ?? this.backgroundColor,
    borderRadius: borderRadius ?? this.borderRadius,
  );

  static LegendExpandableTheme lerp(
    LegendExpandableTheme a,
    LegendExpandableTheme b,
    double t,
  ) => LegendExpandableTheme(
    headerPadding: t < 0.5 ? a.headerPadding : b.headerPadding,
    backgroundColor: t < 0.5 ? a.backgroundColor : b.backgroundColor,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
  );
}

/// Sparse overrides for [LegendExpandableTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendExpandableThemeNullable {
  const LegendExpandableThemeNullable({
    this.headerPadding,
    this.backgroundColor,
    this.borderRadius,
  });

  final EdgeInsetsGeometry? headerPadding;
  final Color? backgroundColor;
  final BorderRadius? borderRadius;

  LegendExpandableThemeNullable merge(LegendExpandableThemeNullable? other) {
    if (other == null) return this;
    return LegendExpandableThemeNullable(
      headerPadding: other.headerPadding ?? headerPadding,
      backgroundColor: other.backgroundColor ?? backgroundColor,
      borderRadius: other.borderRadius ?? borderRadius,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendExpandableThemeNullable &&
          other.headerPadding == headerPadding &&
          other.backgroundColor == backgroundColor &&
          other.borderRadius == borderRadius;

  @override
  int get hashCode =>
      Object.hashAll([headerPadding, backgroundColor, borderRadius]);
}

/// Overrides [LegendExpandableTheme] for a subtree (level 2).
class LegendExpandableThemeOverride extends StatelessWidget {
  const LegendExpandableThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendExpandableThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendExpandableThemeNullable>(
        data: data,
        child: child,
      );
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendExpandableThemeResolve on LegendExpandable {
  /// [LegendExpandableTheme.of] with this widget's constructor params as
  /// level 1.
  LegendExpandableTheme _theme(BuildContext context) =>
      LegendExpandableTheme.of(
        context,
        LegendExpandableThemeNullable(
          headerPadding: headerPadding,
          backgroundColor: backgroundColor,
          borderRadius: borderRadius,
        ),
      );
}
