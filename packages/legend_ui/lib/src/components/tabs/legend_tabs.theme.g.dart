// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_tabs.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_tabs.dart';

/// Resolved theme for [LegendTabs].
class LegendTabsTheme {
  const LegendTabsTheme({
    required this.background,
    required this.indicator,
    required this.indicatorThickness,
    required this.selectedColor,
    required this.unselectedColor,
    required this.padding,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendTabsTheme.defaults(LegendTokens t) => LegendTabsTheme(
    background: _background(t),
    indicator: ColorRef.primary(t),
    indicatorThickness: 2,
    selectedColor: ColorRef.primary(t),
    unselectedColor: ColorRef.foreground2(t),
    padding: _padding(t),
  );

  final InteractiveColors background;
  final Color indicator;
  final double indicatorThickness;
  final Color selectedColor;
  final Color unselectedColor;
  final EdgeInsetsGeometry padding;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendTabs] first, [LegendTabsThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendTabsTheme of(
    BuildContext context, [
    LegendTabsThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectBackground);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendTabsAspectIndicator);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectIndicator,
    );
    LegendTheme.depend(context, _$LegendTabsAspectIndicatorThickness);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectIndicatorThickness,
    );
    LegendTheme.depend(context, _$LegendTabsAspectSelectedColor);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectSelectedColor,
    );
    LegendTheme.depend(context, _$LegendTabsAspectUnselectedColor);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectUnselectedColor,
    );
    LegendTheme.depend(context, _$LegendTabsAspectPadding);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectPadding,
    );
    return LegendTabsTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendTabsThemeNullable>(LegendTabs))
        .merge(override)
        .merge(local);
  }

  LegendTabsTheme merge(LegendTabsThemeNullable? other) {
    if (other == null) return this;
    return LegendTabsTheme(
      background: background.merge(other.background),
      indicator: other.indicator ?? indicator,
      indicatorThickness: other.indicatorThickness ?? indicatorThickness,
      selectedColor: other.selectedColor ?? selectedColor,
      unselectedColor: other.unselectedColor ?? unselectedColor,
      padding: other.padding ?? padding,
    );
  }

  LegendTabsTheme copyWith({
    InteractiveColors? background,
    Color? indicator,
    double? indicatorThickness,
    Color? selectedColor,
    Color? unselectedColor,
    EdgeInsetsGeometry? padding,
  }) => LegendTabsTheme(
    background: background ?? this.background,
    indicator: indicator ?? this.indicator,
    indicatorThickness: indicatorThickness ?? this.indicatorThickness,
    selectedColor: selectedColor ?? this.selectedColor,
    unselectedColor: unselectedColor ?? this.unselectedColor,
    padding: padding ?? this.padding,
  );

  static LegendTabsTheme lerp(LegendTabsTheme a, LegendTabsTheme b, double t) =>
      LegendTabsTheme(
        background: t < 0.5 ? a.background : b.background,
        indicator: Color.lerp(a.indicator, b.indicator, t)!,
        indicatorThickness: t < 0.5
            ? a.indicatorThickness
            : b.indicatorThickness,
        selectedColor: Color.lerp(a.selectedColor, b.selectedColor, t)!,
        unselectedColor: Color.lerp(a.unselectedColor, b.unselectedColor, t)!,
        padding: t < 0.5 ? a.padding : b.padding,
      );
}

/// Sparse overrides for [LegendTabsTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendTabsThemeNullable {
  const LegendTabsThemeNullable({
    this.background,
    this.indicator,
    this.indicatorThickness,
    this.selectedColor,
    this.unselectedColor,
    this.padding,
  });

  final InteractiveColors? background;
  final Color? indicator;
  final double? indicatorThickness;
  final Color? selectedColor;
  final Color? unselectedColor;
  final EdgeInsetsGeometry? padding;

  LegendTabsThemeNullable merge(LegendTabsThemeNullable? other) {
    if (other == null) return this;
    return LegendTabsThemeNullable(
      background: background?.merge(other.background) ?? other.background,
      indicator: other.indicator ?? indicator,
      indicatorThickness: other.indicatorThickness ?? indicatorThickness,
      selectedColor: other.selectedColor ?? selectedColor,
      unselectedColor: other.unselectedColor ?? unselectedColor,
      padding: other.padding ?? padding,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendTabsThemeNullable &&
          other.background == background &&
          other.indicator == indicator &&
          other.indicatorThickness == indicatorThickness &&
          other.selectedColor == selectedColor &&
          other.unselectedColor == unselectedColor &&
          other.padding == padding;

  @override
  int get hashCode => Object.hashAll([
    background,
    indicator,
    indicatorThickness,
    selectedColor,
    unselectedColor,
    padding,
  ]);
}

/// Overrides [LegendTabsTheme] for a subtree (level 2).
class LegendTabsThemeOverride extends StatelessWidget {
  const LegendTabsThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendTabsThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendTabsThemeNullable>(data: data, child: child);
}

/// Resolves [LegendTabs.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendTabsSelectBackground(LegendThemeData data) =>
    _background(
      data.tokens,
    ).merge(data.componentOf<LegendTabsThemeNullable>(LegendTabs)?.background);
const _$LegendTabsAspectBackground = LegendThemeAspect(
  _$LegendTabsSelectBackground,
);
Object? _$LegendTabsOverrideSelectBackground(LegendTabsThemeNullable data) =>
    data.background;
const _$LegendTabsOverrideAspectBackground =
    LegendOverrideAspect<LegendTabsThemeNullable>(
      _$LegendTabsOverrideSelectBackground,
    );

/// Resolves [LegendTabs.indicator] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendTabsSelectIndicator(LegendThemeData data) =>
    data.componentOf<LegendTabsThemeNullable>(LegendTabs)?.indicator ??
    ColorRef.primary(data.tokens);
const _$LegendTabsAspectIndicator = LegendThemeAspect(
  _$LegendTabsSelectIndicator,
);
Object? _$LegendTabsOverrideSelectIndicator(LegendTabsThemeNullable data) =>
    data.indicator;
const _$LegendTabsOverrideAspectIndicator =
    LegendOverrideAspect<LegendTabsThemeNullable>(
      _$LegendTabsOverrideSelectIndicator,
    );

/// Resolves [LegendTabs.indicatorThickness] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendTabsSelectIndicatorThickness(LegendThemeData data) =>
    data.componentOf<LegendTabsThemeNullable>(LegendTabs)?.indicatorThickness ??
    2;
const _$LegendTabsAspectIndicatorThickness = LegendThemeAspect(
  _$LegendTabsSelectIndicatorThickness,
);
Object? _$LegendTabsOverrideSelectIndicatorThickness(
  LegendTabsThemeNullable data,
) => data.indicatorThickness;
const _$LegendTabsOverrideAspectIndicatorThickness =
    LegendOverrideAspect<LegendTabsThemeNullable>(
      _$LegendTabsOverrideSelectIndicatorThickness,
    );

/// Resolves [LegendTabs.selectedColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendTabsSelectSelectedColor(LegendThemeData data) =>
    data.componentOf<LegendTabsThemeNullable>(LegendTabs)?.selectedColor ??
    ColorRef.primary(data.tokens);
const _$LegendTabsAspectSelectedColor = LegendThemeAspect(
  _$LegendTabsSelectSelectedColor,
);
Object? _$LegendTabsOverrideSelectSelectedColor(LegendTabsThemeNullable data) =>
    data.selectedColor;
const _$LegendTabsOverrideAspectSelectedColor =
    LegendOverrideAspect<LegendTabsThemeNullable>(
      _$LegendTabsOverrideSelectSelectedColor,
    );

/// Resolves [LegendTabs.unselectedColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendTabsSelectUnselectedColor(LegendThemeData data) =>
    data.componentOf<LegendTabsThemeNullable>(LegendTabs)?.unselectedColor ??
    ColorRef.foreground2(data.tokens);
const _$LegendTabsAspectUnselectedColor = LegendThemeAspect(
  _$LegendTabsSelectUnselectedColor,
);
Object? _$LegendTabsOverrideSelectUnselectedColor(
  LegendTabsThemeNullable data,
) => data.unselectedColor;
const _$LegendTabsOverrideAspectUnselectedColor =
    LegendOverrideAspect<LegendTabsThemeNullable>(
      _$LegendTabsOverrideSelectUnselectedColor,
    );

/// Resolves [LegendTabs.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendTabsSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendTabsThemeNullable>(LegendTabs)?.padding ??
    _padding(data.tokens);
const _$LegendTabsAspectPadding = LegendThemeAspect(_$LegendTabsSelectPadding);
Object? _$LegendTabsOverrideSelectPadding(LegendTabsThemeNullable data) =>
    data.padding;
const _$LegendTabsOverrideAspectPadding =
    LegendOverrideAspect<LegendTabsThemeNullable>(
      _$LegendTabsOverrideSelectPadding,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendTabsThemeListenables {
  /// Change stream of the resolved [LegendTabsTheme.background].
  static ValueListenable<InteractiveColors> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTabsSelectBackground);

  /// Change stream of the resolved [LegendTabsTheme.indicator].
  static ValueListenable<Color> indicator(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTabsSelectIndicator);

  /// Change stream of the resolved [LegendTabsTheme.indicatorThickness].
  static ValueListenable<double> indicatorThickness(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTabsSelectIndicatorThickness);

  /// Change stream of the resolved [LegendTabsTheme.selectedColor].
  static ValueListenable<Color> selectedColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTabsSelectSelectedColor);

  /// Change stream of the resolved [LegendTabsTheme.unselectedColor].
  static ValueListenable<Color> unselectedColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTabsSelectUnselectedColor);

  /// Change stream of the resolved [LegendTabsTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTabsSelectPadding);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendTabsThemeResolve on LegendTabs {
  /// [LegendTabsTheme.of] with this widget's constructor params as
  /// level 1.
  LegendTabsTheme _theme(BuildContext context) => LegendTabsTheme.of(
    context,
    LegendTabsThemeNullable(
      background: background,
      indicator: indicator,
      indicatorThickness: indicatorThickness,
      selectedColor: selectedColor,
      unselectedColor: unselectedColor,
      padding: padding,
    ),
  );

  /// Resolves ONLY [LegendTabs.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  InteractiveColors _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectBackground);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectBackground,
    );
    return _$LegendTabsSelectBackground(
      data,
    ).merge(override?.background).merge(background);
  }

  /// Resolves ONLY [LegendTabs.indicator] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._indicator(context)`).
  Color _indicator(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectIndicator);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectIndicator,
    );
    return indicator ??
        override?.indicator ??
        _$LegendTabsSelectIndicator(data);
  }

  /// Resolves ONLY [LegendTabs.indicatorThickness] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._indicatorThickness(context)`).
  double _indicatorThickness(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectIndicatorThickness);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectIndicatorThickness,
    );
    return indicatorThickness ??
        override?.indicatorThickness ??
        _$LegendTabsSelectIndicatorThickness(data);
  }

  /// Resolves ONLY [LegendTabs.selectedColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedColor(context)`).
  Color _selectedColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectSelectedColor);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectSelectedColor,
    );
    return selectedColor ??
        override?.selectedColor ??
        _$LegendTabsSelectSelectedColor(data);
  }

  /// Resolves ONLY [LegendTabs.unselectedColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._unselectedColor(context)`).
  Color _unselectedColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectUnselectedColor);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectUnselectedColor,
    );
    return unselectedColor ??
        override?.unselectedColor ??
        _$LegendTabsSelectUnselectedColor(data);
  }

  /// Resolves ONLY [LegendTabs.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTabsThemeNullable>(context);
    LegendTheme.depend(context, _$LegendTabsAspectPadding);
    LegendThemeOverride.depend<LegendTabsThemeNullable>(
      context,
      _$LegendTabsOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendTabsSelectPadding(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendTabsState]
/// is this file's `State<LegendTabs>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendTabsThemeState])
/// wins over this extension.
extension _$LegendTabsThemeOn_LegendTabsState on _LegendTabsState {
  LegendTabsTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendTabs>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendTabsThemeState on State<LegendTabs> {
  LegendTabsTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendTabs extends
/// _$LegendTabsBase` receives the resolved
/// [LegendTabsTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendTabsBase extends LegendStatelessWidget<LegendTabsTheme> {
  const _$LegendTabsBase({super.key});

  InteractiveColors? get background;
  Color? get indicator;
  double? get indicatorThickness;
  Color? get selectedColor;
  Color? get unselectedColor;
  EdgeInsetsGeometry? get padding;

  @override
  LegendTabsTheme resolveThemeOf(BuildContext context) => LegendTabsTheme.of(
    context,
    LegendTabsThemeNullable(
      background: background,
      indicator: indicator,
      indicatorThickness: indicatorThickness,
      selectedColor: selectedColor,
      unselectedColor: unselectedColor,
      padding: padding,
    ),
  );
}
