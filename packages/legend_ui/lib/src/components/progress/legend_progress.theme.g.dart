// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_progress.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_progress.dart';

/// Resolved theme for [LegendProgress].
class LegendProgressTheme {
  const LegendProgressTheme({
    required this.track,
    required this.fill,
    required this.thickness,
    required this.borderRadius,
    required this.size,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendProgressTheme.defaults(LegendTokens t) => LegendProgressTheme(
    track: ColorRef.background3(t),
    fill: ColorRef.primary(t),
    thickness: 6,
    borderRadius: _borderRadius(t),
    size: 40,
  );

  final Color track;
  final Color fill;
  final double thickness;
  final BorderRadius borderRadius;
  final double size;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendProgress] first, [LegendProgressThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendProgressTheme of(
    BuildContext context, [
    LegendProgressThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendProgressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendProgressAspectTrack);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectTrack,
    );
    LegendTheme.depend(context, _$LegendProgressAspectFill);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectFill,
    );
    LegendTheme.depend(context, _$LegendProgressAspectThickness);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectThickness,
    );
    LegendTheme.depend(context, _$LegendProgressAspectBorderRadius);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendProgressAspectSize);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectSize,
    );
    return LegendProgressTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendProgressThemeNullable>(LegendProgress))
        .merge(override)
        .merge(local);
  }

  LegendProgressTheme merge(LegendProgressThemeNullable? other) {
    if (other == null) return this;
    return LegendProgressTheme(
      track: other.track ?? track,
      fill: other.fill ?? fill,
      thickness: other.thickness ?? thickness,
      borderRadius: other.borderRadius ?? borderRadius,
      size: other.size ?? size,
    );
  }

  LegendProgressTheme copyWith({
    Color? track,
    Color? fill,
    double? thickness,
    BorderRadius? borderRadius,
    double? size,
  }) => LegendProgressTheme(
    track: track ?? this.track,
    fill: fill ?? this.fill,
    thickness: thickness ?? this.thickness,
    borderRadius: borderRadius ?? this.borderRadius,
    size: size ?? this.size,
  );

  static LegendProgressTheme lerp(
    LegendProgressTheme a,
    LegendProgressTheme b,
    double t,
  ) => LegendProgressTheme(
    track: t < 0.5 ? a.track : b.track,
    fill: t < 0.5 ? a.fill : b.fill,
    thickness: t < 0.5 ? a.thickness : b.thickness,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    size: t < 0.5 ? a.size : b.size,
  );
}

/// Sparse overrides for [LegendProgressTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendProgressThemeNullable {
  const LegendProgressThemeNullable({
    this.track,
    this.fill,
    this.thickness,
    this.borderRadius,
    this.size,
  });

  final Color? track;
  final Color? fill;
  final double? thickness;
  final BorderRadius? borderRadius;
  final double? size;

  LegendProgressThemeNullable merge(LegendProgressThemeNullable? other) {
    if (other == null) return this;
    return LegendProgressThemeNullable(
      track: other.track ?? track,
      fill: other.fill ?? fill,
      thickness: other.thickness ?? thickness,
      borderRadius: other.borderRadius ?? borderRadius,
      size: other.size ?? size,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendProgressThemeNullable &&
          other.track == track &&
          other.fill == fill &&
          other.thickness == thickness &&
          other.borderRadius == borderRadius &&
          other.size == size;

  @override
  int get hashCode =>
      Object.hashAll([track, fill, thickness, borderRadius, size]);
}

/// Overrides [LegendProgressTheme] for a subtree (level 2).
class LegendProgressThemeOverride extends StatelessWidget {
  const LegendProgressThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendProgressThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendProgressThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendProgress.track] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendProgressSelectTrack(LegendThemeData data) =>
    data.componentOf<LegendProgressThemeNullable>(LegendProgress)?.track ??
    ColorRef.background3(data.tokens);
const _$LegendProgressAspectTrack = LegendThemeAspect(
  _$LegendProgressSelectTrack,
);
Object? _$LegendProgressOverrideSelectTrack(LegendProgressThemeNullable data) =>
    data.track;
const _$LegendProgressOverrideAspectTrack =
    LegendOverrideAspect<LegendProgressThemeNullable>(
      _$LegendProgressOverrideSelectTrack,
    );

/// Resolves [LegendProgress.fill] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendProgressSelectFill(LegendThemeData data) =>
    data.componentOf<LegendProgressThemeNullable>(LegendProgress)?.fill ??
    ColorRef.primary(data.tokens);
const _$LegendProgressAspectFill = LegendThemeAspect(
  _$LegendProgressSelectFill,
);
Object? _$LegendProgressOverrideSelectFill(LegendProgressThemeNullable data) =>
    data.fill;
const _$LegendProgressOverrideAspectFill =
    LegendOverrideAspect<LegendProgressThemeNullable>(
      _$LegendProgressOverrideSelectFill,
    );

/// Resolves [LegendProgress.thickness] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendProgressSelectThickness(LegendThemeData data) =>
    data.componentOf<LegendProgressThemeNullable>(LegendProgress)?.thickness ??
    6;
const _$LegendProgressAspectThickness = LegendThemeAspect(
  _$LegendProgressSelectThickness,
);
Object? _$LegendProgressOverrideSelectThickness(
  LegendProgressThemeNullable data,
) => data.thickness;
const _$LegendProgressOverrideAspectThickness =
    LegendOverrideAspect<LegendProgressThemeNullable>(
      _$LegendProgressOverrideSelectThickness,
    );

/// Resolves [LegendProgress.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendProgressSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendProgressThemeNullable>(LegendProgress)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendProgressAspectBorderRadius = LegendThemeAspect(
  _$LegendProgressSelectBorderRadius,
);
Object? _$LegendProgressOverrideSelectBorderRadius(
  LegendProgressThemeNullable data,
) => data.borderRadius;
const _$LegendProgressOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendProgressThemeNullable>(
      _$LegendProgressOverrideSelectBorderRadius,
    );

/// Resolves [LegendProgress.size] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendProgressSelectSize(LegendThemeData data) =>
    data.componentOf<LegendProgressThemeNullable>(LegendProgress)?.size ?? 40;
const _$LegendProgressAspectSize = LegendThemeAspect(
  _$LegendProgressSelectSize,
);
Object? _$LegendProgressOverrideSelectSize(LegendProgressThemeNullable data) =>
    data.size;
const _$LegendProgressOverrideAspectSize =
    LegendOverrideAspect<LegendProgressThemeNullable>(
      _$LegendProgressOverrideSelectSize,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendProgressThemeListenables {
  /// Change stream of the resolved [LegendProgressTheme.track].
  static ValueListenable<Color> track(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendProgressSelectTrack);

  /// Change stream of the resolved [LegendProgressTheme.fill].
  static ValueListenable<Color> fill(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendProgressSelectFill);

  /// Change stream of the resolved [LegendProgressTheme.thickness].
  static ValueListenable<double> thickness(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendProgressSelectThickness);

  /// Change stream of the resolved [LegendProgressTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendProgressSelectBorderRadius);

  /// Change stream of the resolved [LegendProgressTheme.size].
  static ValueListenable<double> size(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendProgressSelectSize);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendProgressThemeResolve on LegendProgress {
  /// [LegendProgressTheme.of] with this widget's constructor params as
  /// level 1.
  LegendProgressTheme _theme(BuildContext context) => LegendProgressTheme.of(
    context,
    LegendProgressThemeNullable(
      track: track,
      fill: fill,
      thickness: thickness,
      borderRadius: borderRadius,
      size: size,
    ),
  );

  /// Resolves ONLY [LegendProgress.track] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._track(context)`).
  Color _track(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendProgressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendProgressAspectTrack);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectTrack,
    );
    return track ?? override?.track ?? _$LegendProgressSelectTrack(data);
  }

  /// Resolves ONLY [LegendProgress.fill] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._fill(context)`).
  Color _fill(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendProgressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendProgressAspectFill);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectFill,
    );
    return fill ?? override?.fill ?? _$LegendProgressSelectFill(data);
  }

  /// Resolves ONLY [LegendProgress.thickness] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._thickness(context)`).
  double _thickness(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendProgressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendProgressAspectThickness);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectThickness,
    );
    return thickness ??
        override?.thickness ??
        _$LegendProgressSelectThickness(data);
  }

  /// Resolves ONLY [LegendProgress.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendProgressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendProgressAspectBorderRadius);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendProgressSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendProgress.size] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._size(context)`).
  double _size(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendProgressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendProgressAspectSize);
    LegendThemeOverride.depend<LegendProgressThemeNullable>(
      context,
      _$LegendProgressOverrideAspectSize,
    );
    return size ?? override?.size ?? _$LegendProgressSelectSize(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendProgressState]
/// is this file's `State<LegendProgress>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendProgressThemeState])
/// wins over this extension.
extension _$LegendProgressThemeOn_LegendProgressState on _LegendProgressState {
  LegendProgressTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendProgress>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendProgressThemeState on State<LegendProgress> {
  LegendProgressTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendProgress extends
/// _$LegendProgressBase` receives the resolved
/// [LegendProgressTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendProgressBase
    extends LegendStatelessWidget<LegendProgressTheme> {
  const _$LegendProgressBase({super.key});

  Color? get track;
  Color? get fill;
  double? get thickness;
  BorderRadius? get borderRadius;
  double? get size;

  @override
  LegendProgressTheme resolveThemeOf(BuildContext context) =>
      LegendProgressTheme.of(
        context,
        LegendProgressThemeNullable(
          track: track,
          fill: fill,
          thickness: thickness,
          borderRadius: borderRadius,
          size: size,
        ),
      );
}
