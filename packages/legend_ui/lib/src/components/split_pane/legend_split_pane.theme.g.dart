// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_split_pane.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_split_pane.dart';

/// Resolved theme for [LegendSplitPane].
class LegendSplitPaneTheme {
  const LegendSplitPaneTheme({
    required this.divider,
    required this.dividerThickness,
    required this.handle,
    required this.hitTarget,
    required this.collapseBreakpoint,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendSplitPaneTheme.defaults(LegendTokens t) => LegendSplitPaneTheme(
    divider: ColorRef.background3(t),
    dividerThickness: SizeRef.borderWidth(t),
    handle: _handle(t),
    hitTarget: SizeRef.sm(t),
    collapseBreakpoint: 0,
  );

  final Color divider;
  final double dividerThickness;
  final InteractiveColors handle;
  final double hitTarget;
  final double collapseBreakpoint;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendSplitPane] first, [LegendSplitPaneThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendSplitPaneTheme of(
    BuildContext context, [
    LegendSplitPaneThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSplitPaneThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectDivider);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectDivider,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectDividerThickness);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectDividerThickness,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectHandle);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectHandle,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectHitTarget);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectHitTarget,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectCollapseBreakpoint);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectCollapseBreakpoint,
    );
    return LegendSplitPaneTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendSplitPaneThemeNullable>(LegendSplitPane))
        .merge(override)
        .merge(local);
  }

  LegendSplitPaneTheme merge(LegendSplitPaneThemeNullable? other) {
    if (other == null) return this;
    return LegendSplitPaneTheme(
      divider: other.divider ?? divider,
      dividerThickness: other.dividerThickness ?? dividerThickness,
      handle: handle.merge(other.handle),
      hitTarget: other.hitTarget ?? hitTarget,
      collapseBreakpoint: other.collapseBreakpoint ?? collapseBreakpoint,
    );
  }

  LegendSplitPaneTheme copyWith({
    Color? divider,
    double? dividerThickness,
    InteractiveColors? handle,
    double? hitTarget,
    double? collapseBreakpoint,
  }) => LegendSplitPaneTheme(
    divider: divider ?? this.divider,
    dividerThickness: dividerThickness ?? this.dividerThickness,
    handle: handle ?? this.handle,
    hitTarget: hitTarget ?? this.hitTarget,
    collapseBreakpoint: collapseBreakpoint ?? this.collapseBreakpoint,
  );

  static LegendSplitPaneTheme lerp(
    LegendSplitPaneTheme a,
    LegendSplitPaneTheme b,
    double t,
  ) => LegendSplitPaneTheme(
    divider: Color.lerp(a.divider, b.divider, t)!,
    dividerThickness: t < 0.5 ? a.dividerThickness : b.dividerThickness,
    handle: InteractiveColors.lerp(a.handle, b.handle, t)!,
    hitTarget: t < 0.5 ? a.hitTarget : b.hitTarget,
    collapseBreakpoint: t < 0.5 ? a.collapseBreakpoint : b.collapseBreakpoint,
  );
}

/// Sparse overrides for [LegendSplitPaneTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendSplitPaneThemeNullable {
  const LegendSplitPaneThemeNullable({
    this.divider,
    this.dividerThickness,
    this.handle,
    this.hitTarget,
    this.collapseBreakpoint,
  });

  final Color? divider;
  final double? dividerThickness;
  final InteractiveColors? handle;
  final double? hitTarget;
  final double? collapseBreakpoint;

  LegendSplitPaneThemeNullable merge(LegendSplitPaneThemeNullable? other) {
    if (other == null) return this;
    return LegendSplitPaneThemeNullable(
      divider: other.divider ?? divider,
      dividerThickness: other.dividerThickness ?? dividerThickness,
      handle: handle?.merge(other.handle) ?? other.handle,
      hitTarget: other.hitTarget ?? hitTarget,
      collapseBreakpoint: other.collapseBreakpoint ?? collapseBreakpoint,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendSplitPaneThemeNullable &&
          other.divider == divider &&
          other.dividerThickness == dividerThickness &&
          other.handle == handle &&
          other.hitTarget == hitTarget &&
          other.collapseBreakpoint == collapseBreakpoint;

  @override
  int get hashCode => Object.hashAll([
    divider,
    dividerThickness,
    handle,
    hitTarget,
    collapseBreakpoint,
  ]);
}

/// Overrides [LegendSplitPaneTheme] for a subtree (level 2).
class LegendSplitPaneThemeOverride extends StatelessWidget {
  const LegendSplitPaneThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendSplitPaneThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendSplitPaneThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendSplitPane.divider] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendSplitPaneSelectDivider(LegendThemeData data) =>
    data.componentOf<LegendSplitPaneThemeNullable>(LegendSplitPane)?.divider ??
    ColorRef.background3(data.tokens);
const _$LegendSplitPaneAspectDivider = LegendThemeAspect(
  _$LegendSplitPaneSelectDivider,
);
Object? _$LegendSplitPaneOverrideSelectDivider(
  LegendSplitPaneThemeNullable data,
) => data.divider;
const _$LegendSplitPaneOverrideAspectDivider =
    LegendOverrideAspect<LegendSplitPaneThemeNullable>(
      _$LegendSplitPaneOverrideSelectDivider,
    );

/// Resolves [LegendSplitPane.dividerThickness] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendSplitPaneSelectDividerThickness(LegendThemeData data) =>
    data
        .componentOf<LegendSplitPaneThemeNullable>(LegendSplitPane)
        ?.dividerThickness ??
    SizeRef.borderWidth(data.tokens);
const _$LegendSplitPaneAspectDividerThickness = LegendThemeAspect(
  _$LegendSplitPaneSelectDividerThickness,
);
Object? _$LegendSplitPaneOverrideSelectDividerThickness(
  LegendSplitPaneThemeNullable data,
) => data.dividerThickness;
const _$LegendSplitPaneOverrideAspectDividerThickness =
    LegendOverrideAspect<LegendSplitPaneThemeNullable>(
      _$LegendSplitPaneOverrideSelectDividerThickness,
    );

/// Resolves [LegendSplitPane.handle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendSplitPaneSelectHandle(LegendThemeData data) =>
    _handle(data.tokens).merge(
      data.componentOf<LegendSplitPaneThemeNullable>(LegendSplitPane)?.handle,
    );
const _$LegendSplitPaneAspectHandle = LegendThemeAspect(
  _$LegendSplitPaneSelectHandle,
);
Object? _$LegendSplitPaneOverrideSelectHandle(
  LegendSplitPaneThemeNullable data,
) => data.handle;
const _$LegendSplitPaneOverrideAspectHandle =
    LegendOverrideAspect<LegendSplitPaneThemeNullable>(
      _$LegendSplitPaneOverrideSelectHandle,
    );

/// Resolves [LegendSplitPane.hitTarget] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendSplitPaneSelectHitTarget(LegendThemeData data) =>
    data
        .componentOf<LegendSplitPaneThemeNullable>(LegendSplitPane)
        ?.hitTarget ??
    SizeRef.sm(data.tokens);
const _$LegendSplitPaneAspectHitTarget = LegendThemeAspect(
  _$LegendSplitPaneSelectHitTarget,
);
Object? _$LegendSplitPaneOverrideSelectHitTarget(
  LegendSplitPaneThemeNullable data,
) => data.hitTarget;
const _$LegendSplitPaneOverrideAspectHitTarget =
    LegendOverrideAspect<LegendSplitPaneThemeNullable>(
      _$LegendSplitPaneOverrideSelectHitTarget,
    );

/// Resolves [LegendSplitPane.collapseBreakpoint] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendSplitPaneSelectCollapseBreakpoint(LegendThemeData data) =>
    data
        .componentOf<LegendSplitPaneThemeNullable>(LegendSplitPane)
        ?.collapseBreakpoint ??
    0;
const _$LegendSplitPaneAspectCollapseBreakpoint = LegendThemeAspect(
  _$LegendSplitPaneSelectCollapseBreakpoint,
);
Object? _$LegendSplitPaneOverrideSelectCollapseBreakpoint(
  LegendSplitPaneThemeNullable data,
) => data.collapseBreakpoint;
const _$LegendSplitPaneOverrideAspectCollapseBreakpoint =
    LegendOverrideAspect<LegendSplitPaneThemeNullable>(
      _$LegendSplitPaneOverrideSelectCollapseBreakpoint,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendSplitPaneThemeListenables {
  /// Change stream of the resolved [LegendSplitPaneTheme.divider].
  static ValueListenable<Color> divider(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSplitPaneSelectDivider);

  /// Change stream of the resolved [LegendSplitPaneTheme.dividerThickness].
  static ValueListenable<double> dividerThickness(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendSplitPaneSelectDividerThickness,
  );

  /// Change stream of the resolved [LegendSplitPaneTheme.handle].
  static ValueListenable<InteractiveColors> handle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSplitPaneSelectHandle);

  /// Change stream of the resolved [LegendSplitPaneTheme.hitTarget].
  static ValueListenable<double> hitTarget(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSplitPaneSelectHitTarget);

  /// Change stream of the resolved [LegendSplitPaneTheme.collapseBreakpoint].
  static ValueListenable<double> collapseBreakpoint(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendSplitPaneSelectCollapseBreakpoint,
  );
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendSplitPaneThemeResolve on LegendSplitPane {
  /// [LegendSplitPaneTheme.of] with this widget's constructor params as
  /// level 1.
  LegendSplitPaneTheme _theme(BuildContext context) => LegendSplitPaneTheme.of(
    context,
    LegendSplitPaneThemeNullable(
      divider: divider,
      dividerThickness: dividerThickness,
      handle: handle,
      hitTarget: hitTarget,
      collapseBreakpoint: collapseBreakpoint,
    ),
  );

  /// Resolves ONLY [LegendSplitPane.divider] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._divider(context)`).
  Color _divider(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSplitPaneThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectDivider);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectDivider,
    );
    return divider ?? override?.divider ?? _$LegendSplitPaneSelectDivider(data);
  }

  /// Resolves ONLY [LegendSplitPane.dividerThickness] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._dividerThickness(context)`).
  double _dividerThickness(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSplitPaneThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectDividerThickness);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectDividerThickness,
    );
    return dividerThickness ??
        override?.dividerThickness ??
        _$LegendSplitPaneSelectDividerThickness(data);
  }

  /// Resolves ONLY [LegendSplitPane.handle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._handle(context)`).
  InteractiveColors _handle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSplitPaneThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectHandle);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectHandle,
    );
    return _$LegendSplitPaneSelectHandle(
      data,
    ).merge(override?.handle).merge(handle);
  }

  /// Resolves ONLY [LegendSplitPane.hitTarget] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._hitTarget(context)`).
  double _hitTarget(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSplitPaneThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectHitTarget);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectHitTarget,
    );
    return hitTarget ??
        override?.hitTarget ??
        _$LegendSplitPaneSelectHitTarget(data);
  }

  /// Resolves ONLY [LegendSplitPane.collapseBreakpoint] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._collapseBreakpoint(context)`).
  double _collapseBreakpoint(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSplitPaneThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSplitPaneAspectCollapseBreakpoint);
    LegendThemeOverride.depend<LegendSplitPaneThemeNullable>(
      context,
      _$LegendSplitPaneOverrideAspectCollapseBreakpoint,
    );
    return collapseBreakpoint ??
        override?.collapseBreakpoint ??
        _$LegendSplitPaneSelectCollapseBreakpoint(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendSplitPaneState]
/// is this file's `State<LegendSplitPane>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendSplitPaneThemeState])
/// wins over this extension.
extension _$LegendSplitPaneThemeOn_LegendSplitPaneState
    on _LegendSplitPaneState {
  LegendSplitPaneTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendSplitPane>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendSplitPaneThemeState on State<LegendSplitPane> {
  LegendSplitPaneTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendSplitPane extends
/// _$LegendSplitPaneBase` receives the resolved
/// [LegendSplitPaneTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendSplitPaneBase
    extends LegendStatelessWidget<LegendSplitPaneTheme> {
  const _$LegendSplitPaneBase({super.key});

  Color? get divider;
  double? get dividerThickness;
  InteractiveColors? get handle;
  double? get hitTarget;
  double? get collapseBreakpoint;

  @override
  LegendSplitPaneTheme resolveThemeOf(BuildContext context) =>
      LegendSplitPaneTheme.of(
        context,
        LegendSplitPaneThemeNullable(
          divider: divider,
          dividerThickness: dividerThickness,
          handle: handle,
          hitTarget: hitTarget,
          collapseBreakpoint: collapseBreakpoint,
        ),
      );
}
