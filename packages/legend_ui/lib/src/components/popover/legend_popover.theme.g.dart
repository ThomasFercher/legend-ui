// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_popover.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_popover.dart';

/// Resolved theme for [LegendPopover].
class LegendPopoverTheme {
  const LegendPopoverTheme({
    required this.background,
    required this.borderRadius,
    required this.shadows,
    required this.padding,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendPopoverTheme.defaults(LegendTokens t) => LegendPopoverTheme(
    background: ColorRef.surface(t),
    borderRadius: _borderRadius(t),
    shadows: ShadowRef.medium(t),
    padding: _padding(t),
  );

  final Color background;
  final BorderRadius borderRadius;
  final List<BoxShadow> shadows;
  final EdgeInsetsGeometry padding;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendPopover] first, [LegendPopoverThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendPopoverTheme of(
    BuildContext context, [
    LegendPopoverThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPopoverThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectBackground);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectBorderRadius);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectShadows);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectShadows,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectPadding);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectPadding,
    );
    return LegendPopoverTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendPopoverThemeNullable>(LegendPopover))
        .merge(override)
        .merge(local);
  }

  LegendPopoverTheme merge(LegendPopoverThemeNullable? other) {
    if (other == null) return this;
    return LegendPopoverTheme(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      shadows: other.shadows ?? shadows,
      padding: other.padding ?? padding,
    );
  }

  LegendPopoverTheme copyWith({
    Color? background,
    BorderRadius? borderRadius,
    List<BoxShadow>? shadows,
    EdgeInsetsGeometry? padding,
  }) => LegendPopoverTheme(
    background: background ?? this.background,
    borderRadius: borderRadius ?? this.borderRadius,
    shadows: shadows ?? this.shadows,
    padding: padding ?? this.padding,
  );

  static LegendPopoverTheme lerp(
    LegendPopoverTheme a,
    LegendPopoverTheme b,
    double t,
  ) => LegendPopoverTheme(
    background: t < 0.5 ? a.background : b.background,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    shadows: t < 0.5 ? a.shadows : b.shadows,
    padding: t < 0.5 ? a.padding : b.padding,
  );
}

/// Sparse overrides for [LegendPopoverTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendPopoverThemeNullable {
  const LegendPopoverThemeNullable({
    this.background,
    this.borderRadius,
    this.shadows,
    this.padding,
  });

  final Color? background;
  final BorderRadius? borderRadius;
  final List<BoxShadow>? shadows;
  final EdgeInsetsGeometry? padding;

  LegendPopoverThemeNullable merge(LegendPopoverThemeNullable? other) {
    if (other == null) return this;
    return LegendPopoverThemeNullable(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      shadows: other.shadows ?? shadows,
      padding: other.padding ?? padding,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendPopoverThemeNullable &&
          other.background == background &&
          other.borderRadius == borderRadius &&
          other.shadows == shadows &&
          other.padding == padding;

  @override
  int get hashCode =>
      Object.hashAll([background, borderRadius, shadows, padding]);
}

/// Overrides [LegendPopoverTheme] for a subtree (level 2).
class LegendPopoverThemeOverride extends StatelessWidget {
  const LegendPopoverThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendPopoverThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendPopoverThemeNullable>(data: data, child: child);
}

/// Resolves [LegendPopover.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendPopoverSelectBackground(LegendThemeData data) =>
    data.componentOf<LegendPopoverThemeNullable>(LegendPopover)?.background ??
    ColorRef.surface(data.tokens);
const _$LegendPopoverAspectBackground = LegendThemeAspect(
  _$LegendPopoverSelectBackground,
);
Object? _$LegendPopoverOverrideSelectBackground(
  LegendPopoverThemeNullable data,
) => data.background;
const _$LegendPopoverOverrideAspectBackground =
    LegendOverrideAspect<LegendPopoverThemeNullable>(
      _$LegendPopoverOverrideSelectBackground,
    );

/// Resolves [LegendPopover.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendPopoverSelectBorderRadius(LegendThemeData data) =>
    data.componentOf<LegendPopoverThemeNullable>(LegendPopover)?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendPopoverAspectBorderRadius = LegendThemeAspect(
  _$LegendPopoverSelectBorderRadius,
);
Object? _$LegendPopoverOverrideSelectBorderRadius(
  LegendPopoverThemeNullable data,
) => data.borderRadius;
const _$LegendPopoverOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendPopoverThemeNullable>(
      _$LegendPopoverOverrideSelectBorderRadius,
    );

/// Resolves [LegendPopover.shadows] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
List<BoxShadow> _$LegendPopoverSelectShadows(LegendThemeData data) =>
    data.componentOf<LegendPopoverThemeNullable>(LegendPopover)?.shadows ??
    ShadowRef.medium(data.tokens);
const _$LegendPopoverAspectShadows = LegendThemeAspect(
  _$LegendPopoverSelectShadows,
);
Object? _$LegendPopoverOverrideSelectShadows(LegendPopoverThemeNullable data) =>
    data.shadows;
const _$LegendPopoverOverrideAspectShadows =
    LegendOverrideAspect<LegendPopoverThemeNullable>(
      _$LegendPopoverOverrideSelectShadows,
    );

/// Resolves [LegendPopover.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendPopoverSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendPopoverThemeNullable>(LegendPopover)?.padding ??
    _padding(data.tokens);
const _$LegendPopoverAspectPadding = LegendThemeAspect(
  _$LegendPopoverSelectPadding,
);
Object? _$LegendPopoverOverrideSelectPadding(LegendPopoverThemeNullable data) =>
    data.padding;
const _$LegendPopoverOverrideAspectPadding =
    LegendOverrideAspect<LegendPopoverThemeNullable>(
      _$LegendPopoverOverrideSelectPadding,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendPopoverThemeListenables {
  /// Change stream of the resolved [LegendPopoverTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPopoverSelectBackground);

  /// Change stream of the resolved [LegendPopoverTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPopoverSelectBorderRadius);

  /// Change stream of the resolved [LegendPopoverTheme.shadows].
  static ValueListenable<List<BoxShadow>> shadows(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPopoverSelectShadows);

  /// Change stream of the resolved [LegendPopoverTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPopoverSelectPadding);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendPopoverThemeResolve on LegendPopover {
  /// [LegendPopoverTheme.of] with this widget's constructor params as
  /// level 1.
  LegendPopoverTheme _theme(BuildContext context) => LegendPopoverTheme.of(
    context,
    LegendPopoverThemeNullable(
      background: background,
      borderRadius: borderRadius,
      shadows: shadows,
      padding: padding,
    ),
  );

  /// Resolves ONLY [LegendPopover.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPopoverThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectBackground);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendPopoverSelectBackground(data);
  }

  /// Resolves ONLY [LegendPopover.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPopoverThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectBorderRadius);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendPopoverSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendPopover.shadows] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._shadows(context)`).
  List<BoxShadow> _shadows(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPopoverThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectShadows);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectShadows,
    );
    return shadows ?? override?.shadows ?? _$LegendPopoverSelectShadows(data);
  }

  /// Resolves ONLY [LegendPopover.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPopoverThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPopoverAspectPadding);
    LegendThemeOverride.depend<LegendPopoverThemeNullable>(
      context,
      _$LegendPopoverOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendPopoverSelectPadding(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendPopoverState]
/// is this file's `State<LegendPopover>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendPopoverThemeState])
/// wins over this extension.
extension _$LegendPopoverThemeOn_LegendPopoverState on _LegendPopoverState {
  LegendPopoverTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendPopover>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendPopoverThemeState on State<LegendPopover> {
  LegendPopoverTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendPopover extends
/// _$LegendPopoverBase` receives the resolved
/// [LegendPopoverTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendPopoverBase
    extends LegendStatelessWidget<LegendPopoverTheme> {
  const _$LegendPopoverBase({super.key});

  Color? get background;
  BorderRadius? get borderRadius;
  List<BoxShadow>? get shadows;
  EdgeInsetsGeometry? get padding;

  @override
  LegendPopoverTheme resolveThemeOf(BuildContext context) =>
      LegendPopoverTheme.of(
        context,
        LegendPopoverThemeNullable(
          background: background,
          borderRadius: borderRadius,
          shadows: shadows,
          padding: padding,
        ),
      );
}
