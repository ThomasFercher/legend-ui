// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_address.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_address.dart';

/// Resolved theme for [LegendAddress].
class LegendAddressTheme {
  const LegendAddressTheme({required this.textStyle, required this.separator});

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendAddressTheme.defaults(LegendTokens t) =>
      LegendAddressTheme(textStyle: _textStyle(t), separator: '…');

  final TextStyle textStyle;
  final String separator;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendAddress] first, [LegendAddressThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendAddressTheme of(
    BuildContext context, [
    LegendAddressThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAddressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAddressAspectTextStyle);
    LegendThemeOverride.depend<LegendAddressThemeNullable>(
      context,
      _$LegendAddressOverrideAspectTextStyle,
    );
    LegendTheme.depend(context, _$LegendAddressAspectSeparator);
    LegendThemeOverride.depend<LegendAddressThemeNullable>(
      context,
      _$LegendAddressOverrideAspectSeparator,
    );
    return LegendAddressTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendAddressThemeNullable>(LegendAddress))
        .merge(override)
        .merge(local);
  }

  LegendAddressTheme merge(LegendAddressThemeNullable? other) {
    if (other == null) return this;
    return LegendAddressTheme(
      textStyle: other.textStyle ?? textStyle,
      separator: other.separator ?? separator,
    );
  }

  LegendAddressTheme copyWith({TextStyle? textStyle, String? separator}) =>
      LegendAddressTheme(
        textStyle: textStyle ?? this.textStyle,
        separator: separator ?? this.separator,
      );

  static LegendAddressTheme lerp(
    LegendAddressTheme a,
    LegendAddressTheme b,
    double t,
  ) => LegendAddressTheme(
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
    separator: t < 0.5 ? a.separator : b.separator,
  );
}

/// Sparse overrides for [LegendAddressTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendAddressThemeNullable {
  const LegendAddressThemeNullable({this.textStyle, this.separator});

  final TextStyle? textStyle;
  final String? separator;

  LegendAddressThemeNullable merge(LegendAddressThemeNullable? other) {
    if (other == null) return this;
    return LegendAddressThemeNullable(
      textStyle: other.textStyle ?? textStyle,
      separator: other.separator ?? separator,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendAddressThemeNullable &&
          other.textStyle == textStyle &&
          other.separator == separator;

  @override
  int get hashCode => Object.hashAll([textStyle, separator]);
}

/// Overrides [LegendAddressTheme] for a subtree (level 2).
class LegendAddressThemeOverride extends StatelessWidget {
  const LegendAddressThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendAddressThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendAddressThemeNullable>(data: data, child: child);
}

/// Resolves [LegendAddress.textStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendAddressSelectTextStyle(LegendThemeData data) =>
    data.componentOf<LegendAddressThemeNullable>(LegendAddress)?.textStyle ??
    _textStyle(data.tokens);
const _$LegendAddressAspectTextStyle = LegendThemeAspect(
  _$LegendAddressSelectTextStyle,
);
Object? _$LegendAddressOverrideSelectTextStyle(
  LegendAddressThemeNullable data,
) => data.textStyle;
const _$LegendAddressOverrideAspectTextStyle =
    LegendOverrideAspect<LegendAddressThemeNullable>(
      _$LegendAddressOverrideSelectTextStyle,
    );

/// Resolves [LegendAddress.separator] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
String _$LegendAddressSelectSeparator(LegendThemeData data) =>
    data.componentOf<LegendAddressThemeNullable>(LegendAddress)?.separator ??
    '…';
const _$LegendAddressAspectSeparator = LegendThemeAspect(
  _$LegendAddressSelectSeparator,
);
Object? _$LegendAddressOverrideSelectSeparator(
  LegendAddressThemeNullable data,
) => data.separator;
const _$LegendAddressOverrideAspectSeparator =
    LegendOverrideAspect<LegendAddressThemeNullable>(
      _$LegendAddressOverrideSelectSeparator,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendAddressThemeListenables {
  /// Change stream of the resolved [LegendAddressTheme.textStyle].
  static ValueListenable<TextStyle> textStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAddressSelectTextStyle);

  /// Change stream of the resolved [LegendAddressTheme.separator].
  static ValueListenable<String> separator(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAddressSelectSeparator);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendAddressThemeResolve on LegendAddress {
  /// [LegendAddressTheme.of] with this widget's constructor params as
  /// level 1.
  LegendAddressTheme _theme(BuildContext context) => LegendAddressTheme.of(
    context,
    LegendAddressThemeNullable(textStyle: textStyle, separator: separator),
  );

  /// Resolves ONLY [LegendAddress.textStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._textStyle(context)`).
  TextStyle _textStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAddressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAddressAspectTextStyle);
    LegendThemeOverride.depend<LegendAddressThemeNullable>(
      context,
      _$LegendAddressOverrideAspectTextStyle,
    );
    return textStyle ??
        override?.textStyle ??
        _$LegendAddressSelectTextStyle(data);
  }

  /// Resolves ONLY [LegendAddress.separator] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._separator(context)`).
  String _separator(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAddressThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAddressAspectSeparator);
    LegendThemeOverride.depend<LegendAddressThemeNullable>(
      context,
      _$LegendAddressOverrideAspectSeparator,
    );
    return separator ??
        override?.separator ??
        _$LegendAddressSelectSeparator(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendAddress extends
/// _$LegendAddressBase` receives the resolved
/// [LegendAddressTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendAddressBase
    extends LegendStatelessWidget<LegendAddressTheme> {
  const _$LegendAddressBase({super.key});

  TextStyle? get textStyle;
  String? get separator;

  @override
  LegendAddressTheme resolveThemeOf(BuildContext context) =>
      LegendAddressTheme.of(
        context,
        LegendAddressThemeNullable(textStyle: textStyle, separator: separator),
      );
}
