// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_slider.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_slider.dart';

/// Resolved theme for [LegendSlider].
class LegendSliderTheme {
  const LegendSliderTheme({
    required this.track,
    required this.activeTrack,
    required this.thumb,
    required this.trackHeight,
    required this.thumbSize,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendSliderTheme.defaults(LegendTokens t) => LegendSliderTheme(
    track: ColorRef.background3(t),
    activeTrack: ColorRef.primary(t),
    thumb: _thumb(t),
    trackHeight: 4,
    thumbSize: 20,
  );

  final Color track;
  final Color activeTrack;
  final InteractiveColors thumb;
  final double trackHeight;
  final double thumbSize;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendSlider] first, [LegendSliderThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendSliderTheme of(
    BuildContext context, [
    LegendSliderThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSliderThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSliderAspectTrack);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectTrack,
    );
    LegendTheme.depend(context, _$LegendSliderAspectActiveTrack);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectActiveTrack,
    );
    LegendTheme.depend(context, _$LegendSliderAspectThumb);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectThumb,
    );
    LegendTheme.depend(context, _$LegendSliderAspectTrackHeight);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectTrackHeight,
    );
    LegendTheme.depend(context, _$LegendSliderAspectThumbSize);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectThumbSize,
    );
    return LegendSliderTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendSliderThemeNullable>(LegendSlider))
        .merge(override)
        .merge(local);
  }

  LegendSliderTheme merge(LegendSliderThemeNullable? other) {
    if (other == null) return this;
    return LegendSliderTheme(
      track: other.track ?? track,
      activeTrack: other.activeTrack ?? activeTrack,
      thumb: thumb.merge(other.thumb),
      trackHeight: other.trackHeight ?? trackHeight,
      thumbSize: other.thumbSize ?? thumbSize,
    );
  }

  LegendSliderTheme copyWith({
    Color? track,
    Color? activeTrack,
    InteractiveColors? thumb,
    double? trackHeight,
    double? thumbSize,
  }) => LegendSliderTheme(
    track: track ?? this.track,
    activeTrack: activeTrack ?? this.activeTrack,
    thumb: thumb ?? this.thumb,
    trackHeight: trackHeight ?? this.trackHeight,
    thumbSize: thumbSize ?? this.thumbSize,
  );

  static LegendSliderTheme lerp(
    LegendSliderTheme a,
    LegendSliderTheme b,
    double t,
  ) => LegendSliderTheme(
    track: Color.lerp(a.track, b.track, t)!,
    activeTrack: Color.lerp(a.activeTrack, b.activeTrack, t)!,
    thumb: InteractiveColors.lerp(a.thumb, b.thumb, t)!,
    trackHeight: t < 0.5 ? a.trackHeight : b.trackHeight,
    thumbSize: t < 0.5 ? a.thumbSize : b.thumbSize,
  );
}

/// Sparse overrides for [LegendSliderTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendSliderThemeNullable {
  const LegendSliderThemeNullable({
    this.track,
    this.activeTrack,
    this.thumb,
    this.trackHeight,
    this.thumbSize,
  });

  final Color? track;
  final Color? activeTrack;
  final InteractiveColors? thumb;
  final double? trackHeight;
  final double? thumbSize;

  LegendSliderThemeNullable merge(LegendSliderThemeNullable? other) {
    if (other == null) return this;
    return LegendSliderThemeNullable(
      track: other.track ?? track,
      activeTrack: other.activeTrack ?? activeTrack,
      thumb: thumb?.merge(other.thumb) ?? other.thumb,
      trackHeight: other.trackHeight ?? trackHeight,
      thumbSize: other.thumbSize ?? thumbSize,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendSliderThemeNullable &&
          other.track == track &&
          other.activeTrack == activeTrack &&
          other.thumb == thumb &&
          other.trackHeight == trackHeight &&
          other.thumbSize == thumbSize;

  @override
  int get hashCode =>
      Object.hashAll([track, activeTrack, thumb, trackHeight, thumbSize]);
}

/// Overrides [LegendSliderTheme] for a subtree (level 2).
class LegendSliderThemeOverride extends StatelessWidget {
  const LegendSliderThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendSliderThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendSliderThemeNullable>(data: data, child: child);
}

/// Resolves [LegendSlider.track] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendSliderSelectTrack(LegendThemeData data) =>
    data.componentOf<LegendSliderThemeNullable>(LegendSlider)?.track ??
    ColorRef.background3(data.tokens);
const _$LegendSliderAspectTrack = LegendThemeAspect(_$LegendSliderSelectTrack);
Object? _$LegendSliderOverrideSelectTrack(LegendSliderThemeNullable data) =>
    data.track;
const _$LegendSliderOverrideAspectTrack =
    LegendOverrideAspect<LegendSliderThemeNullable>(
      _$LegendSliderOverrideSelectTrack,
    );

/// Resolves [LegendSlider.activeTrack] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendSliderSelectActiveTrack(LegendThemeData data) =>
    data.componentOf<LegendSliderThemeNullable>(LegendSlider)?.activeTrack ??
    ColorRef.primary(data.tokens);
const _$LegendSliderAspectActiveTrack = LegendThemeAspect(
  _$LegendSliderSelectActiveTrack,
);
Object? _$LegendSliderOverrideSelectActiveTrack(
  LegendSliderThemeNullable data,
) => data.activeTrack;
const _$LegendSliderOverrideAspectActiveTrack =
    LegendOverrideAspect<LegendSliderThemeNullable>(
      _$LegendSliderOverrideSelectActiveTrack,
    );

/// Resolves [LegendSlider.thumb] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendSliderSelectThumb(LegendThemeData data) => _thumb(
  data.tokens,
).merge(data.componentOf<LegendSliderThemeNullable>(LegendSlider)?.thumb);
const _$LegendSliderAspectThumb = LegendThemeAspect(_$LegendSliderSelectThumb);
Object? _$LegendSliderOverrideSelectThumb(LegendSliderThemeNullable data) =>
    data.thumb;
const _$LegendSliderOverrideAspectThumb =
    LegendOverrideAspect<LegendSliderThemeNullable>(
      _$LegendSliderOverrideSelectThumb,
    );

/// Resolves [LegendSlider.trackHeight] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendSliderSelectTrackHeight(LegendThemeData data) =>
    data.componentOf<LegendSliderThemeNullable>(LegendSlider)?.trackHeight ?? 4;
const _$LegendSliderAspectTrackHeight = LegendThemeAspect(
  _$LegendSliderSelectTrackHeight,
);
Object? _$LegendSliderOverrideSelectTrackHeight(
  LegendSliderThemeNullable data,
) => data.trackHeight;
const _$LegendSliderOverrideAspectTrackHeight =
    LegendOverrideAspect<LegendSliderThemeNullable>(
      _$LegendSliderOverrideSelectTrackHeight,
    );

/// Resolves [LegendSlider.thumbSize] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendSliderSelectThumbSize(LegendThemeData data) =>
    data.componentOf<LegendSliderThemeNullable>(LegendSlider)?.thumbSize ?? 20;
const _$LegendSliderAspectThumbSize = LegendThemeAspect(
  _$LegendSliderSelectThumbSize,
);
Object? _$LegendSliderOverrideSelectThumbSize(LegendSliderThemeNullable data) =>
    data.thumbSize;
const _$LegendSliderOverrideAspectThumbSize =
    LegendOverrideAspect<LegendSliderThemeNullable>(
      _$LegendSliderOverrideSelectThumbSize,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendSliderThemeListenables {
  /// Change stream of the resolved [LegendSliderTheme.track].
  static ValueListenable<Color> track(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSliderSelectTrack);

  /// Change stream of the resolved [LegendSliderTheme.activeTrack].
  static ValueListenable<Color> activeTrack(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSliderSelectActiveTrack);

  /// Change stream of the resolved [LegendSliderTheme.thumb].
  static ValueListenable<InteractiveColors> thumb(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSliderSelectThumb);

  /// Change stream of the resolved [LegendSliderTheme.trackHeight].
  static ValueListenable<double> trackHeight(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSliderSelectTrackHeight);

  /// Change stream of the resolved [LegendSliderTheme.thumbSize].
  static ValueListenable<double> thumbSize(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendSliderSelectThumbSize);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendSliderThemeResolve on LegendSlider {
  /// [LegendSliderTheme.of] with this widget's constructor params as
  /// level 1.
  LegendSliderTheme _theme(BuildContext context) => LegendSliderTheme.of(
    context,
    LegendSliderThemeNullable(
      track: track,
      activeTrack: activeTrack,
      thumb: thumb,
      trackHeight: trackHeight,
      thumbSize: thumbSize,
    ),
  );

  /// Resolves ONLY [LegendSlider.track] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._track(context)`).
  Color _track(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSliderThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSliderAspectTrack);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectTrack,
    );
    return track ?? override?.track ?? _$LegendSliderSelectTrack(data);
  }

  /// Resolves ONLY [LegendSlider.activeTrack] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._activeTrack(context)`).
  Color _activeTrack(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSliderThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSliderAspectActiveTrack);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectActiveTrack,
    );
    return activeTrack ??
        override?.activeTrack ??
        _$LegendSliderSelectActiveTrack(data);
  }

  /// Resolves ONLY [LegendSlider.thumb] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._thumb(context)`).
  InteractiveColors _thumb(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSliderThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSliderAspectThumb);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectThumb,
    );
    return _$LegendSliderSelectThumb(data).merge(override?.thumb).merge(thumb);
  }

  /// Resolves ONLY [LegendSlider.trackHeight] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._trackHeight(context)`).
  double _trackHeight(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSliderThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSliderAspectTrackHeight);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectTrackHeight,
    );
    return trackHeight ??
        override?.trackHeight ??
        _$LegendSliderSelectTrackHeight(data);
  }

  /// Resolves ONLY [LegendSlider.thumbSize] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._thumbSize(context)`).
  double _thumbSize(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendSliderThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendSliderAspectThumbSize);
    LegendThemeOverride.depend<LegendSliderThemeNullable>(
      context,
      _$LegendSliderOverrideAspectThumbSize,
    );
    return thumbSize ??
        override?.thumbSize ??
        _$LegendSliderSelectThumbSize(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendSliderState]
/// is this file's `State<LegendSlider>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendSliderThemeState])
/// wins over this extension.
extension _$LegendSliderThemeOn_LegendSliderState on _LegendSliderState {
  LegendSliderTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendSlider>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendSliderThemeState on State<LegendSlider> {
  LegendSliderTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendSlider extends
/// _$LegendSliderBase` receives the resolved
/// [LegendSliderTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendSliderBase
    extends LegendStatelessWidget<LegendSliderTheme> {
  const _$LegendSliderBase({super.key});

  Color? get track;
  Color? get activeTrack;
  InteractiveColors? get thumb;
  double? get trackHeight;
  double? get thumbSize;

  @override
  LegendSliderTheme resolveThemeOf(BuildContext context) =>
      LegendSliderTheme.of(
        context,
        LegendSliderThemeNullable(
          track: track,
          activeTrack: activeTrack,
          thumb: thumb,
          trackHeight: trackHeight,
          thumbSize: thumbSize,
        ),
      );
}
