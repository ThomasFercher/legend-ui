// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_pagination.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_pagination.dart';

/// Resolved theme for [LegendPagination].
class LegendPaginationTheme {
  const LegendPaginationTheme({
    required this.fill,
    required this.selectedFill,
    required this.itemStyle,
    required this.selectedStyle,
    required this.chevronColor,
    required this.borderRadius,
    required this.itemPadding,
    required this.spacing,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendPaginationTheme.defaults(LegendTokens t) =>
      LegendPaginationTheme(
        fill: _fill(t),
        selectedFill: _selectedFill(t),
        itemStyle: _itemStyle(t),
        selectedStyle: _selectedStyle(t),
        chevronColor: ColorRef.foreground2(t),
        borderRadius: _borderRadius(t),
        itemPadding: _itemPadding(t),
        spacing: SizeRef.xs(t),
      );

  final InteractiveColors fill;
  final InteractiveColors selectedFill;
  final TextStyle itemStyle;
  final TextStyle selectedStyle;
  final Color chevronColor;
  final BorderRadius borderRadius;
  final EdgeInsetsGeometry itemPadding;
  final double spacing;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendPagination] first, [LegendPaginationThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendPaginationTheme of(
    BuildContext context, [
    LegendPaginationThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectFill);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectFill,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectSelectedFill);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectSelectedFill,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectItemStyle);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectItemStyle,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectSelectedStyle);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectSelectedStyle,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectChevronColor);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectChevronColor,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectBorderRadius);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectItemPadding);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectItemPadding,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectSpacing);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectSpacing,
    );
    return LegendPaginationTheme.defaults(data.tokens)
        .merge(
          data.componentOf<LegendPaginationThemeNullable>(LegendPagination),
        )
        .merge(override)
        .merge(local);
  }

  LegendPaginationTheme merge(LegendPaginationThemeNullable? other) {
    if (other == null) return this;
    return LegendPaginationTheme(
      fill: fill.merge(other.fill),
      selectedFill: selectedFill.merge(other.selectedFill),
      itemStyle: other.itemStyle ?? itemStyle,
      selectedStyle: other.selectedStyle ?? selectedStyle,
      chevronColor: other.chevronColor ?? chevronColor,
      borderRadius: other.borderRadius ?? borderRadius,
      itemPadding: other.itemPadding ?? itemPadding,
      spacing: other.spacing ?? spacing,
    );
  }

  LegendPaginationTheme copyWith({
    InteractiveColors? fill,
    InteractiveColors? selectedFill,
    TextStyle? itemStyle,
    TextStyle? selectedStyle,
    Color? chevronColor,
    BorderRadius? borderRadius,
    EdgeInsetsGeometry? itemPadding,
    double? spacing,
  }) => LegendPaginationTheme(
    fill: fill ?? this.fill,
    selectedFill: selectedFill ?? this.selectedFill,
    itemStyle: itemStyle ?? this.itemStyle,
    selectedStyle: selectedStyle ?? this.selectedStyle,
    chevronColor: chevronColor ?? this.chevronColor,
    borderRadius: borderRadius ?? this.borderRadius,
    itemPadding: itemPadding ?? this.itemPadding,
    spacing: spacing ?? this.spacing,
  );

  static LegendPaginationTheme lerp(
    LegendPaginationTheme a,
    LegendPaginationTheme b,
    double t,
  ) => LegendPaginationTheme(
    fill: t < 0.5 ? a.fill : b.fill,
    selectedFill: t < 0.5 ? a.selectedFill : b.selectedFill,
    itemStyle: t < 0.5 ? a.itemStyle : b.itemStyle,
    selectedStyle: t < 0.5 ? a.selectedStyle : b.selectedStyle,
    chevronColor: Color.lerp(a.chevronColor, b.chevronColor, t)!,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
    spacing: t < 0.5 ? a.spacing : b.spacing,
  );
}

/// Sparse overrides for [LegendPaginationTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendPaginationThemeNullable {
  const LegendPaginationThemeNullable({
    this.fill,
    this.selectedFill,
    this.itemStyle,
    this.selectedStyle,
    this.chevronColor,
    this.borderRadius,
    this.itemPadding,
    this.spacing,
  });

  final InteractiveColors? fill;
  final InteractiveColors? selectedFill;
  final TextStyle? itemStyle;
  final TextStyle? selectedStyle;
  final Color? chevronColor;
  final BorderRadius? borderRadius;
  final EdgeInsetsGeometry? itemPadding;
  final double? spacing;

  LegendPaginationThemeNullable merge(LegendPaginationThemeNullable? other) {
    if (other == null) return this;
    return LegendPaginationThemeNullable(
      fill: fill?.merge(other.fill) ?? other.fill,
      selectedFill:
          selectedFill?.merge(other.selectedFill) ?? other.selectedFill,
      itemStyle: other.itemStyle ?? itemStyle,
      selectedStyle: other.selectedStyle ?? selectedStyle,
      chevronColor: other.chevronColor ?? chevronColor,
      borderRadius: other.borderRadius ?? borderRadius,
      itemPadding: other.itemPadding ?? itemPadding,
      spacing: other.spacing ?? spacing,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendPaginationThemeNullable &&
          other.fill == fill &&
          other.selectedFill == selectedFill &&
          other.itemStyle == itemStyle &&
          other.selectedStyle == selectedStyle &&
          other.chevronColor == chevronColor &&
          other.borderRadius == borderRadius &&
          other.itemPadding == itemPadding &&
          other.spacing == spacing;

  @override
  int get hashCode => Object.hashAll([
    fill,
    selectedFill,
    itemStyle,
    selectedStyle,
    chevronColor,
    borderRadius,
    itemPadding,
    spacing,
  ]);
}

/// Overrides [LegendPaginationTheme] for a subtree (level 2).
class LegendPaginationThemeOverride extends StatelessWidget {
  const LegendPaginationThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendPaginationThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendPaginationThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendPagination.fill] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendPaginationSelectFill(LegendThemeData data) =>
    _fill(data.tokens).merge(
      data.componentOf<LegendPaginationThemeNullable>(LegendPagination)?.fill,
    );
const _$LegendPaginationAspectFill = LegendThemeAspect(
  _$LegendPaginationSelectFill,
);
Object? _$LegendPaginationOverrideSelectFill(
  LegendPaginationThemeNullable data,
) => data.fill;
const _$LegendPaginationOverrideAspectFill =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectFill,
    );

/// Resolves [LegendPagination.selectedFill] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendPaginationSelectSelectedFill(LegendThemeData data) =>
    _selectedFill(data.tokens).merge(
      data
          .componentOf<LegendPaginationThemeNullable>(LegendPagination)
          ?.selectedFill,
    );
const _$LegendPaginationAspectSelectedFill = LegendThemeAspect(
  _$LegendPaginationSelectSelectedFill,
);
Object? _$LegendPaginationOverrideSelectSelectedFill(
  LegendPaginationThemeNullable data,
) => data.selectedFill;
const _$LegendPaginationOverrideAspectSelectedFill =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectSelectedFill,
    );

/// Resolves [LegendPagination.itemStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendPaginationSelectItemStyle(LegendThemeData data) =>
    data
        .componentOf<LegendPaginationThemeNullable>(LegendPagination)
        ?.itemStyle ??
    _itemStyle(data.tokens);
const _$LegendPaginationAspectItemStyle = LegendThemeAspect(
  _$LegendPaginationSelectItemStyle,
);
Object? _$LegendPaginationOverrideSelectItemStyle(
  LegendPaginationThemeNullable data,
) => data.itemStyle;
const _$LegendPaginationOverrideAspectItemStyle =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectItemStyle,
    );

/// Resolves [LegendPagination.selectedStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendPaginationSelectSelectedStyle(LegendThemeData data) =>
    data
        .componentOf<LegendPaginationThemeNullable>(LegendPagination)
        ?.selectedStyle ??
    _selectedStyle(data.tokens);
const _$LegendPaginationAspectSelectedStyle = LegendThemeAspect(
  _$LegendPaginationSelectSelectedStyle,
);
Object? _$LegendPaginationOverrideSelectSelectedStyle(
  LegendPaginationThemeNullable data,
) => data.selectedStyle;
const _$LegendPaginationOverrideAspectSelectedStyle =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectSelectedStyle,
    );

/// Resolves [LegendPagination.chevronColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendPaginationSelectChevronColor(LegendThemeData data) =>
    data
        .componentOf<LegendPaginationThemeNullable>(LegendPagination)
        ?.chevronColor ??
    ColorRef.foreground2(data.tokens);
const _$LegendPaginationAspectChevronColor = LegendThemeAspect(
  _$LegendPaginationSelectChevronColor,
);
Object? _$LegendPaginationOverrideSelectChevronColor(
  LegendPaginationThemeNullable data,
) => data.chevronColor;
const _$LegendPaginationOverrideAspectChevronColor =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectChevronColor,
    );

/// Resolves [LegendPagination.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendPaginationSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendPaginationThemeNullable>(LegendPagination)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendPaginationAspectBorderRadius = LegendThemeAspect(
  _$LegendPaginationSelectBorderRadius,
);
Object? _$LegendPaginationOverrideSelectBorderRadius(
  LegendPaginationThemeNullable data,
) => data.borderRadius;
const _$LegendPaginationOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectBorderRadius,
    );

/// Resolves [LegendPagination.itemPadding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendPaginationSelectItemPadding(LegendThemeData data) =>
    data
        .componentOf<LegendPaginationThemeNullable>(LegendPagination)
        ?.itemPadding ??
    _itemPadding(data.tokens);
const _$LegendPaginationAspectItemPadding = LegendThemeAspect(
  _$LegendPaginationSelectItemPadding,
);
Object? _$LegendPaginationOverrideSelectItemPadding(
  LegendPaginationThemeNullable data,
) => data.itemPadding;
const _$LegendPaginationOverrideAspectItemPadding =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectItemPadding,
    );

/// Resolves [LegendPagination.spacing] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendPaginationSelectSpacing(LegendThemeData data) =>
    data
        .componentOf<LegendPaginationThemeNullable>(LegendPagination)
        ?.spacing ??
    SizeRef.xs(data.tokens);
const _$LegendPaginationAspectSpacing = LegendThemeAspect(
  _$LegendPaginationSelectSpacing,
);
Object? _$LegendPaginationOverrideSelectSpacing(
  LegendPaginationThemeNullable data,
) => data.spacing;
const _$LegendPaginationOverrideAspectSpacing =
    LegendOverrideAspect<LegendPaginationThemeNullable>(
      _$LegendPaginationOverrideSelectSpacing,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendPaginationThemeListenables {
  /// Change stream of the resolved [LegendPaginationTheme.fill].
  static ValueListenable<InteractiveColors> fill(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectFill);

  /// Change stream of the resolved [LegendPaginationTheme.selectedFill].
  static ValueListenable<InteractiveColors> selectedFill(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectSelectedFill);

  /// Change stream of the resolved [LegendPaginationTheme.itemStyle].
  static ValueListenable<TextStyle> itemStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectItemStyle);

  /// Change stream of the resolved [LegendPaginationTheme.selectedStyle].
  static ValueListenable<TextStyle> selectedStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectSelectedStyle);

  /// Change stream of the resolved [LegendPaginationTheme.chevronColor].
  static ValueListenable<Color> chevronColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectChevronColor);

  /// Change stream of the resolved [LegendPaginationTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectBorderRadius);

  /// Change stream of the resolved [LegendPaginationTheme.itemPadding].
  static ValueListenable<EdgeInsetsGeometry> itemPadding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectItemPadding);

  /// Change stream of the resolved [LegendPaginationTheme.spacing].
  static ValueListenable<double> spacing(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendPaginationSelectSpacing);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendPaginationThemeResolve on LegendPagination {
  /// [LegendPaginationTheme.of] with this widget's constructor params as
  /// level 1.
  LegendPaginationTheme _theme(BuildContext context) =>
      LegendPaginationTheme.of(
        context,
        LegendPaginationThemeNullable(
          fill: fill,
          selectedFill: selectedFill,
          itemStyle: itemStyle,
          selectedStyle: selectedStyle,
          chevronColor: chevronColor,
          borderRadius: borderRadius,
          itemPadding: itemPadding,
          spacing: spacing,
        ),
      );

  /// Resolves ONLY [LegendPagination.fill] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._fill(context)`).
  InteractiveColors _fill(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectFill);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectFill,
    );
    return _$LegendPaginationSelectFill(data).merge(override?.fill).merge(fill);
  }

  /// Resolves ONLY [LegendPagination.selectedFill] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedFill(context)`).
  InteractiveColors _selectedFill(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectSelectedFill);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectSelectedFill,
    );
    return _$LegendPaginationSelectSelectedFill(
      data,
    ).merge(override?.selectedFill).merge(selectedFill);
  }

  /// Resolves ONLY [LegendPagination.itemStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemStyle(context)`).
  TextStyle _itemStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectItemStyle);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectItemStyle,
    );
    return itemStyle ??
        override?.itemStyle ??
        _$LegendPaginationSelectItemStyle(data);
  }

  /// Resolves ONLY [LegendPagination.selectedStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedStyle(context)`).
  TextStyle _selectedStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectSelectedStyle);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectSelectedStyle,
    );
    return selectedStyle ??
        override?.selectedStyle ??
        _$LegendPaginationSelectSelectedStyle(data);
  }

  /// Resolves ONLY [LegendPagination.chevronColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._chevronColor(context)`).
  Color _chevronColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectChevronColor);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectChevronColor,
    );
    return chevronColor ??
        override?.chevronColor ??
        _$LegendPaginationSelectChevronColor(data);
  }

  /// Resolves ONLY [LegendPagination.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectBorderRadius);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendPaginationSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendPagination.itemPadding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemPadding(context)`).
  EdgeInsetsGeometry _itemPadding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectItemPadding);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectItemPadding,
    );
    return itemPadding ??
        override?.itemPadding ??
        _$LegendPaginationSelectItemPadding(data);
  }

  /// Resolves ONLY [LegendPagination.spacing] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._spacing(context)`).
  double _spacing(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendPaginationThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendPaginationAspectSpacing);
    LegendThemeOverride.depend<LegendPaginationThemeNullable>(
      context,
      _$LegendPaginationOverrideAspectSpacing,
    );
    return spacing ??
        override?.spacing ??
        _$LegendPaginationSelectSpacing(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendPagination extends
/// _$LegendPaginationBase` receives the resolved
/// [LegendPaginationTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendPaginationBase
    extends LegendStatelessWidget<LegendPaginationTheme> {
  const _$LegendPaginationBase({super.key});

  InteractiveColors? get fill;
  InteractiveColors? get selectedFill;
  TextStyle? get itemStyle;
  TextStyle? get selectedStyle;
  Color? get chevronColor;
  BorderRadius? get borderRadius;
  EdgeInsetsGeometry? get itemPadding;
  double? get spacing;

  @override
  LegendPaginationTheme resolveThemeOf(BuildContext context) =>
      LegendPaginationTheme.of(
        context,
        LegendPaginationThemeNullable(
          fill: fill,
          selectedFill: selectedFill,
          itemStyle: itemStyle,
          selectedStyle: selectedStyle,
          chevronColor: chevronColor,
          borderRadius: borderRadius,
          itemPadding: itemPadding,
          spacing: spacing,
        ),
      );
}
