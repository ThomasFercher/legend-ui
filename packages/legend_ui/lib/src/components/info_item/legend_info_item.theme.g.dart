// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_info_item.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_info_item.dart';

/// Resolved theme for [LegendInfoItem].
class LegendInfoItemTheme {
  const LegendInfoItemTheme({
    required this.labelStyle,
    required this.valueStyle,
    required this.padding,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendInfoItemTheme.defaults(LegendTokens t) => LegendInfoItemTheme(
    labelStyle: LegendInfoItem._labelStyle(t),
    valueStyle: LegendInfoItem._valueStyle(t),
    padding: LegendInfoItem._padding(t),
  );

  final TextStyle labelStyle;
  final TextStyle valueStyle;
  final EdgeInsetsGeometry padding;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendInfoItem] first, [LegendInfoItemThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  static LegendInfoItemTheme of(
    BuildContext context, [
    LegendInfoItemThemeNullable? local,
  ]) {
    final data = LegendTheme.of(context);
    final resolved = LegendInfoItemTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendInfoItemThemeNullable>(LegendInfoItem))
        .merge(
          LegendThemeOverride.maybeOf<LegendInfoItemThemeNullable>(context),
        )
        .merge(local);
    return resolved;
  }

  LegendInfoItemTheme merge(LegendInfoItemThemeNullable? other) {
    if (other == null) return this;
    return LegendInfoItemTheme(
      labelStyle: other.labelStyle ?? labelStyle,
      valueStyle: other.valueStyle ?? valueStyle,
      padding: other.padding ?? padding,
    );
  }

  LegendInfoItemTheme copyWith({
    TextStyle? labelStyle,
    TextStyle? valueStyle,
    EdgeInsetsGeometry? padding,
  }) => LegendInfoItemTheme(
    labelStyle: labelStyle ?? this.labelStyle,
    valueStyle: valueStyle ?? this.valueStyle,
    padding: padding ?? this.padding,
  );

  static LegendInfoItemTheme lerp(
    LegendInfoItemTheme a,
    LegendInfoItemTheme b,
    double t,
  ) => LegendInfoItemTheme(
    labelStyle: t < 0.5 ? a.labelStyle : b.labelStyle,
    valueStyle: t < 0.5 ? a.valueStyle : b.valueStyle,
    padding: t < 0.5 ? a.padding : b.padding,
  );
}

/// Sparse overrides for [LegendInfoItemTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendInfoItemThemeNullable {
  const LegendInfoItemThemeNullable({
    this.labelStyle,
    this.valueStyle,
    this.padding,
  });

  final TextStyle? labelStyle;
  final TextStyle? valueStyle;
  final EdgeInsetsGeometry? padding;

  LegendInfoItemThemeNullable merge(LegendInfoItemThemeNullable? other) {
    if (other == null) return this;
    return LegendInfoItemThemeNullable(
      labelStyle: other.labelStyle ?? labelStyle,
      valueStyle: other.valueStyle ?? valueStyle,
      padding: other.padding ?? padding,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendInfoItemThemeNullable &&
          other.labelStyle == labelStyle &&
          other.valueStyle == valueStyle &&
          other.padding == padding;

  @override
  int get hashCode => Object.hashAll([labelStyle, valueStyle, padding]);
}

/// Overrides [LegendInfoItemTheme] for a subtree (level 2).
class LegendInfoItemThemeOverride extends StatelessWidget {
  const LegendInfoItemThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendInfoItemThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendInfoItemThemeNullable>(
        data: data,
        child: child,
      );
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendInfoItemThemeResolve on LegendInfoItem {
  /// [LegendInfoItemTheme.of] with this widget's constructor params as
  /// level 1.
  LegendInfoItemTheme _theme(BuildContext context) => LegendInfoItemTheme.of(
    context,
    LegendInfoItemThemeNullable(
      labelStyle: labelStyle,
      valueStyle: valueStyle,
      padding: padding,
    ),
  );
}
