// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_menu.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_menu.dart';

/// Resolved theme for [LegendMenu].
class LegendMenuTheme {
  const LegendMenuTheme({
    required this.background,
    required this.borderRadius,
    required this.shadows,
    required this.itemPadding,
    required this.itemColors,
    required this.textStyle,
    required this.destructiveColor,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendMenuTheme.defaults(LegendTokens t) => LegendMenuTheme(
    background: ColorRef.surface(t),
    borderRadius: _borderRadius(t),
    shadows: ShadowRef.medium(t),
    itemPadding: _itemPadding(t),
    itemColors: _itemColors(t),
    textStyle: TextRef.b2(t),
    destructiveColor: ColorRef.error(t),
  );

  final Color background;
  final BorderRadius borderRadius;
  final List<BoxShadow> shadows;
  final EdgeInsetsGeometry itemPadding;
  final InteractiveColors itemColors;
  final TextStyle textStyle;
  final Color destructiveColor;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendMenu] first, [LegendMenuThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendMenuTheme of(
    BuildContext context, [
    LegendMenuThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectBackground);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendMenuAspectBorderRadius);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendMenuAspectShadows);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectShadows,
    );
    LegendTheme.depend(context, _$LegendMenuAspectItemPadding);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectItemPadding,
    );
    LegendTheme.depend(context, _$LegendMenuAspectItemColors);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectItemColors,
    );
    LegendTheme.depend(context, _$LegendMenuAspectTextStyle);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectTextStyle,
    );
    LegendTheme.depend(context, _$LegendMenuAspectDestructiveColor);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectDestructiveColor,
    );
    return LegendMenuTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendMenuThemeNullable>(LegendMenu))
        .merge(override)
        .merge(local);
  }

  LegendMenuTheme merge(LegendMenuThemeNullable? other) {
    if (other == null) return this;
    return LegendMenuTheme(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      shadows: other.shadows ?? shadows,
      itemPadding: other.itemPadding ?? itemPadding,
      itemColors: itemColors.merge(other.itemColors),
      textStyle: other.textStyle ?? textStyle,
      destructiveColor: other.destructiveColor ?? destructiveColor,
    );
  }

  LegendMenuTheme copyWith({
    Color? background,
    BorderRadius? borderRadius,
    List<BoxShadow>? shadows,
    EdgeInsetsGeometry? itemPadding,
    InteractiveColors? itemColors,
    TextStyle? textStyle,
    Color? destructiveColor,
  }) => LegendMenuTheme(
    background: background ?? this.background,
    borderRadius: borderRadius ?? this.borderRadius,
    shadows: shadows ?? this.shadows,
    itemPadding: itemPadding ?? this.itemPadding,
    itemColors: itemColors ?? this.itemColors,
    textStyle: textStyle ?? this.textStyle,
    destructiveColor: destructiveColor ?? this.destructiveColor,
  );

  static LegendMenuTheme lerp(LegendMenuTheme a, LegendMenuTheme b, double t) =>
      LegendMenuTheme(
        background: t < 0.5 ? a.background : b.background,
        borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
        shadows: t < 0.5 ? a.shadows : b.shadows,
        itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
        itemColors: t < 0.5 ? a.itemColors : b.itemColors,
        textStyle: t < 0.5 ? a.textStyle : b.textStyle,
        destructiveColor: t < 0.5 ? a.destructiveColor : b.destructiveColor,
      );
}

/// Sparse overrides for [LegendMenuTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendMenuThemeNullable {
  const LegendMenuThemeNullable({
    this.background,
    this.borderRadius,
    this.shadows,
    this.itemPadding,
    this.itemColors,
    this.textStyle,
    this.destructiveColor,
  });

  final Color? background;
  final BorderRadius? borderRadius;
  final List<BoxShadow>? shadows;
  final EdgeInsetsGeometry? itemPadding;
  final InteractiveColors? itemColors;
  final TextStyle? textStyle;
  final Color? destructiveColor;

  LegendMenuThemeNullable merge(LegendMenuThemeNullable? other) {
    if (other == null) return this;
    return LegendMenuThemeNullable(
      background: other.background ?? background,
      borderRadius: other.borderRadius ?? borderRadius,
      shadows: other.shadows ?? shadows,
      itemPadding: other.itemPadding ?? itemPadding,
      itemColors: itemColors?.merge(other.itemColors) ?? other.itemColors,
      textStyle: other.textStyle ?? textStyle,
      destructiveColor: other.destructiveColor ?? destructiveColor,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendMenuThemeNullable &&
          other.background == background &&
          other.borderRadius == borderRadius &&
          other.shadows == shadows &&
          other.itemPadding == itemPadding &&
          other.itemColors == itemColors &&
          other.textStyle == textStyle &&
          other.destructiveColor == destructiveColor;

  @override
  int get hashCode => Object.hashAll([
    background,
    borderRadius,
    shadows,
    itemPadding,
    itemColors,
    textStyle,
    destructiveColor,
  ]);
}

/// Overrides [LegendMenuTheme] for a subtree (level 2).
class LegendMenuThemeOverride extends StatelessWidget {
  const LegendMenuThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendMenuThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendMenuThemeNullable>(data: data, child: child);
}

/// Resolves [LegendMenu.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendMenuSelectBackground(LegendThemeData data) =>
    data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.background ??
    ColorRef.surface(data.tokens);
const _$LegendMenuAspectBackground = LegendThemeAspect(
  _$LegendMenuSelectBackground,
);
Object? _$LegendMenuOverrideSelectBackground(LegendMenuThemeNullable data) =>
    data.background;
const _$LegendMenuOverrideAspectBackground =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectBackground,
    );

/// Resolves [LegendMenu.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendMenuSelectBorderRadius(LegendThemeData data) =>
    data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendMenuAspectBorderRadius = LegendThemeAspect(
  _$LegendMenuSelectBorderRadius,
);
Object? _$LegendMenuOverrideSelectBorderRadius(LegendMenuThemeNullable data) =>
    data.borderRadius;
const _$LegendMenuOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectBorderRadius,
    );

/// Resolves [LegendMenu.shadows] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
List<BoxShadow> _$LegendMenuSelectShadows(LegendThemeData data) =>
    data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.shadows ??
    ShadowRef.medium(data.tokens);
const _$LegendMenuAspectShadows = LegendThemeAspect(_$LegendMenuSelectShadows);
Object? _$LegendMenuOverrideSelectShadows(LegendMenuThemeNullable data) =>
    data.shadows;
const _$LegendMenuOverrideAspectShadows =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectShadows,
    );

/// Resolves [LegendMenu.itemPadding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendMenuSelectItemPadding(LegendThemeData data) =>
    data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.itemPadding ??
    _itemPadding(data.tokens);
const _$LegendMenuAspectItemPadding = LegendThemeAspect(
  _$LegendMenuSelectItemPadding,
);
Object? _$LegendMenuOverrideSelectItemPadding(LegendMenuThemeNullable data) =>
    data.itemPadding;
const _$LegendMenuOverrideAspectItemPadding =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectItemPadding,
    );

/// Resolves [LegendMenu.itemColors] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendMenuSelectItemColors(LegendThemeData data) =>
    _itemColors(
      data.tokens,
    ).merge(data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.itemColors);
const _$LegendMenuAspectItemColors = LegendThemeAspect(
  _$LegendMenuSelectItemColors,
);
Object? _$LegendMenuOverrideSelectItemColors(LegendMenuThemeNullable data) =>
    data.itemColors;
const _$LegendMenuOverrideAspectItemColors =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectItemColors,
    );

/// Resolves [LegendMenu.textStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendMenuSelectTextStyle(LegendThemeData data) =>
    data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.textStyle ??
    TextRef.b2(data.tokens);
const _$LegendMenuAspectTextStyle = LegendThemeAspect(
  _$LegendMenuSelectTextStyle,
);
Object? _$LegendMenuOverrideSelectTextStyle(LegendMenuThemeNullable data) =>
    data.textStyle;
const _$LegendMenuOverrideAspectTextStyle =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectTextStyle,
    );

/// Resolves [LegendMenu.destructiveColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendMenuSelectDestructiveColor(LegendThemeData data) =>
    data.componentOf<LegendMenuThemeNullable>(LegendMenu)?.destructiveColor ??
    ColorRef.error(data.tokens);
const _$LegendMenuAspectDestructiveColor = LegendThemeAspect(
  _$LegendMenuSelectDestructiveColor,
);
Object? _$LegendMenuOverrideSelectDestructiveColor(
  LegendMenuThemeNullable data,
) => data.destructiveColor;
const _$LegendMenuOverrideAspectDestructiveColor =
    LegendOverrideAspect<LegendMenuThemeNullable>(
      _$LegendMenuOverrideSelectDestructiveColor,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendMenuThemeListenables {
  /// Change stream of the resolved [LegendMenuTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectBackground);

  /// Change stream of the resolved [LegendMenuTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectBorderRadius);

  /// Change stream of the resolved [LegendMenuTheme.shadows].
  static ValueListenable<List<BoxShadow>> shadows(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectShadows);

  /// Change stream of the resolved [LegendMenuTheme.itemPadding].
  static ValueListenable<EdgeInsetsGeometry> itemPadding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectItemPadding);

  /// Change stream of the resolved [LegendMenuTheme.itemColors].
  static ValueListenable<InteractiveColors> itemColors(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectItemColors);

  /// Change stream of the resolved [LegendMenuTheme.textStyle].
  static ValueListenable<TextStyle> textStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectTextStyle);

  /// Change stream of the resolved [LegendMenuTheme.destructiveColor].
  static ValueListenable<Color> destructiveColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendMenuSelectDestructiveColor);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendMenuThemeResolve on LegendMenu {
  /// [LegendMenuTheme.of] with this widget's constructor params as
  /// level 1.
  LegendMenuTheme _theme(BuildContext context) => LegendMenuTheme.of(
    context,
    LegendMenuThemeNullable(
      background: background,
      borderRadius: borderRadius,
      shadows: shadows,
      itemPadding: itemPadding,
      itemColors: itemColors,
      textStyle: textStyle,
      destructiveColor: destructiveColor,
    ),
  );

  /// Resolves ONLY [LegendMenu.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectBackground);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendMenuSelectBackground(data);
  }

  /// Resolves ONLY [LegendMenu.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectBorderRadius);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendMenuSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendMenu.shadows] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._shadows(context)`).
  List<BoxShadow> _shadows(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectShadows);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectShadows,
    );
    return shadows ?? override?.shadows ?? _$LegendMenuSelectShadows(data);
  }

  /// Resolves ONLY [LegendMenu.itemPadding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemPadding(context)`).
  EdgeInsetsGeometry _itemPadding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectItemPadding);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectItemPadding,
    );
    return itemPadding ??
        override?.itemPadding ??
        _$LegendMenuSelectItemPadding(data);
  }

  /// Resolves ONLY [LegendMenu.itemColors] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemColors(context)`).
  InteractiveColors _itemColors(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectItemColors);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectItemColors,
    );
    return _$LegendMenuSelectItemColors(
      data,
    ).merge(override?.itemColors).merge(itemColors);
  }

  /// Resolves ONLY [LegendMenu.textStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._textStyle(context)`).
  TextStyle _textStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectTextStyle);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectTextStyle,
    );
    return textStyle ??
        override?.textStyle ??
        _$LegendMenuSelectTextStyle(data);
  }

  /// Resolves ONLY [LegendMenu.destructiveColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._destructiveColor(context)`).
  Color _destructiveColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendMenuThemeNullable>(context);
    LegendTheme.depend(context, _$LegendMenuAspectDestructiveColor);
    LegendThemeOverride.depend<LegendMenuThemeNullable>(
      context,
      _$LegendMenuOverrideAspectDestructiveColor,
    );
    return destructiveColor ??
        override?.destructiveColor ??
        _$LegendMenuSelectDestructiveColor(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendMenuState]
/// is this file's `State<LegendMenu>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendMenuThemeState])
/// wins over this extension.
extension _$LegendMenuThemeOn_LegendMenuState on _LegendMenuState {
  LegendMenuTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendMenu>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendMenuThemeState on State<LegendMenu> {
  LegendMenuTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendMenu extends
/// _$LegendMenuBase` receives the resolved
/// [LegendMenuTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendMenuBase extends LegendStatelessWidget<LegendMenuTheme> {
  const _$LegendMenuBase({super.key});

  Color? get background;
  BorderRadius? get borderRadius;
  List<BoxShadow>? get shadows;
  EdgeInsetsGeometry? get itemPadding;
  InteractiveColors? get itemColors;
  TextStyle? get textStyle;
  Color? get destructiveColor;

  @override
  LegendMenuTheme resolveThemeOf(BuildContext context) => LegendMenuTheme.of(
    context,
    LegendMenuThemeNullable(
      background: background,
      borderRadius: borderRadius,
      shadows: shadows,
      itemPadding: itemPadding,
      itemColors: itemColors,
      textStyle: textStyle,
      destructiveColor: destructiveColor,
    ),
  );
}
