// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_breadcrumb.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_breadcrumb.dart';

/// Resolved theme for [LegendBreadcrumb].
class LegendBreadcrumbTheme {
  const LegendBreadcrumbTheme({
    required this.linkStyle,
    required this.currentStyle,
    required this.separatorColor,
    required this.itemPadding,
    required this.spacing,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendBreadcrumbTheme.defaults(LegendTokens t) =>
      LegendBreadcrumbTheme(
        linkStyle: _linkStyle(t),
        currentStyle: _currentStyle(t),
        separatorColor: ColorRef.foreground3(t),
        itemPadding: _itemPadding(t),
        spacing: SizeRef.xs(t),
      );

  final TextStyle linkStyle;
  final TextStyle currentStyle;
  final Color separatorColor;
  final EdgeInsetsGeometry itemPadding;
  final double spacing;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendBreadcrumb] first, [LegendBreadcrumbThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendBreadcrumbTheme of(
    BuildContext context, [
    LegendBreadcrumbThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBreadcrumbThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectLinkStyle);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectLinkStyle,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectCurrentStyle);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectCurrentStyle,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectSeparatorColor);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectSeparatorColor,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectItemPadding);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectItemPadding,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectSpacing);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectSpacing,
    );
    return LegendBreadcrumbTheme.defaults(data.tokens)
        .merge(
          data.componentOf<LegendBreadcrumbThemeNullable>(LegendBreadcrumb),
        )
        .merge(override)
        .merge(local);
  }

  LegendBreadcrumbTheme merge(LegendBreadcrumbThemeNullable? other) {
    if (other == null) return this;
    return LegendBreadcrumbTheme(
      linkStyle: other.linkStyle ?? linkStyle,
      currentStyle: other.currentStyle ?? currentStyle,
      separatorColor: other.separatorColor ?? separatorColor,
      itemPadding: other.itemPadding ?? itemPadding,
      spacing: other.spacing ?? spacing,
    );
  }

  LegendBreadcrumbTheme copyWith({
    TextStyle? linkStyle,
    TextStyle? currentStyle,
    Color? separatorColor,
    EdgeInsetsGeometry? itemPadding,
    double? spacing,
  }) => LegendBreadcrumbTheme(
    linkStyle: linkStyle ?? this.linkStyle,
    currentStyle: currentStyle ?? this.currentStyle,
    separatorColor: separatorColor ?? this.separatorColor,
    itemPadding: itemPadding ?? this.itemPadding,
    spacing: spacing ?? this.spacing,
  );

  static LegendBreadcrumbTheme lerp(
    LegendBreadcrumbTheme a,
    LegendBreadcrumbTheme b,
    double t,
  ) => LegendBreadcrumbTheme(
    linkStyle: t < 0.5 ? a.linkStyle : b.linkStyle,
    currentStyle: t < 0.5 ? a.currentStyle : b.currentStyle,
    separatorColor: Color.lerp(a.separatorColor, b.separatorColor, t)!,
    itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
    spacing: t < 0.5 ? a.spacing : b.spacing,
  );
}

/// Sparse overrides for [LegendBreadcrumbTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendBreadcrumbThemeNullable {
  const LegendBreadcrumbThemeNullable({
    this.linkStyle,
    this.currentStyle,
    this.separatorColor,
    this.itemPadding,
    this.spacing,
  });

  final TextStyle? linkStyle;
  final TextStyle? currentStyle;
  final Color? separatorColor;
  final EdgeInsetsGeometry? itemPadding;
  final double? spacing;

  LegendBreadcrumbThemeNullable merge(LegendBreadcrumbThemeNullable? other) {
    if (other == null) return this;
    return LegendBreadcrumbThemeNullable(
      linkStyle: other.linkStyle ?? linkStyle,
      currentStyle: other.currentStyle ?? currentStyle,
      separatorColor: other.separatorColor ?? separatorColor,
      itemPadding: other.itemPadding ?? itemPadding,
      spacing: other.spacing ?? spacing,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendBreadcrumbThemeNullable &&
          other.linkStyle == linkStyle &&
          other.currentStyle == currentStyle &&
          other.separatorColor == separatorColor &&
          other.itemPadding == itemPadding &&
          other.spacing == spacing;

  @override
  int get hashCode => Object.hashAll([
    linkStyle,
    currentStyle,
    separatorColor,
    itemPadding,
    spacing,
  ]);
}

/// Overrides [LegendBreadcrumbTheme] for a subtree (level 2).
class LegendBreadcrumbThemeOverride extends StatelessWidget {
  const LegendBreadcrumbThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendBreadcrumbThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendBreadcrumbThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendBreadcrumb.linkStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendBreadcrumbSelectLinkStyle(LegendThemeData data) =>
    data
        .componentOf<LegendBreadcrumbThemeNullable>(LegendBreadcrumb)
        ?.linkStyle ??
    _linkStyle(data.tokens);
const _$LegendBreadcrumbAspectLinkStyle = LegendThemeAspect(
  _$LegendBreadcrumbSelectLinkStyle,
);
Object? _$LegendBreadcrumbOverrideSelectLinkStyle(
  LegendBreadcrumbThemeNullable data,
) => data.linkStyle;
const _$LegendBreadcrumbOverrideAspectLinkStyle =
    LegendOverrideAspect<LegendBreadcrumbThemeNullable>(
      _$LegendBreadcrumbOverrideSelectLinkStyle,
    );

/// Resolves [LegendBreadcrumb.currentStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendBreadcrumbSelectCurrentStyle(LegendThemeData data) =>
    data
        .componentOf<LegendBreadcrumbThemeNullable>(LegendBreadcrumb)
        ?.currentStyle ??
    _currentStyle(data.tokens);
const _$LegendBreadcrumbAspectCurrentStyle = LegendThemeAspect(
  _$LegendBreadcrumbSelectCurrentStyle,
);
Object? _$LegendBreadcrumbOverrideSelectCurrentStyle(
  LegendBreadcrumbThemeNullable data,
) => data.currentStyle;
const _$LegendBreadcrumbOverrideAspectCurrentStyle =
    LegendOverrideAspect<LegendBreadcrumbThemeNullable>(
      _$LegendBreadcrumbOverrideSelectCurrentStyle,
    );

/// Resolves [LegendBreadcrumb.separatorColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendBreadcrumbSelectSeparatorColor(LegendThemeData data) =>
    data
        .componentOf<LegendBreadcrumbThemeNullable>(LegendBreadcrumb)
        ?.separatorColor ??
    ColorRef.foreground3(data.tokens);
const _$LegendBreadcrumbAspectSeparatorColor = LegendThemeAspect(
  _$LegendBreadcrumbSelectSeparatorColor,
);
Object? _$LegendBreadcrumbOverrideSelectSeparatorColor(
  LegendBreadcrumbThemeNullable data,
) => data.separatorColor;
const _$LegendBreadcrumbOverrideAspectSeparatorColor =
    LegendOverrideAspect<LegendBreadcrumbThemeNullable>(
      _$LegendBreadcrumbOverrideSelectSeparatorColor,
    );

/// Resolves [LegendBreadcrumb.itemPadding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendBreadcrumbSelectItemPadding(LegendThemeData data) =>
    data
        .componentOf<LegendBreadcrumbThemeNullable>(LegendBreadcrumb)
        ?.itemPadding ??
    _itemPadding(data.tokens);
const _$LegendBreadcrumbAspectItemPadding = LegendThemeAspect(
  _$LegendBreadcrumbSelectItemPadding,
);
Object? _$LegendBreadcrumbOverrideSelectItemPadding(
  LegendBreadcrumbThemeNullable data,
) => data.itemPadding;
const _$LegendBreadcrumbOverrideAspectItemPadding =
    LegendOverrideAspect<LegendBreadcrumbThemeNullable>(
      _$LegendBreadcrumbOverrideSelectItemPadding,
    );

/// Resolves [LegendBreadcrumb.spacing] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendBreadcrumbSelectSpacing(LegendThemeData data) =>
    data
        .componentOf<LegendBreadcrumbThemeNullable>(LegendBreadcrumb)
        ?.spacing ??
    SizeRef.xs(data.tokens);
const _$LegendBreadcrumbAspectSpacing = LegendThemeAspect(
  _$LegendBreadcrumbSelectSpacing,
);
Object? _$LegendBreadcrumbOverrideSelectSpacing(
  LegendBreadcrumbThemeNullable data,
) => data.spacing;
const _$LegendBreadcrumbOverrideAspectSpacing =
    LegendOverrideAspect<LegendBreadcrumbThemeNullable>(
      _$LegendBreadcrumbOverrideSelectSpacing,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendBreadcrumbThemeListenables {
  /// Change stream of the resolved [LegendBreadcrumbTheme.linkStyle].
  static ValueListenable<TextStyle> linkStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBreadcrumbSelectLinkStyle);

  /// Change stream of the resolved [LegendBreadcrumbTheme.currentStyle].
  static ValueListenable<TextStyle> currentStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBreadcrumbSelectCurrentStyle);

  /// Change stream of the resolved [LegendBreadcrumbTheme.separatorColor].
  static ValueListenable<Color> separatorColor(
    Listenable source,
    LegendThemeData Function() data,
  ) =>
      LegendThemeSelector(source, data, _$LegendBreadcrumbSelectSeparatorColor);

  /// Change stream of the resolved [LegendBreadcrumbTheme.itemPadding].
  static ValueListenable<EdgeInsetsGeometry> itemPadding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBreadcrumbSelectItemPadding);

  /// Change stream of the resolved [LegendBreadcrumbTheme.spacing].
  static ValueListenable<double> spacing(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBreadcrumbSelectSpacing);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendBreadcrumbThemeResolve on LegendBreadcrumb {
  /// [LegendBreadcrumbTheme.of] with this widget's constructor params as
  /// level 1.
  LegendBreadcrumbTheme _theme(BuildContext context) =>
      LegendBreadcrumbTheme.of(
        context,
        LegendBreadcrumbThemeNullable(
          linkStyle: linkStyle,
          currentStyle: currentStyle,
          separatorColor: separatorColor,
          itemPadding: itemPadding,
          spacing: spacing,
        ),
      );

  /// Resolves ONLY [LegendBreadcrumb.linkStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._linkStyle(context)`).
  TextStyle _linkStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBreadcrumbThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectLinkStyle);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectLinkStyle,
    );
    return linkStyle ??
        override?.linkStyle ??
        _$LegendBreadcrumbSelectLinkStyle(data);
  }

  /// Resolves ONLY [LegendBreadcrumb.currentStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._currentStyle(context)`).
  TextStyle _currentStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBreadcrumbThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectCurrentStyle);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectCurrentStyle,
    );
    return currentStyle ??
        override?.currentStyle ??
        _$LegendBreadcrumbSelectCurrentStyle(data);
  }

  /// Resolves ONLY [LegendBreadcrumb.separatorColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._separatorColor(context)`).
  Color _separatorColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBreadcrumbThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectSeparatorColor);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectSeparatorColor,
    );
    return separatorColor ??
        override?.separatorColor ??
        _$LegendBreadcrumbSelectSeparatorColor(data);
  }

  /// Resolves ONLY [LegendBreadcrumb.itemPadding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemPadding(context)`).
  EdgeInsetsGeometry _itemPadding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBreadcrumbThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectItemPadding);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectItemPadding,
    );
    return itemPadding ??
        override?.itemPadding ??
        _$LegendBreadcrumbSelectItemPadding(data);
  }

  /// Resolves ONLY [LegendBreadcrumb.spacing] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._spacing(context)`).
  double _spacing(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBreadcrumbThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBreadcrumbAspectSpacing);
    LegendThemeOverride.depend<LegendBreadcrumbThemeNullable>(
      context,
      _$LegendBreadcrumbOverrideAspectSpacing,
    );
    return spacing ??
        override?.spacing ??
        _$LegendBreadcrumbSelectSpacing(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendBreadcrumb extends
/// _$LegendBreadcrumbBase` receives the resolved
/// [LegendBreadcrumbTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendBreadcrumbBase
    extends LegendStatelessWidget<LegendBreadcrumbTheme> {
  const _$LegendBreadcrumbBase({super.key});

  TextStyle? get linkStyle;
  TextStyle? get currentStyle;
  Color? get separatorColor;
  EdgeInsetsGeometry? get itemPadding;
  double? get spacing;

  @override
  LegendBreadcrumbTheme resolveThemeOf(BuildContext context) =>
      LegendBreadcrumbTheme.of(
        context,
        LegendBreadcrumbThemeNullable(
          linkStyle: linkStyle,
          currentStyle: currentStyle,
          separatorColor: separatorColor,
          itemPadding: itemPadding,
          spacing: spacing,
        ),
      );
}
