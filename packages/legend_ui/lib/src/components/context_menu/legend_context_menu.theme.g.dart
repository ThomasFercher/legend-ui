// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_context_menu.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_context_menu.dart';

/// Resolved theme for [LegendContextMenu].
class LegendContextMenuTheme {
  const LegendContextMenuTheme({
    required this.menuBackground,
    required this.menuBorderRadius,
    required this.menuShadows,
    required this.itemPadding,
    required this.textStyle,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendContextMenuTheme.defaults(LegendTokens t) =>
      LegendContextMenuTheme(
        menuBackground: _menuBackground(t),
        menuBorderRadius: _menuBorderRadius(t),
        menuShadows: ShadowRef.medium(t),
        itemPadding: _itemPadding(t),
        textStyle: TextRef.b2(t),
      );

  final LegendStates<Color> menuBackground;
  final BorderRadius menuBorderRadius;
  final List<BoxShadow> menuShadows;
  final EdgeInsetsGeometry itemPadding;
  final TextStyle textStyle;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendContextMenu] first, [LegendContextMenuThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  static LegendContextMenuTheme of(
    BuildContext context, [
    LegendContextMenuThemeNullable? local,
  ]) {
    final data = LegendTheme.of(context);
    return LegendContextMenuTheme.defaults(data.tokens)
        .merge(
          data.componentOf<LegendContextMenuThemeNullable>(LegendContextMenu),
        )
        .merge(
          LegendThemeOverride.maybeOf<LegendContextMenuThemeNullable>(context),
        )
        .merge(local);
  }

  LegendContextMenuTheme merge(LegendContextMenuThemeNullable? other) {
    if (other == null) return this;
    return LegendContextMenuTheme(
      menuBackground: other.menuBackground ?? menuBackground,
      menuBorderRadius: other.menuBorderRadius ?? menuBorderRadius,
      menuShadows: other.menuShadows ?? menuShadows,
      itemPadding: other.itemPadding ?? itemPadding,
      textStyle: other.textStyle ?? textStyle,
    );
  }

  LegendContextMenuTheme copyWith({
    LegendStates<Color>? menuBackground,
    BorderRadius? menuBorderRadius,
    List<BoxShadow>? menuShadows,
    EdgeInsetsGeometry? itemPadding,
    TextStyle? textStyle,
  }) => LegendContextMenuTheme(
    menuBackground: menuBackground ?? this.menuBackground,
    menuBorderRadius: menuBorderRadius ?? this.menuBorderRadius,
    menuShadows: menuShadows ?? this.menuShadows,
    itemPadding: itemPadding ?? this.itemPadding,
    textStyle: textStyle ?? this.textStyle,
  );

  static LegendContextMenuTheme lerp(
    LegendContextMenuTheme a,
    LegendContextMenuTheme b,
    double t,
  ) => LegendContextMenuTheme(
    menuBackground: t < 0.5 ? a.menuBackground : b.menuBackground,
    menuBorderRadius: t < 0.5 ? a.menuBorderRadius : b.menuBorderRadius,
    menuShadows: t < 0.5 ? a.menuShadows : b.menuShadows,
    itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
    textStyle: t < 0.5 ? a.textStyle : b.textStyle,
  );
}

/// Sparse overrides for [LegendContextMenuTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendContextMenuThemeNullable {
  const LegendContextMenuThemeNullable({
    this.menuBackground,
    this.menuBorderRadius,
    this.menuShadows,
    this.itemPadding,
    this.textStyle,
  });

  final LegendStates<Color>? menuBackground;
  final BorderRadius? menuBorderRadius;
  final List<BoxShadow>? menuShadows;
  final EdgeInsetsGeometry? itemPadding;
  final TextStyle? textStyle;

  LegendContextMenuThemeNullable merge(LegendContextMenuThemeNullable? other) {
    if (other == null) return this;
    return LegendContextMenuThemeNullable(
      menuBackground: other.menuBackground ?? menuBackground,
      menuBorderRadius: other.menuBorderRadius ?? menuBorderRadius,
      menuShadows: other.menuShadows ?? menuShadows,
      itemPadding: other.itemPadding ?? itemPadding,
      textStyle: other.textStyle ?? textStyle,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendContextMenuThemeNullable &&
          other.menuBackground == menuBackground &&
          other.menuBorderRadius == menuBorderRadius &&
          other.menuShadows == menuShadows &&
          other.itemPadding == itemPadding &&
          other.textStyle == textStyle;

  @override
  int get hashCode => Object.hashAll([
    menuBackground,
    menuBorderRadius,
    menuShadows,
    itemPadding,
    textStyle,
  ]);
}

/// Overrides [LegendContextMenuTheme] for a subtree (level 2).
class LegendContextMenuThemeOverride extends StatelessWidget {
  const LegendContextMenuThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendContextMenuThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendContextMenuThemeNullable>(
        data: data,
        child: child,
      );
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendContextMenuThemeResolve on LegendContextMenu {
  /// [LegendContextMenuTheme.of] with this widget's constructor params as
  /// level 1.
  LegendContextMenuTheme _theme(BuildContext context) =>
      LegendContextMenuTheme.of(
        context,
        LegendContextMenuThemeNullable(
          menuBackground: menuBackground,
          menuBorderRadius: menuBorderRadius,
          menuShadows: menuShadows,
          itemPadding: itemPadding,
          textStyle: textStyle,
        ),
      );
}
