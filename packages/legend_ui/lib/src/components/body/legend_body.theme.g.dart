// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_body.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_body.dart';

/// Resolved theme for [LegendBody].
class LegendBodyTheme {
  const LegendBodyTheme({
    required this.padding,
    required this.maxContentWidth,
    required this.scrollbarThickness,
    required this.scrollbarRadius,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendBodyTheme.defaults(LegendTokens t) => LegendBodyTheme(
    padding: _padding(t),
    maxContentWidth: null,
    scrollbarThickness: 8,
    scrollbarRadius: Radius.circular(4),
  );

  final EdgeInsetsGeometry padding;
  final double? maxContentWidth;
  final double scrollbarThickness;
  final Radius scrollbarRadius;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendBody] first, [LegendBodyThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendBodyTheme of(
    BuildContext context, [
    LegendBodyThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBodyThemeNullable>(context);
    LegendTheme.depend(context, _$LegendBodyAspectPadding);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$LegendBodyAspectMaxContentWidth);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectMaxContentWidth,
    );
    LegendTheme.depend(context, _$LegendBodyAspectScrollbarThickness);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectScrollbarThickness,
    );
    LegendTheme.depend(context, _$LegendBodyAspectScrollbarRadius);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectScrollbarRadius,
    );
    return LegendBodyTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendBodyThemeNullable>(LegendBody))
        .merge(override)
        .merge(local);
  }

  LegendBodyTheme merge(LegendBodyThemeNullable? other) {
    if (other == null) return this;
    return LegendBodyTheme(
      padding: other.padding ?? padding,
      maxContentWidth: other.maxContentWidth ?? maxContentWidth,
      scrollbarThickness: other.scrollbarThickness ?? scrollbarThickness,
      scrollbarRadius: other.scrollbarRadius ?? scrollbarRadius,
    );
  }

  LegendBodyTheme copyWith({
    EdgeInsetsGeometry? padding,
    double? maxContentWidth,
    double? scrollbarThickness,
    Radius? scrollbarRadius,
  }) => LegendBodyTheme(
    padding: padding ?? this.padding,
    maxContentWidth: maxContentWidth ?? this.maxContentWidth,
    scrollbarThickness: scrollbarThickness ?? this.scrollbarThickness,
    scrollbarRadius: scrollbarRadius ?? this.scrollbarRadius,
  );

  static LegendBodyTheme lerp(LegendBodyTheme a, LegendBodyTheme b, double t) =>
      LegendBodyTheme(
        padding: t < 0.5 ? a.padding : b.padding,
        maxContentWidth: t < 0.5 ? a.maxContentWidth : b.maxContentWidth,
        scrollbarThickness: t < 0.5
            ? a.scrollbarThickness
            : b.scrollbarThickness,
        scrollbarRadius: t < 0.5 ? a.scrollbarRadius : b.scrollbarRadius,
      );
}

/// Sparse overrides for [LegendBodyTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendBodyThemeNullable {
  const LegendBodyThemeNullable({
    this.padding,
    this.maxContentWidth,
    this.scrollbarThickness,
    this.scrollbarRadius,
  });

  final EdgeInsetsGeometry? padding;
  final double? maxContentWidth;
  final double? scrollbarThickness;
  final Radius? scrollbarRadius;

  LegendBodyThemeNullable merge(LegendBodyThemeNullable? other) {
    if (other == null) return this;
    return LegendBodyThemeNullable(
      padding: other.padding ?? padding,
      maxContentWidth: other.maxContentWidth ?? maxContentWidth,
      scrollbarThickness: other.scrollbarThickness ?? scrollbarThickness,
      scrollbarRadius: other.scrollbarRadius ?? scrollbarRadius,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendBodyThemeNullable &&
          other.padding == padding &&
          other.maxContentWidth == maxContentWidth &&
          other.scrollbarThickness == scrollbarThickness &&
          other.scrollbarRadius == scrollbarRadius;

  @override
  int get hashCode => Object.hashAll([
    padding,
    maxContentWidth,
    scrollbarThickness,
    scrollbarRadius,
  ]);
}

/// Overrides [LegendBodyTheme] for a subtree (level 2).
class LegendBodyThemeOverride extends StatelessWidget {
  const LegendBodyThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendBodyThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendBodyThemeNullable>(data: data, child: child);
}

/// Resolves [LegendBody.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendBodySelectPadding(LegendThemeData data) =>
    data.componentOf<LegendBodyThemeNullable>(LegendBody)?.padding ??
    _padding(data.tokens);
const _$LegendBodyAspectPadding = LegendThemeAspect(_$LegendBodySelectPadding);
Object? _$LegendBodyOverrideSelectPadding(LegendBodyThemeNullable data) =>
    data.padding;
const _$LegendBodyOverrideAspectPadding =
    LegendOverrideAspect<LegendBodyThemeNullable>(
      _$LegendBodyOverrideSelectPadding,
    );

/// Resolves [LegendBody.maxContentWidth] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double? _$LegendBodySelectMaxContentWidth(LegendThemeData data) =>
    data.componentOf<LegendBodyThemeNullable>(LegendBody)?.maxContentWidth;
const _$LegendBodyAspectMaxContentWidth = LegendThemeAspect(
  _$LegendBodySelectMaxContentWidth,
);
Object? _$LegendBodyOverrideSelectMaxContentWidth(
  LegendBodyThemeNullable data,
) => data.maxContentWidth;
const _$LegendBodyOverrideAspectMaxContentWidth =
    LegendOverrideAspect<LegendBodyThemeNullable>(
      _$LegendBodyOverrideSelectMaxContentWidth,
    );

/// Resolves [LegendBody.scrollbarThickness] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendBodySelectScrollbarThickness(LegendThemeData data) =>
    data.componentOf<LegendBodyThemeNullable>(LegendBody)?.scrollbarThickness ??
    8;
const _$LegendBodyAspectScrollbarThickness = LegendThemeAspect(
  _$LegendBodySelectScrollbarThickness,
);
Object? _$LegendBodyOverrideSelectScrollbarThickness(
  LegendBodyThemeNullable data,
) => data.scrollbarThickness;
const _$LegendBodyOverrideAspectScrollbarThickness =
    LegendOverrideAspect<LegendBodyThemeNullable>(
      _$LegendBodyOverrideSelectScrollbarThickness,
    );

/// Resolves [LegendBody.scrollbarRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Radius _$LegendBodySelectScrollbarRadius(LegendThemeData data) =>
    data.componentOf<LegendBodyThemeNullable>(LegendBody)?.scrollbarRadius ??
    Radius.circular(4);
const _$LegendBodyAspectScrollbarRadius = LegendThemeAspect(
  _$LegendBodySelectScrollbarRadius,
);
Object? _$LegendBodyOverrideSelectScrollbarRadius(
  LegendBodyThemeNullable data,
) => data.scrollbarRadius;
const _$LegendBodyOverrideAspectScrollbarRadius =
    LegendOverrideAspect<LegendBodyThemeNullable>(
      _$LegendBodyOverrideSelectScrollbarRadius,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendBodyThemeListenables {
  /// Change stream of the resolved [LegendBodyTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBodySelectPadding);

  /// Change stream of the resolved [LegendBodyTheme.maxContentWidth].
  static ValueListenable<double?> maxContentWidth(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBodySelectMaxContentWidth);

  /// Change stream of the resolved [LegendBodyTheme.scrollbarThickness].
  static ValueListenable<double> scrollbarThickness(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBodySelectScrollbarThickness);

  /// Change stream of the resolved [LegendBodyTheme.scrollbarRadius].
  static ValueListenable<Radius> scrollbarRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBodySelectScrollbarRadius);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendBodyThemeResolve on LegendBody {
  /// [LegendBodyTheme.of] with this widget's constructor params as
  /// level 1.
  LegendBodyTheme _theme(BuildContext context) => LegendBodyTheme.of(
    context,
    LegendBodyThemeNullable(
      padding: padding,
      maxContentWidth: maxContentWidth,
      scrollbarThickness: scrollbarThickness,
      scrollbarRadius: scrollbarRadius,
    ),
  );

  /// Resolves ONLY [LegendBody.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBodyThemeNullable>(context);
    LegendTheme.depend(context, _$LegendBodyAspectPadding);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendBodySelectPadding(data);
  }

  /// Resolves ONLY [LegendBody.maxContentWidth] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._maxContentWidth(context)`).
  double? _maxContentWidth(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBodyThemeNullable>(context);
    LegendTheme.depend(context, _$LegendBodyAspectMaxContentWidth);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectMaxContentWidth,
    );
    return maxContentWidth ??
        override?.maxContentWidth ??
        _$LegendBodySelectMaxContentWidth(data);
  }

  /// Resolves ONLY [LegendBody.scrollbarThickness] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._scrollbarThickness(context)`).
  double _scrollbarThickness(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBodyThemeNullable>(context);
    LegendTheme.depend(context, _$LegendBodyAspectScrollbarThickness);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectScrollbarThickness,
    );
    return scrollbarThickness ??
        override?.scrollbarThickness ??
        _$LegendBodySelectScrollbarThickness(data);
  }

  /// Resolves ONLY [LegendBody.scrollbarRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._scrollbarRadius(context)`).
  Radius _scrollbarRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBodyThemeNullable>(context);
    LegendTheme.depend(context, _$LegendBodyAspectScrollbarRadius);
    LegendThemeOverride.depend<LegendBodyThemeNullable>(
      context,
      _$LegendBodyOverrideAspectScrollbarRadius,
    );
    return scrollbarRadius ??
        override?.scrollbarRadius ??
        _$LegendBodySelectScrollbarRadius(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendBodyState]
/// is this file's `State<LegendBody>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendBodyThemeState])
/// wins over this extension.
extension _$LegendBodyThemeOn_LegendBodyState on _LegendBodyState {
  LegendBodyTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendBody>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendBodyThemeState on State<LegendBody> {
  LegendBodyTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendBody extends
/// _$LegendBodyBase` receives the resolved
/// [LegendBodyTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendBodyBase extends LegendStatelessWidget<LegendBodyTheme> {
  const _$LegendBodyBase({super.key});

  EdgeInsetsGeometry? get padding;
  double? get maxContentWidth;
  double? get scrollbarThickness;
  Radius? get scrollbarRadius;

  @override
  LegendBodyTheme resolveThemeOf(BuildContext context) => LegendBodyTheme.of(
    context,
    LegendBodyThemeNullable(
      padding: padding,
      maxContentWidth: maxContentWidth,
      scrollbarThickness: scrollbarThickness,
      scrollbarRadius: scrollbarRadius,
    ),
  );
}
