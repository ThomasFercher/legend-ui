// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_timeline.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_timeline.dart';

/// Resolved theme for [LegendTimeline].
class LegendTimelineTheme {
  const LegendTimelineTheme({
    required this.indicatorSize,
    required this.indicatorColor,
    required this.connectorColor,
    required this.connectorThickness,
    required this.titleStyle,
    required this.descriptionStyle,
    required this.timestampStyle,
    required this.spacing,
    required this.entrySpacing,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendTimelineTheme.defaults(LegendTokens t) => LegendTimelineTheme(
    indicatorSize: 10,
    indicatorColor: ColorRef.primary(t),
    connectorColor: ColorRef.background3(t),
    connectorThickness: SizeRef.borderWidth(t),
    titleStyle: _titleStyle(t),
    descriptionStyle: _descriptionStyle(t),
    timestampStyle: _timestampStyle(t),
    spacing: SizeRef.sm(t),
    entrySpacing: SizeRef.md(t),
  );

  final double indicatorSize;
  final Color indicatorColor;
  final Color connectorColor;
  final double connectorThickness;
  final TextStyle titleStyle;
  final TextStyle descriptionStyle;
  final TextStyle timestampStyle;
  final double spacing;
  final double entrySpacing;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendTimeline] first, [LegendTimelineThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendTimelineTheme of(
    BuildContext context, [
    LegendTimelineThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectIndicatorSize);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectIndicatorSize,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectIndicatorColor);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectIndicatorColor,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectConnectorColor);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectConnectorColor,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectConnectorThickness);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectConnectorThickness,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectTitleStyle);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectTitleStyle,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectDescriptionStyle);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectDescriptionStyle,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectTimestampStyle);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectTimestampStyle,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectSpacing);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectSpacing,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectEntrySpacing);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectEntrySpacing,
    );
    return LegendTimelineTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendTimelineThemeNullable>(LegendTimeline))
        .merge(override)
        .merge(local);
  }

  LegendTimelineTheme merge(LegendTimelineThemeNullable? other) {
    if (other == null) return this;
    return LegendTimelineTheme(
      indicatorSize: other.indicatorSize ?? indicatorSize,
      indicatorColor: other.indicatorColor ?? indicatorColor,
      connectorColor: other.connectorColor ?? connectorColor,
      connectorThickness: other.connectorThickness ?? connectorThickness,
      titleStyle: other.titleStyle ?? titleStyle,
      descriptionStyle: other.descriptionStyle ?? descriptionStyle,
      timestampStyle: other.timestampStyle ?? timestampStyle,
      spacing: other.spacing ?? spacing,
      entrySpacing: other.entrySpacing ?? entrySpacing,
    );
  }

  LegendTimelineTheme copyWith({
    double? indicatorSize,
    Color? indicatorColor,
    Color? connectorColor,
    double? connectorThickness,
    TextStyle? titleStyle,
    TextStyle? descriptionStyle,
    TextStyle? timestampStyle,
    double? spacing,
    double? entrySpacing,
  }) => LegendTimelineTheme(
    indicatorSize: indicatorSize ?? this.indicatorSize,
    indicatorColor: indicatorColor ?? this.indicatorColor,
    connectorColor: connectorColor ?? this.connectorColor,
    connectorThickness: connectorThickness ?? this.connectorThickness,
    titleStyle: titleStyle ?? this.titleStyle,
    descriptionStyle: descriptionStyle ?? this.descriptionStyle,
    timestampStyle: timestampStyle ?? this.timestampStyle,
    spacing: spacing ?? this.spacing,
    entrySpacing: entrySpacing ?? this.entrySpacing,
  );

  static LegendTimelineTheme lerp(
    LegendTimelineTheme a,
    LegendTimelineTheme b,
    double t,
  ) => LegendTimelineTheme(
    indicatorSize: t < 0.5 ? a.indicatorSize : b.indicatorSize,
    indicatorColor: t < 0.5 ? a.indicatorColor : b.indicatorColor,
    connectorColor: t < 0.5 ? a.connectorColor : b.connectorColor,
    connectorThickness: t < 0.5 ? a.connectorThickness : b.connectorThickness,
    titleStyle: t < 0.5 ? a.titleStyle : b.titleStyle,
    descriptionStyle: t < 0.5 ? a.descriptionStyle : b.descriptionStyle,
    timestampStyle: t < 0.5 ? a.timestampStyle : b.timestampStyle,
    spacing: t < 0.5 ? a.spacing : b.spacing,
    entrySpacing: t < 0.5 ? a.entrySpacing : b.entrySpacing,
  );
}

/// Sparse overrides for [LegendTimelineTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendTimelineThemeNullable {
  const LegendTimelineThemeNullable({
    this.indicatorSize,
    this.indicatorColor,
    this.connectorColor,
    this.connectorThickness,
    this.titleStyle,
    this.descriptionStyle,
    this.timestampStyle,
    this.spacing,
    this.entrySpacing,
  });

  final double? indicatorSize;
  final Color? indicatorColor;
  final Color? connectorColor;
  final double? connectorThickness;
  final TextStyle? titleStyle;
  final TextStyle? descriptionStyle;
  final TextStyle? timestampStyle;
  final double? spacing;
  final double? entrySpacing;

  LegendTimelineThemeNullable merge(LegendTimelineThemeNullable? other) {
    if (other == null) return this;
    return LegendTimelineThemeNullable(
      indicatorSize: other.indicatorSize ?? indicatorSize,
      indicatorColor: other.indicatorColor ?? indicatorColor,
      connectorColor: other.connectorColor ?? connectorColor,
      connectorThickness: other.connectorThickness ?? connectorThickness,
      titleStyle: other.titleStyle ?? titleStyle,
      descriptionStyle: other.descriptionStyle ?? descriptionStyle,
      timestampStyle: other.timestampStyle ?? timestampStyle,
      spacing: other.spacing ?? spacing,
      entrySpacing: other.entrySpacing ?? entrySpacing,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendTimelineThemeNullable &&
          other.indicatorSize == indicatorSize &&
          other.indicatorColor == indicatorColor &&
          other.connectorColor == connectorColor &&
          other.connectorThickness == connectorThickness &&
          other.titleStyle == titleStyle &&
          other.descriptionStyle == descriptionStyle &&
          other.timestampStyle == timestampStyle &&
          other.spacing == spacing &&
          other.entrySpacing == entrySpacing;

  @override
  int get hashCode => Object.hashAll([
    indicatorSize,
    indicatorColor,
    connectorColor,
    connectorThickness,
    titleStyle,
    descriptionStyle,
    timestampStyle,
    spacing,
    entrySpacing,
  ]);
}

/// Overrides [LegendTimelineTheme] for a subtree (level 2).
class LegendTimelineThemeOverride extends StatelessWidget {
  const LegendTimelineThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendTimelineThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendTimelineThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendTimeline.indicatorSize] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendTimelineSelectIndicatorSize(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.indicatorSize ??
    10;
const _$LegendTimelineAspectIndicatorSize = LegendThemeAspect(
  _$LegendTimelineSelectIndicatorSize,
);
Object? _$LegendTimelineOverrideSelectIndicatorSize(
  LegendTimelineThemeNullable data,
) => data.indicatorSize;
const _$LegendTimelineOverrideAspectIndicatorSize =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectIndicatorSize,
    );

/// Resolves [LegendTimeline.indicatorColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendTimelineSelectIndicatorColor(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.indicatorColor ??
    ColorRef.primary(data.tokens);
const _$LegendTimelineAspectIndicatorColor = LegendThemeAspect(
  _$LegendTimelineSelectIndicatorColor,
);
Object? _$LegendTimelineOverrideSelectIndicatorColor(
  LegendTimelineThemeNullable data,
) => data.indicatorColor;
const _$LegendTimelineOverrideAspectIndicatorColor =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectIndicatorColor,
    );

/// Resolves [LegendTimeline.connectorColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendTimelineSelectConnectorColor(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.connectorColor ??
    ColorRef.background3(data.tokens);
const _$LegendTimelineAspectConnectorColor = LegendThemeAspect(
  _$LegendTimelineSelectConnectorColor,
);
Object? _$LegendTimelineOverrideSelectConnectorColor(
  LegendTimelineThemeNullable data,
) => data.connectorColor;
const _$LegendTimelineOverrideAspectConnectorColor =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectConnectorColor,
    );

/// Resolves [LegendTimeline.connectorThickness] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendTimelineSelectConnectorThickness(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.connectorThickness ??
    SizeRef.borderWidth(data.tokens);
const _$LegendTimelineAspectConnectorThickness = LegendThemeAspect(
  _$LegendTimelineSelectConnectorThickness,
);
Object? _$LegendTimelineOverrideSelectConnectorThickness(
  LegendTimelineThemeNullable data,
) => data.connectorThickness;
const _$LegendTimelineOverrideAspectConnectorThickness =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectConnectorThickness,
    );

/// Resolves [LegendTimeline.titleStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendTimelineSelectTitleStyle(LegendThemeData data) =>
    data.componentOf<LegendTimelineThemeNullable>(LegendTimeline)?.titleStyle ??
    _titleStyle(data.tokens);
const _$LegendTimelineAspectTitleStyle = LegendThemeAspect(
  _$LegendTimelineSelectTitleStyle,
);
Object? _$LegendTimelineOverrideSelectTitleStyle(
  LegendTimelineThemeNullable data,
) => data.titleStyle;
const _$LegendTimelineOverrideAspectTitleStyle =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectTitleStyle,
    );

/// Resolves [LegendTimeline.descriptionStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendTimelineSelectDescriptionStyle(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.descriptionStyle ??
    _descriptionStyle(data.tokens);
const _$LegendTimelineAspectDescriptionStyle = LegendThemeAspect(
  _$LegendTimelineSelectDescriptionStyle,
);
Object? _$LegendTimelineOverrideSelectDescriptionStyle(
  LegendTimelineThemeNullable data,
) => data.descriptionStyle;
const _$LegendTimelineOverrideAspectDescriptionStyle =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectDescriptionStyle,
    );

/// Resolves [LegendTimeline.timestampStyle] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendTimelineSelectTimestampStyle(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.timestampStyle ??
    _timestampStyle(data.tokens);
const _$LegendTimelineAspectTimestampStyle = LegendThemeAspect(
  _$LegendTimelineSelectTimestampStyle,
);
Object? _$LegendTimelineOverrideSelectTimestampStyle(
  LegendTimelineThemeNullable data,
) => data.timestampStyle;
const _$LegendTimelineOverrideAspectTimestampStyle =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectTimestampStyle,
    );

/// Resolves [LegendTimeline.spacing] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendTimelineSelectSpacing(LegendThemeData data) =>
    data.componentOf<LegendTimelineThemeNullable>(LegendTimeline)?.spacing ??
    SizeRef.sm(data.tokens);
const _$LegendTimelineAspectSpacing = LegendThemeAspect(
  _$LegendTimelineSelectSpacing,
);
Object? _$LegendTimelineOverrideSelectSpacing(
  LegendTimelineThemeNullable data,
) => data.spacing;
const _$LegendTimelineOverrideAspectSpacing =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectSpacing,
    );

/// Resolves [LegendTimeline.entrySpacing] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendTimelineSelectEntrySpacing(LegendThemeData data) =>
    data
        .componentOf<LegendTimelineThemeNullable>(LegendTimeline)
        ?.entrySpacing ??
    SizeRef.md(data.tokens);
const _$LegendTimelineAspectEntrySpacing = LegendThemeAspect(
  _$LegendTimelineSelectEntrySpacing,
);
Object? _$LegendTimelineOverrideSelectEntrySpacing(
  LegendTimelineThemeNullable data,
) => data.entrySpacing;
const _$LegendTimelineOverrideAspectEntrySpacing =
    LegendOverrideAspect<LegendTimelineThemeNullable>(
      _$LegendTimelineOverrideSelectEntrySpacing,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendTimelineThemeListenables {
  /// Change stream of the resolved [LegendTimelineTheme.indicatorSize].
  static ValueListenable<double> indicatorSize(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectIndicatorSize);

  /// Change stream of the resolved [LegendTimelineTheme.indicatorColor].
  static ValueListenable<Color> indicatorColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectIndicatorColor);

  /// Change stream of the resolved [LegendTimelineTheme.connectorColor].
  static ValueListenable<Color> connectorColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectConnectorColor);

  /// Change stream of the resolved [LegendTimelineTheme.connectorThickness].
  static ValueListenable<double> connectorThickness(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendTimelineSelectConnectorThickness,
  );

  /// Change stream of the resolved [LegendTimelineTheme.titleStyle].
  static ValueListenable<TextStyle> titleStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectTitleStyle);

  /// Change stream of the resolved [LegendTimelineTheme.descriptionStyle].
  static ValueListenable<TextStyle> descriptionStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) =>
      LegendThemeSelector(source, data, _$LegendTimelineSelectDescriptionStyle);

  /// Change stream of the resolved [LegendTimelineTheme.timestampStyle].
  static ValueListenable<TextStyle> timestampStyle(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectTimestampStyle);

  /// Change stream of the resolved [LegendTimelineTheme.spacing].
  static ValueListenable<double> spacing(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectSpacing);

  /// Change stream of the resolved [LegendTimelineTheme.entrySpacing].
  static ValueListenable<double> entrySpacing(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendTimelineSelectEntrySpacing);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendTimelineThemeResolve on LegendTimeline {
  /// [LegendTimelineTheme.of] with this widget's constructor params as
  /// level 1.
  LegendTimelineTheme _theme(BuildContext context) => LegendTimelineTheme.of(
    context,
    LegendTimelineThemeNullable(
      indicatorSize: indicatorSize,
      indicatorColor: indicatorColor,
      connectorColor: connectorColor,
      connectorThickness: connectorThickness,
      titleStyle: titleStyle,
      descriptionStyle: descriptionStyle,
      timestampStyle: timestampStyle,
      spacing: spacing,
      entrySpacing: entrySpacing,
    ),
  );

  /// Resolves ONLY [LegendTimeline.indicatorSize] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._indicatorSize(context)`).
  double _indicatorSize(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectIndicatorSize);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectIndicatorSize,
    );
    return indicatorSize ??
        override?.indicatorSize ??
        _$LegendTimelineSelectIndicatorSize(data);
  }

  /// Resolves ONLY [LegendTimeline.indicatorColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._indicatorColor(context)`).
  Color _indicatorColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectIndicatorColor);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectIndicatorColor,
    );
    return indicatorColor ??
        override?.indicatorColor ??
        _$LegendTimelineSelectIndicatorColor(data);
  }

  /// Resolves ONLY [LegendTimeline.connectorColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._connectorColor(context)`).
  Color _connectorColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectConnectorColor);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectConnectorColor,
    );
    return connectorColor ??
        override?.connectorColor ??
        _$LegendTimelineSelectConnectorColor(data);
  }

  /// Resolves ONLY [LegendTimeline.connectorThickness] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._connectorThickness(context)`).
  double _connectorThickness(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectConnectorThickness);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectConnectorThickness,
    );
    return connectorThickness ??
        override?.connectorThickness ??
        _$LegendTimelineSelectConnectorThickness(data);
  }

  /// Resolves ONLY [LegendTimeline.titleStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._titleStyle(context)`).
  TextStyle _titleStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectTitleStyle);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectTitleStyle,
    );
    return titleStyle ??
        override?.titleStyle ??
        _$LegendTimelineSelectTitleStyle(data);
  }

  /// Resolves ONLY [LegendTimeline.descriptionStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._descriptionStyle(context)`).
  TextStyle _descriptionStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectDescriptionStyle);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectDescriptionStyle,
    );
    return descriptionStyle ??
        override?.descriptionStyle ??
        _$LegendTimelineSelectDescriptionStyle(data);
  }

  /// Resolves ONLY [LegendTimeline.timestampStyle] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._timestampStyle(context)`).
  TextStyle _timestampStyle(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectTimestampStyle);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectTimestampStyle,
    );
    return timestampStyle ??
        override?.timestampStyle ??
        _$LegendTimelineSelectTimestampStyle(data);
  }

  /// Resolves ONLY [LegendTimeline.spacing] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._spacing(context)`).
  double _spacing(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectSpacing);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectSpacing,
    );
    return spacing ?? override?.spacing ?? _$LegendTimelineSelectSpacing(data);
  }

  /// Resolves ONLY [LegendTimeline.entrySpacing] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._entrySpacing(context)`).
  double _entrySpacing(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendTimelineThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendTimelineAspectEntrySpacing);
    LegendThemeOverride.depend<LegendTimelineThemeNullable>(
      context,
      _$LegendTimelineOverrideAspectEntrySpacing,
    );
    return entrySpacing ??
        override?.entrySpacing ??
        _$LegendTimelineSelectEntrySpacing(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendTimeline extends
/// _$LegendTimelineBase` receives the resolved
/// [LegendTimelineTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendTimelineBase
    extends LegendStatelessWidget<LegendTimelineTheme> {
  const _$LegendTimelineBase({super.key});

  double? get indicatorSize;
  Color? get indicatorColor;
  Color? get connectorColor;
  double? get connectorThickness;
  TextStyle? get titleStyle;
  TextStyle? get descriptionStyle;
  TextStyle? get timestampStyle;
  double? get spacing;
  double? get entrySpacing;

  @override
  LegendTimelineTheme resolveThemeOf(BuildContext context) =>
      LegendTimelineTheme.of(
        context,
        LegendTimelineThemeNullable(
          indicatorSize: indicatorSize,
          indicatorColor: indicatorColor,
          connectorColor: connectorColor,
          connectorThickness: connectorThickness,
          titleStyle: titleStyle,
          descriptionStyle: descriptionStyle,
          timestampStyle: timestampStyle,
          spacing: spacing,
          entrySpacing: entrySpacing,
        ),
      );
}
