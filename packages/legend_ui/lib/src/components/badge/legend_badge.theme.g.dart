// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_badge.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_badge.dart';

/// Resolved theme for [LegendBadge].
class LegendBadgeTheme {
  const LegendBadgeTheme({
    required this.background,
    required this.foreground,
    required this.dotSize,
    required this.padding,
    required this.ringColor,
    required this.ringWidth,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendBadgeTheme.defaults(LegendTokens t) => LegendBadgeTheme(
    background: ColorRef.error(t),
    foreground: ColorRef.onError(t),
    dotSize: SizeRef.sm(t),
    padding: _padding(t),
    ringColor: ColorRef.surface(t),
    ringWidth: _ringWidth(t),
  );

  final Color background;
  final Color foreground;
  final double dotSize;
  final EdgeInsetsGeometry padding;
  final Color ringColor;
  final double ringWidth;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendBadge] first, [LegendBadgeThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendBadgeTheme of(
    BuildContext context, [
    LegendBadgeThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectBackground);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectForeground);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectForeground,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectDotSize);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectDotSize,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectPadding);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectPadding,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectRingColor);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectRingColor,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectRingWidth);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectRingWidth,
    );
    return LegendBadgeTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendBadgeThemeNullable>(LegendBadge))
        .merge(override)
        .merge(local);
  }

  LegendBadgeTheme merge(LegendBadgeThemeNullable? other) {
    if (other == null) return this;
    return LegendBadgeTheme(
      background: other.background ?? background,
      foreground: other.foreground ?? foreground,
      dotSize: other.dotSize ?? dotSize,
      padding: other.padding ?? padding,
      ringColor: other.ringColor ?? ringColor,
      ringWidth: other.ringWidth ?? ringWidth,
    );
  }

  LegendBadgeTheme copyWith({
    Color? background,
    Color? foreground,
    double? dotSize,
    EdgeInsetsGeometry? padding,
    Color? ringColor,
    double? ringWidth,
  }) => LegendBadgeTheme(
    background: background ?? this.background,
    foreground: foreground ?? this.foreground,
    dotSize: dotSize ?? this.dotSize,
    padding: padding ?? this.padding,
    ringColor: ringColor ?? this.ringColor,
    ringWidth: ringWidth ?? this.ringWidth,
  );

  static LegendBadgeTheme lerp(
    LegendBadgeTheme a,
    LegendBadgeTheme b,
    double t,
  ) => LegendBadgeTheme(
    background: t < 0.5 ? a.background : b.background,
    foreground: t < 0.5 ? a.foreground : b.foreground,
    dotSize: t < 0.5 ? a.dotSize : b.dotSize,
    padding: t < 0.5 ? a.padding : b.padding,
    ringColor: t < 0.5 ? a.ringColor : b.ringColor,
    ringWidth: t < 0.5 ? a.ringWidth : b.ringWidth,
  );
}

/// Sparse overrides for [LegendBadgeTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendBadgeThemeNullable {
  const LegendBadgeThemeNullable({
    this.background,
    this.foreground,
    this.dotSize,
    this.padding,
    this.ringColor,
    this.ringWidth,
  });

  final Color? background;
  final Color? foreground;
  final double? dotSize;
  final EdgeInsetsGeometry? padding;
  final Color? ringColor;
  final double? ringWidth;

  LegendBadgeThemeNullable merge(LegendBadgeThemeNullable? other) {
    if (other == null) return this;
    return LegendBadgeThemeNullable(
      background: other.background ?? background,
      foreground: other.foreground ?? foreground,
      dotSize: other.dotSize ?? dotSize,
      padding: other.padding ?? padding,
      ringColor: other.ringColor ?? ringColor,
      ringWidth: other.ringWidth ?? ringWidth,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendBadgeThemeNullable &&
          other.background == background &&
          other.foreground == foreground &&
          other.dotSize == dotSize &&
          other.padding == padding &&
          other.ringColor == ringColor &&
          other.ringWidth == ringWidth;

  @override
  int get hashCode => Object.hashAll([
    background,
    foreground,
    dotSize,
    padding,
    ringColor,
    ringWidth,
  ]);
}

/// Overrides [LegendBadgeTheme] for a subtree (level 2).
class LegendBadgeThemeOverride extends StatelessWidget {
  const LegendBadgeThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendBadgeThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendBadgeThemeNullable>(data: data, child: child);
}

/// Resolves [LegendBadge.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendBadgeSelectBackground(LegendThemeData data) =>
    data.componentOf<LegendBadgeThemeNullable>(LegendBadge)?.background ??
    ColorRef.error(data.tokens);
const _$LegendBadgeAspectBackground = LegendThemeAspect(
  _$LegendBadgeSelectBackground,
);
Object? _$LegendBadgeOverrideSelectBackground(LegendBadgeThemeNullable data) =>
    data.background;
const _$LegendBadgeOverrideAspectBackground =
    LegendOverrideAspect<LegendBadgeThemeNullable>(
      _$LegendBadgeOverrideSelectBackground,
    );

/// Resolves [LegendBadge.foreground] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendBadgeSelectForeground(LegendThemeData data) =>
    data.componentOf<LegendBadgeThemeNullable>(LegendBadge)?.foreground ??
    ColorRef.onError(data.tokens);
const _$LegendBadgeAspectForeground = LegendThemeAspect(
  _$LegendBadgeSelectForeground,
);
Object? _$LegendBadgeOverrideSelectForeground(LegendBadgeThemeNullable data) =>
    data.foreground;
const _$LegendBadgeOverrideAspectForeground =
    LegendOverrideAspect<LegendBadgeThemeNullable>(
      _$LegendBadgeOverrideSelectForeground,
    );

/// Resolves [LegendBadge.dotSize] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendBadgeSelectDotSize(LegendThemeData data) =>
    data.componentOf<LegendBadgeThemeNullable>(LegendBadge)?.dotSize ??
    SizeRef.sm(data.tokens);
const _$LegendBadgeAspectDotSize = LegendThemeAspect(
  _$LegendBadgeSelectDotSize,
);
Object? _$LegendBadgeOverrideSelectDotSize(LegendBadgeThemeNullable data) =>
    data.dotSize;
const _$LegendBadgeOverrideAspectDotSize =
    LegendOverrideAspect<LegendBadgeThemeNullable>(
      _$LegendBadgeOverrideSelectDotSize,
    );

/// Resolves [LegendBadge.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendBadgeSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendBadgeThemeNullable>(LegendBadge)?.padding ??
    _padding(data.tokens);
const _$LegendBadgeAspectPadding = LegendThemeAspect(
  _$LegendBadgeSelectPadding,
);
Object? _$LegendBadgeOverrideSelectPadding(LegendBadgeThemeNullable data) =>
    data.padding;
const _$LegendBadgeOverrideAspectPadding =
    LegendOverrideAspect<LegendBadgeThemeNullable>(
      _$LegendBadgeOverrideSelectPadding,
    );

/// Resolves [LegendBadge.ringColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendBadgeSelectRingColor(LegendThemeData data) =>
    data.componentOf<LegendBadgeThemeNullable>(LegendBadge)?.ringColor ??
    ColorRef.surface(data.tokens);
const _$LegendBadgeAspectRingColor = LegendThemeAspect(
  _$LegendBadgeSelectRingColor,
);
Object? _$LegendBadgeOverrideSelectRingColor(LegendBadgeThemeNullable data) =>
    data.ringColor;
const _$LegendBadgeOverrideAspectRingColor =
    LegendOverrideAspect<LegendBadgeThemeNullable>(
      _$LegendBadgeOverrideSelectRingColor,
    );

/// Resolves [LegendBadge.ringWidth] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendBadgeSelectRingWidth(LegendThemeData data) =>
    data.componentOf<LegendBadgeThemeNullable>(LegendBadge)?.ringWidth ??
    _ringWidth(data.tokens);
const _$LegendBadgeAspectRingWidth = LegendThemeAspect(
  _$LegendBadgeSelectRingWidth,
);
Object? _$LegendBadgeOverrideSelectRingWidth(LegendBadgeThemeNullable data) =>
    data.ringWidth;
const _$LegendBadgeOverrideAspectRingWidth =
    LegendOverrideAspect<LegendBadgeThemeNullable>(
      _$LegendBadgeOverrideSelectRingWidth,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendBadgeThemeListenables {
  /// Change stream of the resolved [LegendBadgeTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBadgeSelectBackground);

  /// Change stream of the resolved [LegendBadgeTheme.foreground].
  static ValueListenable<Color> foreground(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBadgeSelectForeground);

  /// Change stream of the resolved [LegendBadgeTheme.dotSize].
  static ValueListenable<double> dotSize(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBadgeSelectDotSize);

  /// Change stream of the resolved [LegendBadgeTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBadgeSelectPadding);

  /// Change stream of the resolved [LegendBadgeTheme.ringColor].
  static ValueListenable<Color> ringColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBadgeSelectRingColor);

  /// Change stream of the resolved [LegendBadgeTheme.ringWidth].
  static ValueListenable<double> ringWidth(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendBadgeSelectRingWidth);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendBadgeThemeResolve on LegendBadge {
  /// [LegendBadgeTheme.of] with this widget's constructor params as
  /// level 1.
  LegendBadgeTheme _theme(BuildContext context) => LegendBadgeTheme.of(
    context,
    LegendBadgeThemeNullable(
      background: background,
      foreground: foreground,
      dotSize: dotSize,
      padding: padding,
      ringColor: ringColor,
      ringWidth: ringWidth,
    ),
  );

  /// Resolves ONLY [LegendBadge.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectBackground);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendBadgeSelectBackground(data);
  }

  /// Resolves ONLY [LegendBadge.foreground] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._foreground(context)`).
  Color _foreground(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectForeground);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectForeground,
    );
    return foreground ??
        override?.foreground ??
        _$LegendBadgeSelectForeground(data);
  }

  /// Resolves ONLY [LegendBadge.dotSize] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._dotSize(context)`).
  double _dotSize(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectDotSize);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectDotSize,
    );
    return dotSize ?? override?.dotSize ?? _$LegendBadgeSelectDotSize(data);
  }

  /// Resolves ONLY [LegendBadge.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectPadding);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendBadgeSelectPadding(data);
  }

  /// Resolves ONLY [LegendBadge.ringColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._ringColor(context)`).
  Color _ringColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectRingColor);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectRingColor,
    );
    return ringColor ??
        override?.ringColor ??
        _$LegendBadgeSelectRingColor(data);
  }

  /// Resolves ONLY [LegendBadge.ringWidth] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._ringWidth(context)`).
  double _ringWidth(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendBadgeThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendBadgeAspectRingWidth);
    LegendThemeOverride.depend<LegendBadgeThemeNullable>(
      context,
      _$LegendBadgeOverrideAspectRingWidth,
    );
    return ringWidth ??
        override?.ringWidth ??
        _$LegendBadgeSelectRingWidth(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendBadge extends
/// _$LegendBadgeBase` receives the resolved
/// [LegendBadgeTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendBadgeBase
    extends LegendStatelessWidget<LegendBadgeTheme> {
  const _$LegendBadgeBase({super.key});

  Color? get background;
  Color? get foreground;
  double? get dotSize;
  EdgeInsetsGeometry? get padding;
  Color? get ringColor;
  double? get ringWidth;

  @override
  LegendBadgeTheme resolveThemeOf(BuildContext context) => LegendBadgeTheme.of(
    context,
    LegendBadgeThemeNullable(
      background: background,
      foreground: foreground,
      dotSize: dotSize,
      padding: padding,
      ringColor: ringColor,
      ringWidth: ringWidth,
    ),
  );
}
