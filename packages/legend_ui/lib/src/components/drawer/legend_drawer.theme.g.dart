// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_drawer.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_drawer.dart';

/// Resolved theme for [LegendDrawer].
class LegendDrawerTheme {
  const LegendDrawerTheme({
    required this.background,
    required this.width,
    required this.borderRadius,
    required this.handleColor,
    required this.padding,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendDrawerTheme.defaults(LegendTokens t) => LegendDrawerTheme(
    background: ColorRef.surface(t),
    width: 320,
    borderRadius: _borderRadius(t),
    handleColor: ColorRef.background3(t),
    padding: _padding(t),
  );

  final Color background;
  final double width;
  final BorderRadius borderRadius;
  final Color handleColor;
  final EdgeInsetsGeometry padding;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendDrawer] first, [LegendDrawerThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendDrawerTheme of(
    BuildContext context, [
    LegendDrawerThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendDrawerThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectBackground);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectWidth);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectWidth,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectBorderRadius);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectHandleColor);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectHandleColor,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectPadding);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectPadding,
    );
    return LegendDrawerTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendDrawerThemeNullable>(LegendDrawer))
        .merge(override)
        .merge(local);
  }

  LegendDrawerTheme merge(LegendDrawerThemeNullable? other) {
    if (other == null) return this;
    return LegendDrawerTheme(
      background: other.background ?? background,
      width: other.width ?? width,
      borderRadius: other.borderRadius ?? borderRadius,
      handleColor: other.handleColor ?? handleColor,
      padding: other.padding ?? padding,
    );
  }

  LegendDrawerTheme copyWith({
    Color? background,
    double? width,
    BorderRadius? borderRadius,
    Color? handleColor,
    EdgeInsetsGeometry? padding,
  }) => LegendDrawerTheme(
    background: background ?? this.background,
    width: width ?? this.width,
    borderRadius: borderRadius ?? this.borderRadius,
    handleColor: handleColor ?? this.handleColor,
    padding: padding ?? this.padding,
  );

  static LegendDrawerTheme lerp(
    LegendDrawerTheme a,
    LegendDrawerTheme b,
    double t,
  ) => LegendDrawerTheme(
    background: t < 0.5 ? a.background : b.background,
    width: t < 0.5 ? a.width : b.width,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    handleColor: t < 0.5 ? a.handleColor : b.handleColor,
    padding: t < 0.5 ? a.padding : b.padding,
  );
}

/// Sparse overrides for [LegendDrawerTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendDrawerThemeNullable {
  const LegendDrawerThemeNullable({
    this.background,
    this.width,
    this.borderRadius,
    this.handleColor,
    this.padding,
  });

  final Color? background;
  final double? width;
  final BorderRadius? borderRadius;
  final Color? handleColor;
  final EdgeInsetsGeometry? padding;

  LegendDrawerThemeNullable merge(LegendDrawerThemeNullable? other) {
    if (other == null) return this;
    return LegendDrawerThemeNullable(
      background: other.background ?? background,
      width: other.width ?? width,
      borderRadius: other.borderRadius ?? borderRadius,
      handleColor: other.handleColor ?? handleColor,
      padding: other.padding ?? padding,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendDrawerThemeNullable &&
          other.background == background &&
          other.width == width &&
          other.borderRadius == borderRadius &&
          other.handleColor == handleColor &&
          other.padding == padding;

  @override
  int get hashCode =>
      Object.hashAll([background, width, borderRadius, handleColor, padding]);
}

/// Overrides [LegendDrawerTheme] for a subtree (level 2).
class LegendDrawerThemeOverride extends StatelessWidget {
  const LegendDrawerThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendDrawerThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendDrawerThemeNullable>(data: data, child: child);
}

/// Resolves [LegendDrawer.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendDrawerSelectBackground(LegendThemeData data) =>
    data.componentOf<LegendDrawerThemeNullable>(LegendDrawer)?.background ??
    ColorRef.surface(data.tokens);
const _$LegendDrawerAspectBackground = LegendThemeAspect(
  _$LegendDrawerSelectBackground,
);
Object? _$LegendDrawerOverrideSelectBackground(
  LegendDrawerThemeNullable data,
) => data.background;
const _$LegendDrawerOverrideAspectBackground =
    LegendOverrideAspect<LegendDrawerThemeNullable>(
      _$LegendDrawerOverrideSelectBackground,
    );

/// Resolves [LegendDrawer.width] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendDrawerSelectWidth(LegendThemeData data) =>
    data.componentOf<LegendDrawerThemeNullable>(LegendDrawer)?.width ?? 320;
const _$LegendDrawerAspectWidth = LegendThemeAspect(_$LegendDrawerSelectWidth);
Object? _$LegendDrawerOverrideSelectWidth(LegendDrawerThemeNullable data) =>
    data.width;
const _$LegendDrawerOverrideAspectWidth =
    LegendOverrideAspect<LegendDrawerThemeNullable>(
      _$LegendDrawerOverrideSelectWidth,
    );

/// Resolves [LegendDrawer.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendDrawerSelectBorderRadius(LegendThemeData data) =>
    data.componentOf<LegendDrawerThemeNullable>(LegendDrawer)?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendDrawerAspectBorderRadius = LegendThemeAspect(
  _$LegendDrawerSelectBorderRadius,
);
Object? _$LegendDrawerOverrideSelectBorderRadius(
  LegendDrawerThemeNullable data,
) => data.borderRadius;
const _$LegendDrawerOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendDrawerThemeNullable>(
      _$LegendDrawerOverrideSelectBorderRadius,
    );

/// Resolves [LegendDrawer.handleColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendDrawerSelectHandleColor(LegendThemeData data) =>
    data.componentOf<LegendDrawerThemeNullable>(LegendDrawer)?.handleColor ??
    ColorRef.background3(data.tokens);
const _$LegendDrawerAspectHandleColor = LegendThemeAspect(
  _$LegendDrawerSelectHandleColor,
);
Object? _$LegendDrawerOverrideSelectHandleColor(
  LegendDrawerThemeNullable data,
) => data.handleColor;
const _$LegendDrawerOverrideAspectHandleColor =
    LegendOverrideAspect<LegendDrawerThemeNullable>(
      _$LegendDrawerOverrideSelectHandleColor,
    );

/// Resolves [LegendDrawer.padding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendDrawerSelectPadding(LegendThemeData data) =>
    data.componentOf<LegendDrawerThemeNullable>(LegendDrawer)?.padding ??
    _padding(data.tokens);
const _$LegendDrawerAspectPadding = LegendThemeAspect(
  _$LegendDrawerSelectPadding,
);
Object? _$LegendDrawerOverrideSelectPadding(LegendDrawerThemeNullable data) =>
    data.padding;
const _$LegendDrawerOverrideAspectPadding =
    LegendOverrideAspect<LegendDrawerThemeNullable>(
      _$LegendDrawerOverrideSelectPadding,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendDrawerThemeListenables {
  /// Change stream of the resolved [LegendDrawerTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendDrawerSelectBackground);

  /// Change stream of the resolved [LegendDrawerTheme.width].
  static ValueListenable<double> width(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendDrawerSelectWidth);

  /// Change stream of the resolved [LegendDrawerTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendDrawerSelectBorderRadius);

  /// Change stream of the resolved [LegendDrawerTheme.handleColor].
  static ValueListenable<Color> handleColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendDrawerSelectHandleColor);

  /// Change stream of the resolved [LegendDrawerTheme.padding].
  static ValueListenable<EdgeInsetsGeometry> padding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendDrawerSelectPadding);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendDrawerThemeResolve on LegendDrawer {
  /// [LegendDrawerTheme.of] with this widget's constructor params as
  /// level 1.
  LegendDrawerTheme _theme(BuildContext context) => LegendDrawerTheme.of(
    context,
    LegendDrawerThemeNullable(
      background: background,
      width: width,
      borderRadius: borderRadius,
      handleColor: handleColor,
      padding: padding,
    ),
  );

  /// Resolves ONLY [LegendDrawer.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendDrawerThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectBackground);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendDrawerSelectBackground(data);
  }

  /// Resolves ONLY [LegendDrawer.width] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._width(context)`).
  double _width(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendDrawerThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectWidth);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectWidth,
    );
    return width ?? override?.width ?? _$LegendDrawerSelectWidth(data);
  }

  /// Resolves ONLY [LegendDrawer.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendDrawerThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectBorderRadius);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendDrawerSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendDrawer.handleColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._handleColor(context)`).
  Color _handleColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendDrawerThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectHandleColor);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectHandleColor,
    );
    return handleColor ??
        override?.handleColor ??
        _$LegendDrawerSelectHandleColor(data);
  }

  /// Resolves ONLY [LegendDrawer.padding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._padding(context)`).
  EdgeInsetsGeometry _padding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendDrawerThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendDrawerAspectPadding);
    LegendThemeOverride.depend<LegendDrawerThemeNullable>(
      context,
      _$LegendDrawerOverrideAspectPadding,
    );
    return padding ?? override?.padding ?? _$LegendDrawerSelectPadding(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendDrawerState]
/// is this file's `State<LegendDrawer>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendDrawerThemeState])
/// wins over this extension.
extension _$LegendDrawerThemeOn_LegendDrawerState on _LegendDrawerState {
  LegendDrawerTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendDrawer>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendDrawerThemeState on State<LegendDrawer> {
  LegendDrawerTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendDrawer extends
/// _$LegendDrawerBase` receives the resolved
/// [LegendDrawerTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendDrawerBase
    extends LegendStatelessWidget<LegendDrawerTheme> {
  const _$LegendDrawerBase({super.key});

  Color? get background;
  double? get width;
  BorderRadius? get borderRadius;
  Color? get handleColor;
  EdgeInsetsGeometry? get padding;

  @override
  LegendDrawerTheme resolveThemeOf(BuildContext context) =>
      LegendDrawerTheme.of(
        context,
        LegendDrawerThemeNullable(
          background: background,
          width: width,
          borderRadius: borderRadius,
          handleColor: handleColor,
          padding: padding,
        ),
      );
}
