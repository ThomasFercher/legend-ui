// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_avatar.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_avatar.dart';

/// Resolved theme for [LegendAvatar].
class LegendAvatarTheme {
  const LegendAvatarTheme({
    required this.size,
    required this.background,
    required this.foreground,
    required this.borderRadius,
    required this.border,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendAvatarTheme.defaults(LegendTokens t) => LegendAvatarTheme(
    size: SizeRef.xl(t),
    background: ColorRef.background2(t),
    foreground: _foreground(t),
    borderRadius: BorderRadius.all(Radius.circular(9999)),
    border: null,
  );

  final double size;
  final Color background;
  final TextStyle foreground;
  final BorderRadius borderRadius;
  final BoxBorder? border;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendAvatar] first, [LegendAvatarThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendAvatarTheme of(
    BuildContext context, [
    LegendAvatarThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAvatarThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectSize);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectSize,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectBackground);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectBackground,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectForeground);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectForeground,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectBorderRadius);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectBorderRadius,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectBorder);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectBorder,
    );
    return LegendAvatarTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendAvatarThemeNullable>(LegendAvatar))
        .merge(override)
        .merge(local);
  }

  LegendAvatarTheme merge(LegendAvatarThemeNullable? other) {
    if (other == null) return this;
    return LegendAvatarTheme(
      size: other.size ?? size,
      background: other.background ?? background,
      foreground: other.foreground ?? foreground,
      borderRadius: other.borderRadius ?? borderRadius,
      border: other.border ?? border,
    );
  }

  LegendAvatarTheme copyWith({
    double? size,
    Color? background,
    TextStyle? foreground,
    BorderRadius? borderRadius,
    BoxBorder? border,
  }) => LegendAvatarTheme(
    size: size ?? this.size,
    background: background ?? this.background,
    foreground: foreground ?? this.foreground,
    borderRadius: borderRadius ?? this.borderRadius,
    border: border ?? this.border,
  );

  static LegendAvatarTheme lerp(
    LegendAvatarTheme a,
    LegendAvatarTheme b,
    double t,
  ) => LegendAvatarTheme(
    size: t < 0.5 ? a.size : b.size,
    background: t < 0.5 ? a.background : b.background,
    foreground: t < 0.5 ? a.foreground : b.foreground,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
    border: t < 0.5 ? a.border : b.border,
  );
}

/// Sparse overrides for [LegendAvatarTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendAvatarThemeNullable {
  const LegendAvatarThemeNullable({
    this.size,
    this.background,
    this.foreground,
    this.borderRadius,
    this.border,
  });

  final double? size;
  final Color? background;
  final TextStyle? foreground;
  final BorderRadius? borderRadius;
  final BoxBorder? border;

  LegendAvatarThemeNullable merge(LegendAvatarThemeNullable? other) {
    if (other == null) return this;
    return LegendAvatarThemeNullable(
      size: other.size ?? size,
      background: other.background ?? background,
      foreground: other.foreground ?? foreground,
      borderRadius: other.borderRadius ?? borderRadius,
      border: other.border ?? border,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendAvatarThemeNullable &&
          other.size == size &&
          other.background == background &&
          other.foreground == foreground &&
          other.borderRadius == borderRadius &&
          other.border == border;

  @override
  int get hashCode =>
      Object.hashAll([size, background, foreground, borderRadius, border]);
}

/// Overrides [LegendAvatarTheme] for a subtree (level 2).
class LegendAvatarThemeOverride extends StatelessWidget {
  const LegendAvatarThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendAvatarThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendAvatarThemeNullable>(data: data, child: child);
}

/// Resolves [LegendAvatar.size] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendAvatarSelectSize(LegendThemeData data) =>
    data.componentOf<LegendAvatarThemeNullable>(LegendAvatar)?.size ??
    SizeRef.xl(data.tokens);
const _$LegendAvatarAspectSize = LegendThemeAspect(_$LegendAvatarSelectSize);
Object? _$LegendAvatarOverrideSelectSize(LegendAvatarThemeNullable data) =>
    data.size;
const _$LegendAvatarOverrideAspectSize =
    LegendOverrideAspect<LegendAvatarThemeNullable>(
      _$LegendAvatarOverrideSelectSize,
    );

/// Resolves [LegendAvatar.background] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendAvatarSelectBackground(LegendThemeData data) =>
    data.componentOf<LegendAvatarThemeNullable>(LegendAvatar)?.background ??
    ColorRef.background2(data.tokens);
const _$LegendAvatarAspectBackground = LegendThemeAspect(
  _$LegendAvatarSelectBackground,
);
Object? _$LegendAvatarOverrideSelectBackground(
  LegendAvatarThemeNullable data,
) => data.background;
const _$LegendAvatarOverrideAspectBackground =
    LegendOverrideAspect<LegendAvatarThemeNullable>(
      _$LegendAvatarOverrideSelectBackground,
    );

/// Resolves [LegendAvatar.foreground] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
TextStyle _$LegendAvatarSelectForeground(LegendThemeData data) =>
    data.componentOf<LegendAvatarThemeNullable>(LegendAvatar)?.foreground ??
    _foreground(data.tokens);
const _$LegendAvatarAspectForeground = LegendThemeAspect(
  _$LegendAvatarSelectForeground,
);
Object? _$LegendAvatarOverrideSelectForeground(
  LegendAvatarThemeNullable data,
) => data.foreground;
const _$LegendAvatarOverrideAspectForeground =
    LegendOverrideAspect<LegendAvatarThemeNullable>(
      _$LegendAvatarOverrideSelectForeground,
    );

/// Resolves [LegendAvatar.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendAvatarSelectBorderRadius(LegendThemeData data) =>
    data.componentOf<LegendAvatarThemeNullable>(LegendAvatar)?.borderRadius ??
    BorderRadius.all(Radius.circular(9999));
const _$LegendAvatarAspectBorderRadius = LegendThemeAspect(
  _$LegendAvatarSelectBorderRadius,
);
Object? _$LegendAvatarOverrideSelectBorderRadius(
  LegendAvatarThemeNullable data,
) => data.borderRadius;
const _$LegendAvatarOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendAvatarThemeNullable>(
      _$LegendAvatarOverrideSelectBorderRadius,
    );

/// Resolves [LegendAvatar.border] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BoxBorder? _$LegendAvatarSelectBorder(LegendThemeData data) =>
    data.componentOf<LegendAvatarThemeNullable>(LegendAvatar)?.border;
const _$LegendAvatarAspectBorder = LegendThemeAspect(
  _$LegendAvatarSelectBorder,
);
Object? _$LegendAvatarOverrideSelectBorder(LegendAvatarThemeNullable data) =>
    data.border;
const _$LegendAvatarOverrideAspectBorder =
    LegendOverrideAspect<LegendAvatarThemeNullable>(
      _$LegendAvatarOverrideSelectBorder,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendAvatarThemeListenables {
  /// Change stream of the resolved [LegendAvatarTheme.size].
  static ValueListenable<double> size(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAvatarSelectSize);

  /// Change stream of the resolved [LegendAvatarTheme.background].
  static ValueListenable<Color> background(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAvatarSelectBackground);

  /// Change stream of the resolved [LegendAvatarTheme.foreground].
  static ValueListenable<TextStyle> foreground(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAvatarSelectForeground);

  /// Change stream of the resolved [LegendAvatarTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAvatarSelectBorderRadius);

  /// Change stream of the resolved [LegendAvatarTheme.border].
  static ValueListenable<BoxBorder?> border(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendAvatarSelectBorder);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendAvatarThemeResolve on LegendAvatar {
  /// [LegendAvatarTheme.of] with this widget's constructor params as
  /// level 1.
  LegendAvatarTheme _theme(BuildContext context) => LegendAvatarTheme.of(
    context,
    LegendAvatarThemeNullable(
      size: size,
      background: background,
      foreground: foreground,
      borderRadius: borderRadius,
      border: border,
    ),
  );

  /// Resolves ONLY [LegendAvatar.size] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._size(context)`).
  double _size(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAvatarThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectSize);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectSize,
    );
    return size ?? override?.size ?? _$LegendAvatarSelectSize(data);
  }

  /// Resolves ONLY [LegendAvatar.background] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._background(context)`).
  Color _background(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAvatarThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectBackground);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectBackground,
    );
    return background ??
        override?.background ??
        _$LegendAvatarSelectBackground(data);
  }

  /// Resolves ONLY [LegendAvatar.foreground] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._foreground(context)`).
  TextStyle _foreground(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAvatarThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectForeground);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectForeground,
    );
    return foreground ??
        override?.foreground ??
        _$LegendAvatarSelectForeground(data);
  }

  /// Resolves ONLY [LegendAvatar.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAvatarThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectBorderRadius);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendAvatarSelectBorderRadius(data);
  }

  /// Resolves ONLY [LegendAvatar.border] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._border(context)`).
  BoxBorder? _border(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendAvatarThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendAvatarAspectBorder);
    LegendThemeOverride.depend<LegendAvatarThemeNullable>(
      context,
      _$LegendAvatarOverrideAspectBorder,
    );
    return border ?? override?.border ?? _$LegendAvatarSelectBorder(data);
  }
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendAvatar extends
/// _$LegendAvatarBase` receives the resolved
/// [LegendAvatarTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendAvatarBase
    extends LegendStatelessWidget<LegendAvatarTheme> {
  const _$LegendAvatarBase({super.key});

  double? get size;
  Color? get background;
  TextStyle? get foreground;
  BorderRadius? get borderRadius;
  BoxBorder? get border;

  @override
  LegendAvatarTheme resolveThemeOf(BuildContext context) =>
      LegendAvatarTheme.of(
        context,
        LegendAvatarThemeNullable(
          size: size,
          background: background,
          foreground: foreground,
          borderRadius: borderRadius,
          border: border,
        ),
      );
}
