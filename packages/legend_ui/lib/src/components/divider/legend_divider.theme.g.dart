// GENERATED by legend_gen v0.1.0-dev.0 — do not edit.
// Source: legend_divider.dart
// ignore_for_file: type=lint, unused_import

import 'legend_divider.dart';
import 'package:flutter/widgets.dart';
import 'package:legend_ui/legend_ui.dart';
import 'package:legend_ui/src/annotations/annotations.dart';
import 'package:legend_ui/src/primitives/legend_surface.dart';

/// Resolved theme for [LegendDivider].
class LegendDividerTheme {
  const LegendDividerTheme({
    required this.color,
    required this.thickness,
    required this.spacing,
  });

  /// Token-derived defaults (level 5).
  factory LegendDividerTheme.defaults(LegendTokens t) => LegendDividerTheme(
    color: t.colors.background3,
    thickness: t.sizes.borderWidth,
    spacing: t.sizes.md,
  );

  final Color color;
  final double thickness;
  final double spacing;

  /// Resolves the theme: defaults <- app theme <- subtree
  /// override <- constructor params ([local]).
  static LegendDividerTheme of(
    BuildContext context, [
    LegendDividerThemeNullable? local,
  ]) {
    final data = LegendTheme.of(context);
    return LegendDividerTheme.defaults(data.tokens)
        .merge(data.component<LegendDividerThemeNullable>())
        .merge(LegendThemeOverride.maybeOf<LegendDividerThemeNullable>(context))
        .merge(local);
  }

  LegendDividerTheme merge(LegendDividerThemeNullable? other) {
    if (other == null) return this;
    return LegendDividerTheme(
      color: other.color ?? color,
      thickness: other.thickness ?? thickness,
      spacing: other.spacing ?? spacing,
    );
  }

  LegendDividerTheme copyWith({
    Color? color,
    double? thickness,
    double? spacing,
  }) => LegendDividerTheme(
    color: color ?? this.color,
    thickness: thickness ?? this.thickness,
    spacing: spacing ?? this.spacing,
  );

  static LegendDividerTheme lerp(
    LegendDividerTheme a,
    LegendDividerTheme b,
    double t,
  ) => LegendDividerTheme(
    color: t < 0.5 ? a.color : b.color,
    thickness: t < 0.5 ? a.thickness : b.thickness,
    spacing: t < 0.5 ? a.spacing : b.spacing,
  );
}

/// Sparse overrides for [LegendDividerTheme]; null means "inherit".
class LegendDividerThemeNullable {
  const LegendDividerThemeNullable({this.color, this.thickness, this.spacing});

  final Color? color;
  final double? thickness;
  final double? spacing;

  LegendDividerThemeNullable merge(LegendDividerThemeNullable? other) {
    if (other == null) return this;
    return LegendDividerThemeNullable(
      color: other.color ?? color,
      thickness: other.thickness ?? thickness,
      spacing: other.spacing ?? spacing,
    );
  }
}

/// Overrides [LegendDividerTheme] for a subtree (level 2).
class LegendDividerThemeOverride extends StatelessWidget {
  const LegendDividerThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendDividerThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendDividerThemeNullable>(data: data, child: child);
}
