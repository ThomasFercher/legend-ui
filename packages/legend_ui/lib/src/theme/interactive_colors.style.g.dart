// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: interactive_colors.dart
// ignore_for_file: type=lint, unused_element
part of 'interactive_colors.dart';

/// Generated mechanical members for [InteractiveColors] (RFC-002 R6
/// amendment 7): member-wise sparse [merge] and value
/// `==`/`hashCode`. Applied via `with _$InteractiveColors`; fields are
/// read through a private cast (no abstract getters, so the
/// class declares no overrides).
mixin _$InteractiveColors {
  InteractiveColors get _self => this as InteractiveColors;

  /// Member-wise sparse merge: [other]'s set members win,
  /// unset members inherit — a theme level that names only
  /// one member never clobbers another level's members. Null
  /// passes this instance through unchanged.
  InteractiveColors merge(InteractiveColors? other) {
    if (other == null) return _self;
    return InteractiveColors(
      normal: other.normal ?? _self.normal,
      hovered: other.hovered ?? _self.hovered,
      pressed: other.pressed ?? _self.pressed,
      focused: other.focused ?? _self.focused,
      disabled: other.disabled ?? _self.disabled,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is InteractiveColors &&
          other.normal == _self.normal &&
          other.hovered == _self.hovered &&
          other.pressed == _self.pressed &&
          other.focused == _self.focused &&
          other.disabled == _self.disabled;

  @override
  int get hashCode => Object.hashAll([
    _self.normal,
    _self.hovered,
    _self.pressed,
    _self.focused,
    _self.disabled,
  ]);
}

/// Member-wise lerp for [InteractiveColors] with the type-appropriate
/// lerper per member (nested style classes via their own
/// `lerp` static, members without a lerper step at t=0.5);
/// the public [InteractiveColors.lerp] redirects here.
InteractiveColors? _$InteractiveColorsLerp(
  InteractiveColors? a,
  InteractiveColors? b,
  double t,
) {
  if (identical(a, b)) return a;
  return InteractiveColors(
    normal: Color.lerp(a?.normal, b?.normal, t),
    hovered: Color.lerp(a?.hovered, b?.hovered, t),
    pressed: Color.lerp(a?.pressed, b?.pressed, t),
    focused: Color.lerp(a?.focused, b?.focused, t),
    disabled: Color.lerp(a?.disabled, b?.disabled, t),
  );
}
