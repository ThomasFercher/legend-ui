// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_app_bar.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_app_bar.dart';

/// Resolved theme for [LegendAppBar].
class LegendAppBarTheme {
  const LegendAppBarTheme({
    required this.background,
    required this.height,
    required this.padding,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendAppBarTheme.defaults(LegendTokens t) => LegendAppBarTheme(
    background: LegendAppBar._background(t),
    height: 56,
    padding: LegendAppBar._padding(t),
  );

  final Color background;
  final double height;
  final EdgeInsetsGeometry padding;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendAppBar] first, [LegendAppBarThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  static LegendAppBarTheme of(
    BuildContext context, [
    LegendAppBarThemeNullable? local,
  ]) {
    final data = LegendTheme.of(context);
    final resolved = LegendAppBarTheme.defaults(data.tokens)
        .merge(data.componentOf<LegendAppBarThemeNullable>(LegendAppBar))
        .merge(LegendThemeOverride.maybeOf<LegendAppBarThemeNullable>(context))
        .merge(local);
    return resolved;
  }

  LegendAppBarTheme merge(LegendAppBarThemeNullable? other) {
    if (other == null) return this;
    return LegendAppBarTheme(
      background: other.background ?? background,
      height: other.height ?? height,
      padding: other.padding ?? padding,
    );
  }

  LegendAppBarTheme copyWith({
    Color? background,
    double? height,
    EdgeInsetsGeometry? padding,
  }) => LegendAppBarTheme(
    background: background ?? this.background,
    height: height ?? this.height,
    padding: padding ?? this.padding,
  );

  static LegendAppBarTheme lerp(
    LegendAppBarTheme a,
    LegendAppBarTheme b,
    double t,
  ) => LegendAppBarTheme(
    background: t < 0.5 ? a.background : b.background,
    height: t < 0.5 ? a.height : b.height,
    padding: t < 0.5 ? a.padding : b.padding,
  );
}

/// Sparse overrides for [LegendAppBarTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendAppBarThemeNullable {
  const LegendAppBarThemeNullable({this.background, this.height, this.padding});

  final Color? background;
  final double? height;
  final EdgeInsetsGeometry? padding;

  LegendAppBarThemeNullable merge(LegendAppBarThemeNullable? other) {
    if (other == null) return this;
    return LegendAppBarThemeNullable(
      background: other.background ?? background,
      height: other.height ?? height,
      padding: other.padding ?? padding,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendAppBarThemeNullable &&
          other.background == background &&
          other.height == height &&
          other.padding == padding;

  @override
  int get hashCode => Object.hashAll([background, height, padding]);
}

/// Overrides [LegendAppBarTheme] for a subtree (level 2).
class LegendAppBarThemeOverride extends StatelessWidget {
  const LegendAppBarThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendAppBarThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendAppBarThemeNullable>(data: data, child: child);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendAppBarThemeResolve on LegendAppBar {
  /// [LegendAppBarTheme.of] with this widget's constructor params as
  /// level 1.
  LegendAppBarTheme _theme(BuildContext context) => LegendAppBarTheme.of(
    context,
    LegendAppBarThemeNullable(
      background: background,
      height: height,
      padding: padding,
    ),
  );
}
