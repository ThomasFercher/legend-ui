// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_vertical_menu.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_vertical_menu.dart';

/// Resolved theme for [LegendVerticalMenu].
class LegendVerticalMenuTheme {
  const LegendVerticalMenuTheme({
    required this.itemColors,
    required this.selectedColor,
    required this.unselectedColor,
    required this.selectedBackground,
    required this.itemPadding,
    required this.itemSpacing,
    required this.borderRadius,
  });

  /// Token-derived defaults (level 4) — the annotation
  /// defaults ARE the kit defaults (DESIGN.md §9.9).
  factory LegendVerticalMenuTheme.defaults(LegendTokens t) =>
      LegendVerticalMenuTheme(
        itemColors: _itemColors(t),
        selectedColor: ColorRef.primary(t),
        unselectedColor: ColorRef.foreground2(t),
        selectedBackground: ColorRef.primaryContainer(t),
        itemPadding: _itemPadding(t),
        itemSpacing: SizeRef.xs(t),
        borderRadius: _borderRadius(t),
      );

  final InteractiveColors itemColors;
  final Color selectedColor;
  final Color unselectedColor;
  final Color selectedBackground;
  final EdgeInsetsGeometry itemPadding;
  final double itemSpacing;
  final BorderRadius borderRadius;

  /// Resolves the theme: defaults <- app registry (keyed by
  /// [LegendVerticalMenu] first, [LegendVerticalMenuThemeNullable] as the legacy
  /// fallback — RFC-002 R3) <- subtree override <- constructor
  /// params ([local]).
  ///
  /// Registers one rebuild aspect per `listen: true` field
  /// (RFC-002 R12): the caller rebuilds only when a listened
  /// field's resolved value changes; `listen: false` fields
  /// resolve fresh but never cause a rebuild by themselves.
  static LegendVerticalMenuTheme of(
    BuildContext context, [
    LegendVerticalMenuThemeNullable? local,
  ]) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectItemColors);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectItemColors,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectSelectedColor);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectSelectedColor,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectUnselectedColor);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectUnselectedColor,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectSelectedBackground);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectSelectedBackground,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectItemPadding);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectItemPadding,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectItemSpacing);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectItemSpacing,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectBorderRadius);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectBorderRadius,
    );
    return LegendVerticalMenuTheme.defaults(data.tokens)
        .merge(
          data.componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu),
        )
        .merge(override)
        .merge(local);
  }

  LegendVerticalMenuTheme merge(LegendVerticalMenuThemeNullable? other) {
    if (other == null) return this;
    return LegendVerticalMenuTheme(
      itemColors: itemColors.merge(other.itemColors),
      selectedColor: other.selectedColor ?? selectedColor,
      unselectedColor: other.unselectedColor ?? unselectedColor,
      selectedBackground: other.selectedBackground ?? selectedBackground,
      itemPadding: other.itemPadding ?? itemPadding,
      itemSpacing: other.itemSpacing ?? itemSpacing,
      borderRadius: other.borderRadius ?? borderRadius,
    );
  }

  LegendVerticalMenuTheme copyWith({
    InteractiveColors? itemColors,
    Color? selectedColor,
    Color? unselectedColor,
    Color? selectedBackground,
    EdgeInsetsGeometry? itemPadding,
    double? itemSpacing,
    BorderRadius? borderRadius,
  }) => LegendVerticalMenuTheme(
    itemColors: itemColors ?? this.itemColors,
    selectedColor: selectedColor ?? this.selectedColor,
    unselectedColor: unselectedColor ?? this.unselectedColor,
    selectedBackground: selectedBackground ?? this.selectedBackground,
    itemPadding: itemPadding ?? this.itemPadding,
    itemSpacing: itemSpacing ?? this.itemSpacing,
    borderRadius: borderRadius ?? this.borderRadius,
  );

  static LegendVerticalMenuTheme lerp(
    LegendVerticalMenuTheme a,
    LegendVerticalMenuTheme b,
    double t,
  ) => LegendVerticalMenuTheme(
    itemColors: t < 0.5 ? a.itemColors : b.itemColors,
    selectedColor: Color.lerp(a.selectedColor, b.selectedColor, t)!,
    unselectedColor: Color.lerp(a.unselectedColor, b.unselectedColor, t)!,
    selectedBackground: Color.lerp(
      a.selectedBackground,
      b.selectedBackground,
      t,
    )!,
    itemPadding: t < 0.5 ? a.itemPadding : b.itemPadding,
    itemSpacing: t < 0.5 ? a.itemSpacing : b.itemSpacing,
    borderRadius: t < 0.5 ? a.borderRadius : b.borderRadius,
  );
}

/// Sparse overrides for [LegendVerticalMenuTheme]; null means "inherit".
/// Value-compared (RFC-002 R2), so rebuilding an ancestor
/// with an equal override never invalidates dependents.
class LegendVerticalMenuThemeNullable {
  const LegendVerticalMenuThemeNullable({
    this.itemColors,
    this.selectedColor,
    this.unselectedColor,
    this.selectedBackground,
    this.itemPadding,
    this.itemSpacing,
    this.borderRadius,
  });

  final InteractiveColors? itemColors;
  final Color? selectedColor;
  final Color? unselectedColor;
  final Color? selectedBackground;
  final EdgeInsetsGeometry? itemPadding;
  final double? itemSpacing;
  final BorderRadius? borderRadius;

  LegendVerticalMenuThemeNullable merge(
    LegendVerticalMenuThemeNullable? other,
  ) {
    if (other == null) return this;
    return LegendVerticalMenuThemeNullable(
      itemColors: itemColors?.merge(other.itemColors) ?? other.itemColors,
      selectedColor: other.selectedColor ?? selectedColor,
      unselectedColor: other.unselectedColor ?? unselectedColor,
      selectedBackground: other.selectedBackground ?? selectedBackground,
      itemPadding: other.itemPadding ?? itemPadding,
      itemSpacing: other.itemSpacing ?? itemSpacing,
      borderRadius: other.borderRadius ?? borderRadius,
    );
  }

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendVerticalMenuThemeNullable &&
          other.itemColors == itemColors &&
          other.selectedColor == selectedColor &&
          other.unselectedColor == unselectedColor &&
          other.selectedBackground == selectedBackground &&
          other.itemPadding == itemPadding &&
          other.itemSpacing == itemSpacing &&
          other.borderRadius == borderRadius;

  @override
  int get hashCode => Object.hashAll([
    itemColors,
    selectedColor,
    unselectedColor,
    selectedBackground,
    itemPadding,
    itemSpacing,
    borderRadius,
  ]);
}

/// Overrides [LegendVerticalMenuTheme] for a subtree (level 2).
class LegendVerticalMenuThemeOverride extends StatelessWidget {
  const LegendVerticalMenuThemeOverride({
    required this.data,
    required this.child,
    super.key,
  });

  final LegendVerticalMenuThemeNullable data;
  final Widget child;

  @override
  Widget build(BuildContext context) =>
      LegendThemeOverride<LegendVerticalMenuThemeNullable>(
        data: data,
        child: child,
      );
}

/// Resolves [LegendVerticalMenu.itemColors] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
InteractiveColors _$LegendVerticalMenuSelectItemColors(LegendThemeData data) =>
    _itemColors(data.tokens).merge(
      data
          .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
          ?.itemColors,
    );
const _$LegendVerticalMenuAspectItemColors = LegendThemeAspect(
  _$LegendVerticalMenuSelectItemColors,
);
Object? _$LegendVerticalMenuOverrideSelectItemColors(
  LegendVerticalMenuThemeNullable data,
) => data.itemColors;
const _$LegendVerticalMenuOverrideAspectItemColors =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectItemColors,
    );

/// Resolves [LegendVerticalMenu.selectedColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendVerticalMenuSelectSelectedColor(LegendThemeData data) =>
    data
        .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
        ?.selectedColor ??
    ColorRef.primary(data.tokens);
const _$LegendVerticalMenuAspectSelectedColor = LegendThemeAspect(
  _$LegendVerticalMenuSelectSelectedColor,
);
Object? _$LegendVerticalMenuOverrideSelectSelectedColor(
  LegendVerticalMenuThemeNullable data,
) => data.selectedColor;
const _$LegendVerticalMenuOverrideAspectSelectedColor =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectSelectedColor,
    );

/// Resolves [LegendVerticalMenu.unselectedColor] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendVerticalMenuSelectUnselectedColor(LegendThemeData data) =>
    data
        .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
        ?.unselectedColor ??
    ColorRef.foreground2(data.tokens);
const _$LegendVerticalMenuAspectUnselectedColor = LegendThemeAspect(
  _$LegendVerticalMenuSelectUnselectedColor,
);
Object? _$LegendVerticalMenuOverrideSelectUnselectedColor(
  LegendVerticalMenuThemeNullable data,
) => data.unselectedColor;
const _$LegendVerticalMenuOverrideAspectUnselectedColor =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectUnselectedColor,
    );

/// Resolves [LegendVerticalMenu.selectedBackground] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
Color _$LegendVerticalMenuSelectSelectedBackground(LegendThemeData data) =>
    data
        .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
        ?.selectedBackground ??
    ColorRef.primaryContainer(data.tokens);
const _$LegendVerticalMenuAspectSelectedBackground = LegendThemeAspect(
  _$LegendVerticalMenuSelectSelectedBackground,
);
Object? _$LegendVerticalMenuOverrideSelectSelectedBackground(
  LegendVerticalMenuThemeNullable data,
) => data.selectedBackground;
const _$LegendVerticalMenuOverrideAspectSelectedBackground =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectSelectedBackground,
    );

/// Resolves [LegendVerticalMenu.itemPadding] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
EdgeInsetsGeometry _$LegendVerticalMenuSelectItemPadding(
  LegendThemeData data,
) =>
    data
        .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
        ?.itemPadding ??
    _itemPadding(data.tokens);
const _$LegendVerticalMenuAspectItemPadding = LegendThemeAspect(
  _$LegendVerticalMenuSelectItemPadding,
);
Object? _$LegendVerticalMenuOverrideSelectItemPadding(
  LegendVerticalMenuThemeNullable data,
) => data.itemPadding;
const _$LegendVerticalMenuOverrideAspectItemPadding =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectItemPadding,
    );

/// Resolves [LegendVerticalMenu.itemSpacing] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
double _$LegendVerticalMenuSelectItemSpacing(LegendThemeData data) =>
    data
        .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
        ?.itemSpacing ??
    SizeRef.xs(data.tokens);
const _$LegendVerticalMenuAspectItemSpacing = LegendThemeAspect(
  _$LegendVerticalMenuSelectItemSpacing,
);
Object? _$LegendVerticalMenuOverrideSelectItemSpacing(
  LegendVerticalMenuThemeNullable data,
) => data.itemSpacing;
const _$LegendVerticalMenuOverrideAspectItemSpacing =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectItemSpacing,
    );

/// Resolves [LegendVerticalMenu.borderRadius] through the
/// registry and the token defaults (levels 4+3) — the
/// comparator behind its rebuild aspect and listenable
/// (RFC-002 R12).
BorderRadius _$LegendVerticalMenuSelectBorderRadius(LegendThemeData data) =>
    data
        .componentOf<LegendVerticalMenuThemeNullable>(LegendVerticalMenu)
        ?.borderRadius ??
    _borderRadius(data.tokens);
const _$LegendVerticalMenuAspectBorderRadius = LegendThemeAspect(
  _$LegendVerticalMenuSelectBorderRadius,
);
Object? _$LegendVerticalMenuOverrideSelectBorderRadius(
  LegendVerticalMenuThemeNullable data,
) => data.borderRadius;
const _$LegendVerticalMenuOverrideAspectBorderRadius =
    LegendOverrideAspect<LegendVerticalMenuThemeNullable>(
      _$LegendVerticalMenuOverrideSelectBorderRadius,
    );

/// Distinct-until-changed per-field change streams over a
/// theme source (RFC-002 R12.4) — for animation and
/// imperative consumers that want theme changes without any
/// widget rebuild. Bind `source` to the app theme
/// controller (any [Listenable]) and `data` to its
/// [LegendThemeData] getter; dispose the returned selector
/// when done. Values resolve through registry + token
/// defaults (constructor params and subtree overrides are
/// element-tree concerns and have no controller-level
/// equivalent).
abstract final class LegendVerticalMenuThemeListenables {
  /// Change stream of the resolved [LegendVerticalMenuTheme.itemColors].
  static ValueListenable<InteractiveColors> itemColors(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendVerticalMenuSelectItemColors);

  /// Change stream of the resolved [LegendVerticalMenuTheme.selectedColor].
  static ValueListenable<Color> selectedColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendVerticalMenuSelectSelectedColor,
  );

  /// Change stream of the resolved [LegendVerticalMenuTheme.unselectedColor].
  static ValueListenable<Color> unselectedColor(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendVerticalMenuSelectUnselectedColor,
  );

  /// Change stream of the resolved [LegendVerticalMenuTheme.selectedBackground].
  static ValueListenable<Color> selectedBackground(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(
    source,
    data,
    _$LegendVerticalMenuSelectSelectedBackground,
  );

  /// Change stream of the resolved [LegendVerticalMenuTheme.itemPadding].
  static ValueListenable<EdgeInsetsGeometry> itemPadding(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendVerticalMenuSelectItemPadding);

  /// Change stream of the resolved [LegendVerticalMenuTheme.itemSpacing].
  static ValueListenable<double> itemSpacing(
    Listenable source,
    LegendThemeData Function() data,
  ) => LegendThemeSelector(source, data, _$LegendVerticalMenuSelectItemSpacing);

  /// Change stream of the resolved [LegendVerticalMenuTheme.borderRadius].
  static ValueListenable<BorderRadius> borderRadius(
    Listenable source,
    LegendThemeData Function() data,
  ) =>
      LegendThemeSelector(source, data, _$LegendVerticalMenuSelectBorderRadius);
}

/// In-library resolver (RFC-002 R1): re-lists the themed
/// fields so the widget author never does — build calls
/// `_theme(context)` (`widget._theme(context)` from a State).
extension _$LegendVerticalMenuThemeResolve on LegendVerticalMenu {
  /// [LegendVerticalMenuTheme.of] with this widget's constructor params as
  /// level 1.
  LegendVerticalMenuTheme _theme(BuildContext context) =>
      LegendVerticalMenuTheme.of(
        context,
        LegendVerticalMenuThemeNullable(
          itemColors: itemColors,
          selectedColor: selectedColor,
          unselectedColor: unselectedColor,
          selectedBackground: selectedBackground,
          itemPadding: itemPadding,
          itemSpacing: itemSpacing,
          borderRadius: borderRadius,
        ),
      );

  /// Resolves ONLY [LegendVerticalMenu.itemColors] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemColors(context)`).
  InteractiveColors _itemColors(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectItemColors);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectItemColors,
    );
    return _$LegendVerticalMenuSelectItemColors(
      data,
    ).merge(override?.itemColors).merge(itemColors);
  }

  /// Resolves ONLY [LegendVerticalMenu.selectedColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedColor(context)`).
  Color _selectedColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectSelectedColor);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectSelectedColor,
    );
    return selectedColor ??
        override?.selectedColor ??
        _$LegendVerticalMenuSelectSelectedColor(data);
  }

  /// Resolves ONLY [LegendVerticalMenu.unselectedColor] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._unselectedColor(context)`).
  Color _unselectedColor(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectUnselectedColor);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectUnselectedColor,
    );
    return unselectedColor ??
        override?.unselectedColor ??
        _$LegendVerticalMenuSelectUnselectedColor(data);
  }

  /// Resolves ONLY [LegendVerticalMenu.selectedBackground] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._selectedBackground(context)`).
  Color _selectedBackground(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectSelectedBackground);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectSelectedBackground,
    );
    return selectedBackground ??
        override?.selectedBackground ??
        _$LegendVerticalMenuSelectSelectedBackground(data);
  }

  /// Resolves ONLY [LegendVerticalMenu.itemPadding] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemPadding(context)`).
  EdgeInsetsGeometry _itemPadding(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectItemPadding);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectItemPadding,
    );
    return itemPadding ??
        override?.itemPadding ??
        _$LegendVerticalMenuSelectItemPadding(data);
  }

  /// Resolves ONLY [LegendVerticalMenu.itemSpacing] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._itemSpacing(context)`).
  double _itemSpacing(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectItemSpacing);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectItemSpacing,
    );
    return itemSpacing ??
        override?.itemSpacing ??
        _$LegendVerticalMenuSelectItemSpacing(data);
  }

  /// Resolves ONLY [LegendVerticalMenu.borderRadius] (full
  /// four-level chain), registering just this field's
  /// rebuild aspect (RFC-002 R12.3) — for widgets consuming
  /// one property. When a top-level tear-off shares the
  /// name, call it receiver-qualified
  /// (`this._borderRadius(context)`).
  BorderRadius _borderRadius(BuildContext context) {
    final data = LegendTheme.read(context);
    final override = LegendThemeOverride.read<LegendVerticalMenuThemeNullable>(
      context,
    );
    LegendTheme.depend(context, _$LegendVerticalMenuAspectBorderRadius);
    LegendThemeOverride.depend<LegendVerticalMenuThemeNullable>(
      context,
      _$LegendVerticalMenuOverrideAspectBorderRadius,
    );
    return borderRadius ??
        override?.borderRadius ??
        _$LegendVerticalMenuSelectBorderRadius(data);
  }
}

/// Auto-detected State wiring (RFC-002 R13): [_LegendVerticalMenuState]
/// is this file's `State<LegendVerticalMenu>`, so its
/// build reads the resolved theme as a plain `theme`
/// getter — zero visible wiring. A same-named instance
/// member (e.g. from [_$LegendVerticalMenuThemeState])
/// wins over this extension.
extension _$LegendVerticalMenuThemeOn_LegendVerticalMenuState
    on _LegendVerticalMenuState {
  LegendVerticalMenuTheme get theme => widget._theme(context);
}

/// Explicit State wiring (RFC-002 R13): mix onto any
/// `State<LegendVerticalMenu>` for the `theme` getter as a
/// real, overridable inherited member — no State-class
/// detection involved.
mixin _$LegendVerticalMenuThemeState on State<LegendVerticalMenu> {
  LegendVerticalMenuTheme get theme => widget._theme(context);
}

/// Opt-in two-argument build base (RFC-002 R13, opt-in
/// base): `class LegendVerticalMenu extends
/// _$LegendVerticalMenuBase` receives the resolved
/// [LegendVerticalMenuTheme] as a build parameter. Plain-widget forms stay
/// the default; there is no stateful two-argument variant.
abstract class _$LegendVerticalMenuBase
    extends LegendStatelessWidget<LegendVerticalMenuTheme> {
  const _$LegendVerticalMenuBase({super.key});

  InteractiveColors? get itemColors;
  Color? get selectedColor;
  Color? get unselectedColor;
  Color? get selectedBackground;
  EdgeInsetsGeometry? get itemPadding;
  double? get itemSpacing;
  BorderRadius? get borderRadius;

  @override
  LegendVerticalMenuTheme resolveThemeOf(BuildContext context) =>
      LegendVerticalMenuTheme.of(
        context,
        LegendVerticalMenuThemeNullable(
          itemColors: itemColors,
          selectedColor: selectedColor,
          unselectedColor: unselectedColor,
          selectedBackground: selectedBackground,
          itemPadding: itemPadding,
          itemSpacing: itemSpacing,
          borderRadius: borderRadius,
        ),
      );
}
