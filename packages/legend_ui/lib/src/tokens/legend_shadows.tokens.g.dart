// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_shadows.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_shadows.dart';

/// Generated mechanical members for [LegendShadows] (RFC-002 R5):
/// `copyWith` and value `==`/`hashCode`. Applied via
/// `with _$LegendShadows`; fields are read through a private cast
/// (no abstract getters, so the class declares no overrides).
mixin _$LegendShadows {
  LegendShadows get _self => this as LegendShadows;

  /// Copy with the given fields replaced.
  LegendShadows copyWith({
    List<BoxShadow>? none,
    List<BoxShadow>? low,
    List<BoxShadow>? medium,
    List<BoxShadow>? high,
  }) => LegendShadows(
    none: none ?? _self.none,
    low: low ?? _self.low,
    medium: medium ?? _self.medium,
    high: high ?? _self.high,
  );

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendShadows &&
          _$listEquals(other.none, _self.none) &&
          _$listEquals(other.low, _self.low) &&
          _$listEquals(other.medium, _self.medium) &&
          _$listEquals(other.high, _self.high);

  @override
  int get hashCode => Object.hashAll([
    Object.hashAll(_self.none),
    Object.hashAll(_self.low),
    Object.hashAll(_self.medium),
    Object.hashAll(_self.high),
  ]);
}

/// Member-wise lerp for [LegendShadows]; the public [LegendShadows.lerp]
/// redirects here (part of the one token lerp a theme switch
/// pays, DESIGN.md §2.4).
LegendShadows _$LegendShadowsLerp(LegendShadows a, LegendShadows b, double t) =>
    LegendShadows(
      none: BoxShadow.lerpList(a.none, b.none, t)!,
      low: BoxShadow.lerpList(a.low, b.low, t)!,
      medium: BoxShadow.lerpList(a.medium, b.medium, t)!,
      high: BoxShadow.lerpList(a.high, b.high, t)!,
    );

/// Const tear-off catalog for [LegendShadows] (RFC-002 R10
/// amendment): one static per token field, usable directly
/// inside `@Style<T>.resolve` annotations —
/// `@Style<List<BoxShadow>>.resolve(LegendShadowsRef.none)`.
abstract final class LegendShadowsRef {
  /// Flat — no shadow at all.
  static List<BoxShadow> none(LegendTokens t) => t.shadows.none;

  /// Subtle lift for resting content (cards).
  static List<BoxShadow> low(LegendTokens t) => t.shadows.low;

  /// Clear separation for floating chrome (menus, toasts, bars).
  static List<BoxShadow> medium(LegendTokens t) => t.shadows.medium;

  /// Strong lift for modal surfaces (dialogs).
  static List<BoxShadow> high(LegendTokens t) => t.shadows.high;
}

/// Element-wise list equality (lists compare by identity;
/// equal token sets must compare equal).
bool _$listEquals(List<Object?> a, List<Object?> b) {
  if (identical(a, b)) return true;
  if (a.length != b.length) return false;
  for (var i = 0; i < a.length; i++) {
    if (a[i] != b[i]) return false;
  }
  return true;
}
