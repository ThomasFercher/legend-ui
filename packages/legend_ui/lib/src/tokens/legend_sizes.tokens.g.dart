// GENERATED by legend_gen v0.2.0-dev.0 — do not edit.
// Source: legend_sizes.dart
// ignore_for_file: type=lint, unused_element
part of 'legend_sizes.dart';

/// Generated mechanical members for [LegendSizes] (RFC-002 R5):
/// `copyWith` and value `==`/`hashCode`. Applied via
/// `with _$LegendSizes`; fields are read through a private cast
/// (no abstract getters, so the class declares no overrides).
mixin _$LegendSizes {
  LegendSizes get _self => this as LegendSizes;

  /// Copy with the given fields replaced.
  LegendSizes copyWith({
    double? xs,
    double? sm,
    double? md,
    double? lg,
    double? xl,
    double? xxl,
    double? radiusSm,
    double? radiusMd,
    double? radiusLg,
    double? borderWidth,
    double? iconSm,
    double? iconMd,
    double? iconLg,
  }) => LegendSizes(
    xs: xs ?? _self.xs,
    sm: sm ?? _self.sm,
    md: md ?? _self.md,
    lg: lg ?? _self.lg,
    xl: xl ?? _self.xl,
    xxl: xxl ?? _self.xxl,
    radiusSm: radiusSm ?? _self.radiusSm,
    radiusMd: radiusMd ?? _self.radiusMd,
    radiusLg: radiusLg ?? _self.radiusLg,
    borderWidth: borderWidth ?? _self.borderWidth,
    iconSm: iconSm ?? _self.iconSm,
    iconMd: iconMd ?? _self.iconMd,
    iconLg: iconLg ?? _self.iconLg,
  );

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is LegendSizes &&
          other.xs == _self.xs &&
          other.sm == _self.sm &&
          other.md == _self.md &&
          other.lg == _self.lg &&
          other.xl == _self.xl &&
          other.xxl == _self.xxl &&
          other.radiusSm == _self.radiusSm &&
          other.radiusMd == _self.radiusMd &&
          other.radiusLg == _self.radiusLg &&
          other.borderWidth == _self.borderWidth &&
          other.iconSm == _self.iconSm &&
          other.iconMd == _self.iconMd &&
          other.iconLg == _self.iconLg;

  @override
  int get hashCode => Object.hashAll([
    _self.xs,
    _self.sm,
    _self.md,
    _self.lg,
    _self.xl,
    _self.xxl,
    _self.radiusSm,
    _self.radiusMd,
    _self.radiusLg,
    _self.borderWidth,
    _self.iconSm,
    _self.iconMd,
    _self.iconLg,
  ]);
}

/// Member-wise lerp for [LegendSizes]; the public [LegendSizes.lerp]
/// redirects here (part of the one token lerp a theme switch
/// pays, DESIGN.md §2.4).
LegendSizes _$LegendSizesLerp(
  LegendSizes a,
  LegendSizes b,
  double t,
) => LegendSizes(
  xs: a.xs == b.xs ? a.xs : a.xs * (1.0 - t) + b.xs * t,
  sm: a.sm == b.sm ? a.sm : a.sm * (1.0 - t) + b.sm * t,
  md: a.md == b.md ? a.md : a.md * (1.0 - t) + b.md * t,
  lg: a.lg == b.lg ? a.lg : a.lg * (1.0 - t) + b.lg * t,
  xl: a.xl == b.xl ? a.xl : a.xl * (1.0 - t) + b.xl * t,
  xxl: a.xxl == b.xxl ? a.xxl : a.xxl * (1.0 - t) + b.xxl * t,
  radiusSm: a.radiusSm == b.radiusSm
      ? a.radiusSm
      : a.radiusSm * (1.0 - t) + b.radiusSm * t,
  radiusMd: a.radiusMd == b.radiusMd
      ? a.radiusMd
      : a.radiusMd * (1.0 - t) + b.radiusMd * t,
  radiusLg: a.radiusLg == b.radiusLg
      ? a.radiusLg
      : a.radiusLg * (1.0 - t) + b.radiusLg * t,
  borderWidth: a.borderWidth == b.borderWidth
      ? a.borderWidth
      : a.borderWidth * (1.0 - t) + b.borderWidth * t,
  iconSm: a.iconSm == b.iconSm ? a.iconSm : a.iconSm * (1.0 - t) + b.iconSm * t,
  iconMd: a.iconMd == b.iconMd ? a.iconMd : a.iconMd * (1.0 - t) + b.iconMd * t,
  iconLg: a.iconLg == b.iconLg ? a.iconLg : a.iconLg * (1.0 - t) + b.iconLg * t,
);
